# P1.27 Build 82 Hotfix 7 — Install note

## أهم التغييرات

- إصلاح `SYNC_ATTENDANCE_23505` عبر upsert على `student_id,date` دون استبدال UUID السحابي القديم.
- تنسيق كود الطالب/العائلة تلقائيًا كل خمس خانات.
- تشخيص نشر/عقد بوابة الطالب بدل الرسالة العامة.
- رسالة عملية عند عدم تفعيل Google Provider، وزر Google متاح للدخول/التسجيل.
- تحسين تشخيص إنشاء/ربط المركز؛ لا تعاود P7.3 التاريخية.
- إضافة VERIFY موحد للبوابة/الإشراف/مفتاح الحضور، وإصلاح SQL مستهدف للبوابة فقط عند الحاجة.

## SQL

ابدأ دائمًا بالملف القراءة فقط:

`P1.27_BUILD82_HOTFIX7_VERIFY.sql`

إذا كانت فحوص `portal` فقط هي الفاشلة، خذ Backup ثم شغّل:

`P1.27_BUILD82_HOTFIX7_PORTAL_REPAIR.sql`

لا تشغّل PORTAL_REPAIR إذا كانت فحوص scope/supervision ناقصة؛ أصلح Build 75/76 أولًا.

## Flutter

طبّق modified-files فوق Build 81 الحالي ثم:

```powershell
PowerShell -ExecutionPolicy Bypass -File .\tools\verify_source_prerequisites.ps1
flutter clean
flutter pub get
flutter analyze
flutter test
flutter run
```

لا تشغّل `flutter run` إذا فشل analyze أو test.

## Web/Edge Function

بعد إعداد Google وSecrets الخاصة بالبوابة، أعد نشر الموقع وFunction `student-portal`.
