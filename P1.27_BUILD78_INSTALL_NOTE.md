# P1.27 Build 78 — ملاحظة التثبيت

**Version:** `4.3.0-alpha.24+78`  
**Base:** P1.27 Build 77  
**SQLite:** `26`  
**Supabase:** يحتاج APPLY/VERIFY Build 78  
**Dependency pins:** بلا تغيير عن Build 77

## قبل التثبيت

1. خذ نسخة احتياطية من مجلد المشروع وبيانات التطبيق وقاعدة Supabase.
2. استخدم حزمة `modified-files-only` فوق **Build 77** الحالي ووافق على الاستبدال.
3. احتفظ بـ`pubspec.lock` المعروف نجاحه على جهازك ولا تحذفه.
4. لا تنسخ قاعدة SQLite من جهاز إلى آخر يدويًا؛ التطبيق يرقيها من 25 إلى 26 عند الفتح.

## Supabase

بعد استبدال الملفات نفّذ مرة واحدة فقط:

```text
website/supabase/P1.27_BUILD78_APPLY.sql
```

ثم نفّذ:

```text
website/supabase/P1.27_BUILD78_VERIFY.sql
```

يجب أن تكون كل قيم `passed = true`.

Build 78 SQL incremental فوق Build 76/77 ولا يعيد P7.3 أو Build75/76. يضيف:

- `plans.friday_mode`؛
- `points.amount NUMERIC(10,2)` للنقاط الكسرية؛
- snapshot رقمي لتصحيحات النقاط؛
- جداول مسابقات الجهة الإشرافية وفئاتها ومشاركاتها وتحكيمها؛
- RLS وRPCs المحمية للترشيح والسحب والتحكيم.

## Flutter

من جذر المشروع:

```powershell
flutter clean
flutter pub get
flutter analyze
flutter test
flutter run
```

إذا نجحت، اختبر على نسخة بيانات احتياطية:

- طالب قديم صعودي وطالب قديم نزولي بعد مصالحة المحفوظ؛
- تقرير أسبوعي/شهري وفترة مخصصة مع دفعة صندوق داخل الفترة؛
- تصدير كل الطلاب مع استثناء طالب ومستجد؛
- ملخص صفحة واحدة لكل طالب؛
- تسميع طالب مستأذن والتأكد من ظهور التحذير؛
- منتقي التاريخ الهجري والميلادي؛
- أوضاع الجمعة الثلاثة؛
- مقترح المراجعة وإلغاء اختياره؛
- مكافأة نسبية تعطي 2.5 مثلًا؛
- طباعة مجموعة وفعالية/مسابقة داخل المجموعة.

## الويب

بعد نشر الملفات وتطبيق SQL:

```bash
cd website
npm ci
npm run quality:ci
```

ثم اختبر `/supervision/competitions` بحسابين على الأقل:

1. owner/admin للجهة: إنشاء مسابقة وفئات، فتح التسجيل، بدء التحكيم، نشر النتائج.
2. center_admin لمركز مرتبط: ترشيح طالب وسحب الترشيح قبل التحكيم.

اختبر أن analyst يستطيع القراءة ولا يستطيع إدارة المسابقة.

## ملاحظة الخطوط

حزمة المصدر الكاملة لا تحتوي ملفات Tajawal الثنائية عمدًا. إذا استخدمتها بدل الحزمة الجزئية فاحتفظ بخطوط مشروعك الحالية قبل Flutter build.
