# P1.27 Build 85 Hotfix 10 installation

1. Back up the current Supabase project and local Flutter project.
2. Run `P1.27_BUILD85_HOTFIX10_APPLY.sql` in Supabase SQL Editor.
3. Run `P1.27_BUILD85_HOTFIX10_VERIFY.sql`; every returned `passed` value must be `true`.
4. Apply the Build 85 modified-files package over Build 84 (including the analyzer-clean patch).
5. Redeploy the `website` directory to Vercel.
6. Run `flutter clean`, `flutter pub get`, `flutter analyze`, and `flutter test` before `flutter run`.
7. Test a brand-new Google account: callback -> onboarding -> role -> center/supervision -> select center.
8. Run bidirectional sync. Legacy malformed memorization rows should be skipped and counted rather than terminating sync.

No SQLite migration or dependency upgrade is included in Build 85.
