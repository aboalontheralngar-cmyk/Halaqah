# P1.27 Build 78 Hotfix 2 — التثبيت

**Base:** P1.27 Build 78 Hotfix 1  
**SQLite:** 26 (بلا تغيير)  
**Supabase SQL:** لا يوجد SQL جديد  
**Dependency pins:** بلا تغيير

## ما أصلحه هذا التصحيح

1. **طباعة الخطة الأسبوعية/الشهرية:** يوم الجمعة في وضع `catchup_recitation`
   أصبح صالحًا للطباعة. الحفظ والمراجعة في هذا اليوم تعليمات تدارك وليسا
   نطاقين جديدين، لذلك لا يطلب PDF منهما سورة وآية؛ يبقى السرد/التلاوة فقط
   مطلوبًا كنطاق قرآني دقيق.
   كما لا تظهر بادئة Dart التقنية `Bad state:` في رسالة الخطأ للمستخدم.
2. **الإجازات العارضة في المزامنة:** التطبيق يقرأ حالة `study_suspensions`
   السحابية مرة واحدة ويحسب الفرق، ثم يستدعي `set_study_suspension` فقط
   للتواريخ التي تغيرت فعلًا بدل إعادة إرسال كل التاريخ السابق.
3. **منع المزامنة المتوازية:** إذا بدأت المزامنة التلقائية ثم ضغط المستخدم
   المزامنة اليدوية، لا تُفتح سلسلة Supabase ثانية منافسة. العملية المطابقة
   تنضم إلى العملية الجارية، والاتجاه المختلف ينتظر دوره.
4. تبقى مهلة RPC الشبكية ورسالة الخطأ المنقحة من Hotfix 1 موجودتين.

## مهم جدًا لحزمة full-source المرفوعة

اسم الحزمة التي تم فحصها يحتوي `no-font-binaries`، وهي فعلًا لا تحتوي:

```text
assets/fonts/Tajawal-400.ttf
assets/fonts/Tajawal-700.ttf
```

بينما `pubspec.yaml` ما زال يعتمد عليهما للواجهة وPDF. لا يمكن تضمين ملفات
الخط هنا. انسخهما من نسخة مشروعك السابقة التي كانت تعمل إلى المسارين أعلاه
قبل `flutter run` أو استخدم حزمة الملفات المعدلة فقط فوق مشروعك الحالي.

تشغيل `flutter pub get` وظهور رسائل `(... available)` لا يعني فشلًا؛ هذه
إشعارات عن إصدارات أحدث. الحكم يكون من نهاية الأمر ثم `flutter analyze`.

## خطوات Windows الموصى بها

من جذر المشروع بعد إعادة الخطين:

```powershell
PowerShell -ExecutionPolicy Bypass -File .\tools\verify_source_prerequisites.ps1
flutter clean
flutter pub get
flutter analyze
flutter test
flutter run
```

لا تحذف `pubspec.lock` إذا كانت لديك نسخة معروفة النجاح من المشروع السابق.
إذا كانت الحزمة الكاملة الجديدة بلا lock، فإن `flutter pub get` سينشئه مرة
واحدة؛ احتفظ به بعد نجاح الاختبارات.

## قبول سريع على الهاتف

- افتح خطة تمتد من الأحد إلى السبت ووضع الجمعة `تدارك الفائت + سرد تلاوة`،
  ثم اطبع A4 وطابعة الكاشير.
- نفّذ مزامنة ثنائية مرتين متتاليتين بلا تغيير في الإجازات العارضة؛ المرة
  الثانية يجب ألا تعيد استدعاء RPC لكل التواريخ القديمة.
- أثناء مزامنة تلقائية افتح المزامنة اليدوية؛ يجب ألا تعمل عمليتان سحابيتان
  متوازيتان.
- افصل الإنترنت أثناء مزامنة إجازة عارضة؛ يجب أن تنتهي برسالة عربية مختصرة
  وتبقى البيانات المحلية محفوظة.
