# P1.25 install note — build 70

هذا التحديث **حزمة ترقية مصدر** فوق مشروعك الحالي، وليس مشروعًا مستقلًا يحتوي ملفات الخطوط الثنائية.

## 1) طبّق المصدر فوق النسخة الحالية

استخدم مشروعك الحالي الذي يحتوي أصول الخطوط المحلية من P1.23/build68 أو أحدث، ثم استبدل ملفات المصدر بهذه الحزمة.

يجب أن تبقى هذه الملفات موجودة لديك محليًا:

- `assets/fonts/ReadexPro.ttf` — واجهة Flutter فقط.
- `assets/fonts/Tajawal-400.ttf` — PDF العربي.
- `assets/fonts/Tajawal-700.ttf` — PDF العربي العريض.

ملفات الخطوط الثنائية لا تُضمّن في حزمة النقل نفسها.

## 2) تحقق Flutter

من جذر المشروع:

```powershell
flutter clean
flutter pub get
flutter analyze
flutter test
flutter run
```

لا تقم بترقية الـpackages لمجرد ظهور رسالة وجود إصدارات أحدث قبل إغلاق هذا الإصدار.

بعد تشغيل التطبيق افتح:

**الإعدادات ← إدارة النظام والدعم/التشخيص ← اختبار PDF العربي**

ثم اختبر عمليًا:

1. تقرير طالب خلال فترة.
2. نموذج اختبار قرآن.
3. PDF جماعي أو تقرير حلقة إن أمكن.

إذا كانت صفحة التشخيص سليمة والتقرير الكبير غير سليم، فالمشكلة في قالب ذلك التقرير وليست في تحميل الخط.

## 3) Supabase — لا تستخدم SQL P1.24 القديم

القاعدة التي أعادت:

```text
ERROR: 42P01: relation "public.students" does not exist
```

لا يُعاد عليها تشغيل P1.24 SQL ولا P1.24 verify.

شغّل أولًا فقط:

```text
website/supabase/P1.25_SUPABASE_PREFLIGHT.sql
```

هذا فحص قراءة فقط.

- إذا كانت `recommendation` تبدأ بـ `STOP_`: توقف وأرسل **كل ناتج الفحص** ولا تشغّل repair.
- إذا بدأت بـ `READY_`: خذ نسخة احتياطية ثم شغّل:

```text
website/supabase/migrations/20260809000100_p1_25_cloud_compat.sql
```

وبعد نجاحه فقط:

```text
website/supabase/P1.25_VERIFY.sql
```

لا يُحذف orphan أو duplicate تلقائيًا؛ أي نتيجة غير صفرية تُراجع أولًا.

## 4) ملاحظة القبول

وجود الكود واختبارات العقود لا يغني عن قبول الجهاز الفعلي. لا تعتبر P1.25 نهائية قبل نجاح Flutter على SDK لديك، واختبار PDF والطابعة، واختبار المزامنة/RLS على Supabase الفعلي.
