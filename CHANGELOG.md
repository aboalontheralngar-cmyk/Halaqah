## P1.27 Build 76 Hotfix 1 — Flutter compile fixes — 2026-08-15

- أصلح خطأ `num → int` في شاشة مجموعات التنافس.
- أصلح callback غير صالح نحويًا في اختيار عدد المجموعات.
- أضيف استيراد `AppSemanticColors` المفقود في شاشة الحفظ.
- لا يوجد SQL جديد ولا تغيير في SQLite أو الاعتماديات.
- أضيف validator مستقل لحماية أخطاء التجميع الثلاثة.

## P1.27 Build 75 Hotfix 3 — 2026-08-13

## P1.27 Build 76 — 4.3.0-alpha.22+76 — إكمال المواصفات (2026-08-14)

- أُكملت مزامنة الحذف بدفتر SQLite v25 عام، وتطبيق tombstones قبل upload، وغطاء `mushaf_progress`، وحذف طالب ذري محمي.
- أضيف drill-down إشرافي: جهة → مركز → حلقات → أداء الطلاب مع بقاء analyst للقراءة.
- توسعت مجموعات المستوى المتقارب إلى منافسة أسبوعية فعلية داخل كل مجموعة.
- أصبح الاختبار الشهري مرتبطًا بقالب وستة أسئلة منظمة و`template_id` مع sync ثنائي الاتجاه وحفظ ذري.
- أُغلقت مشاكل تباين AppBar tabs والحالات الدلالية Light/Dark وأضيف اختبار contrast.
- توسع ErrorWidget إلى رمز حادثة مع source/operation، وأصبحت سياسة تقريب نقاط الإنجاز قابلة للضبط.
- الاعتماديات المباشرة لم تتغير عن Hotfix 3 المعروف نجاحه على جهاز المالك.
- SQL الجديد incremental: `P1.27_BUILD76_APPLY.sql` ثم `P1.27_BUILD76_VERIFY.sql`.

- Fixed Pub solver conflict between `printing 5.14.3` (`package:web` 1.x) and `share_plus 9.0.0` (`web ^0.5.0`).
- Pinned `share_plus: 10.0.3`; existing `Share.share` and `Share.shareXFiles` callsites remain compatible.
- Added a regression validator for the dependency pair.
- No SQL/SQLite change; live Build 75 VERIFY is already fully green.

# سجل التحسينات — مشروع حلقتي

توثيق كامل لكل التحسينات والتطويرات على المشروع، الأحدث في الأعلى.

---

## [Hotfix] — P1.27 Build 75 Hotfix 1: توافق Flutter SDK — 2026-08-12

- صُحح تعارض محلل الاعتماديات الذي ظهر على Flutter SDK الحديث: `flutter_test` يثبت `path 1.9.1` بينما Build 75 كان يثبت `path 1.8.3`.
- أصبح قيد المشروع `path: 1.9.1` حتى ينسجم مع نسخة Flutter الحالية ويتيح `flutter pub get`.
- لا يوجد أي تغيير في SQLite أو Supabase لهذا التصحيح، ولا حاجة إلى إعادة تشغيل `P1.27_BUILD75_APPLY.sql`.
- خطأ شاشة Supabase `Failed to fetch (api.supabase.com)` موثق كفشل اتصال/واجهة أثناء تشغيل VERIFY؛ يكفي إعادة محاولة ملف التحقق بعد استقرار لوحة Supabase، دون إعادة APPLY.

## [غير مُصدر] — P1.27 Build 75: مزامنة بلا فقد وخطط أذكى وإشراف مباشر — 2026-08-12

- رُفعت النسخة إلى `4.3.0-alpha.21+75` مع بقاء SQLite عند `24`.
- أضيف إنشاء مركز تابع مباشرة من بوابة الجهة الإشرافية لمالك الجهة/الإداري، مع تشخيص صلاحيات وعقد Build 75 بدل رسالة P7.3 العامة.
- أصبحت نقاط التسميع الآلية نسبية إلى مقدار إنجاز المقرر؛ المكافأة الكاملة لا تُمنح إلا عند 100%، ومكافأة الزيادة تبدأ بعد التجاوز.
- أضيف مصدر موحد للمحفوظ يستنتج الرصيد المتصل من اتجاه الطالب وأبعد سجل فعلي، ويغذي المراجعة وبيان الطالب ومجموعات التنافس.
- تعرض الخطط اسم اليوم والتاريخ الهجري والميلادي في الشاشة وPDF.
- وُحد اسم المنتج `حلقتي` في Android/iOS/Web وأسماء الصور/التقارير التي يولدها التطبيق للمشاركة.
- أصلح التباين في شرائح حفظ/مراجعة والتقييمات، وأضيف اختيار foreground ديناميكي للحالات الملونة.
- أصلح crash محتمل عند حفظ جلسة بلا تقييمات آيات تفصيلية، وأصبح ErrorWidget يعرض رمز حادثة قابلًا للتتبع.
- تحويل يوم سابق إلى إجازة يتراجع فقط عن الغياب والعقوبات وإغلاق اليوم التي أنشئت تلقائيًا ويحافظ على السجلات اليدوية.
- أضيف `CloudAutoSyncCoordinator` للمزامنة عند البدء والاستئناف ودوريًا، و`sync_tombstones` السحابي لمنع جهاز قديم من إعادة صف حُذف على جهاز آخر.
- أضيف تسجيل الحفظ/التسميع/المراجعة لليوم والثلاثة أيام السابقة فقط.
- أضيفت نافذة مجموعات المستويات المتقاربة بعدد مجموعات يختاره المعلم.
- أضيف اختبار الخطة الشهرية: 3 حفظ=30، 3 مراجعة=30، إكمال الحفظ=20، إكمال المراجعة=20.
- أضيف ملف SQL موحد `P1.27_BUILD75_APPLY.sql` وفحص قرائي `P1.27_BUILD75_VERIFY.sql`.
- ارتفع مجموع بوابات الإصدار إلى `58` بإضافة validator P1.27 واختبار runtime لإنشاء المركز الإشرافي وتشخيصه.

### قاعدة البيانات

- لا تعاود P7.3 القديمة. شغّل `website/supabase/P1.27_BUILD75_APPLY.sql` ثم `website/supabase/P1.27_BUILD75_VERIFY.sql`.
- ضمان منع إحياء الحذف بين الأجهزة يعتمد على تطبيق SQL Build 75 لأن ledger السحابي جزء من قاعدة البيانات.

## [غير مُصدر] — P1.26 Build 74: إغلاق التدقيق وإصلاح بوابة الإشراف — 2026-08-12

- رُفعت النسخة إلى `4.3.0-alpha.20+74` مع بقاء SQLite عند `24`.
- أُصلح تاريخ الأعمال في الويب ليستخدم تقويم الجهاز المحلي بدل UTC في حقول `DATE`.
- ثُبتت الاعتماديات المباشرة وأزيل تجاهل ملفات lock، وأضيف preflight للخطوط و`pubspec.lock`.
- فُصل إعداد Supabase إلى Development/Staging/Production ومنع Release من fallback الصامت.
- أزيل سر QR legacy الثابت وعُطلت القراءة القديمة افتراضيًا مع صلاحية زمنية عند تفعيل انتقال صريح.
- فُصل مخطط SQLite وترقياته عن `DatabaseService`، وأضيفت خدمات مستقلة لإعداد السحابة والسجل التشغيلي والإشراف.
- حُولت اشتراكات Zustand العامة إلى selectors، وأضيف Flutter lint baseline، وأزيلت السجلات الخام من Flutter/الويب.
- أضيفت RPCs ذرية لحذف الأسرة وتعديل أفرادها وأولياء أمورها، مع مزامنة رقم ولي الأمر الرئيسي داخل نفس المعاملة.
- أضيف إصلاح idempotent لـP7.3 الجزئي: إزالة العضويات المكررة، إعادة عضوية المالك، إعادة RPC/RLS/grants، health RPC، وعقد الزيارات.
- استُبدلت رسالة P7.3 العامة في onboarding وبوابة الإشراف بتشخيص يميز نقص RPC والصلاحيات والعضوية.
- أضيف `P1.26_BUILD74_APPLY.sql` و`P1.26_BUILD74_VERIFY.sql` ومسار تثبيت موحد.
- ارتفع مجموع بوابات الإصدار إلى `56` بإضافة validator تشديد Build 74 واختبار runtime للتاريخ المحلي وتشخيص الإشراف.
- وُثقت Android/Web كمنصتي الإنتاج الحاليتين؛ بقية Flutter scaffolding غير معتمدة للنشر.

### قاعدة البيانات

- لا تُعاد P7.3 القديمة على مخطط المالك الحالي.
- شغّل `website/supabase/P1.26_BUILD74_APPLY.sql` بعد نسخة احتياطية، ثم `website/supabase/P1.26_BUILD74_VERIFY.sql` ويجب أن تكون كل `passed=true`.

## [غير مُصدر] — المرحلة P1.26: خط موحد وتباين وصيانة المسابقات وتدقيق سحابي عميق — 2026-08-09

- رُفعت النسخة إلى `4.3.0-alpha.19+73 (P1.26)` مع بقاء SQLite عند `24`.
- وُحد خط Flutter وتقارير PDF على Tajawal 400/700 الثابتين، وأزيلت إحالات `Amiri` غير المعلنة من شاشات القرآن/الاختبار حتى لا يعتمد العرض على fallback مختلف بين الأجهزة.
- عُزز `TextTheme` والثيمات الفرعية لتستخدم `ColorScheme.onSurface/onSurfaceVariant`، ونُظفت أسطح ونصوص رمادية ثابتة في الشاشات الأعلى استخدامًا لتحسين القراءة في الوضعين الفاتح والداكن.
- أضيف تعديل اسم/فئة المسابقة وحذف المسابقة من قائمة المسابقات، مع تأكيد واضح وحذف ذري للنتائج التابعة.
- استبدل عدّ نتائج المسابقات باستعلام تجميعي واحد بدل استعلام لكل مسابقة.
- نتيجة P1.25 preflight التي تحمل `STOP_BASE_SCOPE_MISSING` أصبحت حاجزًا صريحًا: لا يُشغّل بعدها migration P1.25 ولا verify.
- أضيف `P1.26_SUPABASE_DEEP_AUDIT.sql` للقراءة فقط لتمييز مشروع فارغ عن مخطط جزئي/يتيم قبل أي إنشاء.
- أضيف `P1.26_GUARDED_BASE_BOOTSTRAP.sql` لإنشاء base scope فقط عندما يثبت الفحص أن جميع جداول حلقتي المستهدفة غير موجودة؛ السكربت يتوقف قبل أي تغيير إذا وجد أي جدول تطبيقي قائم.
- أضيفت بوابة P1.26 لحماية الخط الموحد، صيانة المسابقات، تباين الواجهات، وشرط الأمان في مسار Supabase.
- Hotfix build 73: إصلاح سبعة أخطاء تجميع ناتجة عن `Theme.of(context)` داخل `const Text` ظهرت في `flutter analyze/test/run` على جهاز المالك.
- بعد نجاح guarded base bootstrap، تمت مطابقة مخطط Supabase المرسل وإضافة `P1.26_POST_BOOTSTRAP_CORE.sql` لإكمال جداول حلقتي المفقودة فقط، مع verify قراءة فقط وبدون لمس جداول workload الآخر الموجودة في نفس المشروع.

### قاعدة البيانات

- نفّذ المالك guarded base bootstrap بنجاح، والمخطط المرسل بعده يثبت وجود `profiles/supervisors/centers/halaqat/center_members/families/family_guardians`.
- الخطوة الحالية هي `P1.26_POST_BOOTSTRAP_CORE.sql` ثم `P1.26_POST_BOOTSTRAP_VERIFY.sql` فقط بعد نجاح الأول.
- جداول workload الآخر في نفس Supabase موثقة كحد أمان ولا تستهدفها migration حلقتي.

## [غير مُصدر] — المرحلة P1.25: توافق السحابة وPDF ثابت وتسريع المسارات الثقيلة — 2026-08-09

- رُفعت النسخة إلى `4.3.0-alpha.18+70 (P1.25)` ومخطط SQLite إلى `24`.
- أُلغي الاعتماد على migration P1.24 في القاعدة الحالية بعد ظهور غياب `public.students`؛ أضيف preflight للقراءة فقط وmigration توافق إضافي يتوقف عند غياب نطاق الصلاحيات أو اختلاف أنواع المفاتيح.
- أُضيف verify جديد يدقق الأعمدة وUUID والمفاتيح الخارجية والأيتام والمفاتيح الطبيعية المكررة من دون حذف تلقائي.
- استبدل PDF استخدام Readex Pro المتغير بخطي Tajawal 400/700 الثابتين فقط، مع RTL صريح لكل صفحة وعكس فعلي لأعمدة الجداول، وأضيف اختبار PDF عربي من مركز التشخيص.
- بقيت واجهة Flutter على Readex Pro حفاظًا على المقاسات والتوزيع المعتمد؛ اختلاف الخط في PDF استثناء تقني موثق.
- صحح قياس النقاط التلقائية للصفحات والأحزاب ليعتمد نسبة الأسطر الفعلية، ويزيل تكرار الآيات المتداخلة، ويمنع مكافأة زيادة وهمية عند إتمام وجه واحد بالضبط.
- أضيف سجل `rules_config_history` وsnapshot لقواعد النقاط التلقائية لمنع تغيير النتائج التاريخية عند تعديل الإعدادات.
- أضيفت فهارس SQLite للمسارات الأعلى استخدامًا، وجُمعت قراءات تقرير الفترة بدل N+1 لكل طالب.
- جُمعت تنزيلات الحضور والعائلات والسلوك والإجازات والاختبارات والتلقين والإداريات والصندوق والإشعارات وتميز اليوم والدورات في معاملات/دفعات محلية لتقليل زمن المزامنة وI/O.
- حُدث تدقيق الميزات المتبقية: البنود القديمة المفقودة في المصدر أغلقت، والمتبقي قبول جهاز/سحابة/طابعة أو تكاملات اختيارية.
- نجحت بوابات المصدر حتى P1.20، وتوقفت السلسلة عند P1.21 لغياب `website/node_modules/typescript` في الحزمة؛ نجحت P1.22–P1.25 منفردة. Flutter/Dart SDK غير متاح في بيئة العمل الحالية.

### قاعدة البيانات

- لا تعاود تشغيل P1.24 SQL أو verify على القاعدة التي فشل فيها `public.students`.
- شغّل `P1.25_SUPABASE_PREFLIGHT.sql` أولًا. عند `READY_...` فقط شغّل migration P1.25 ثم `P1.25_VERIFY.sql`.
- عند `STOP_...` لا تغيّر القاعدة وأرسل الناتج الكامل للمراجعة.

## [غير مُصدر] — المرحلة P1.23: إصلاح PDF وتحديث UI/UX وتنظيف Flutter — 2026-08-08

- رُفعت النسخة إلى `4.3.0-alpha.16+68 (P1.23)` من دون تغيير SQLite 22 أو إضافة SQL جديد.
- Hotfix build 68: أزيل `visualDensity` غير المدعوم من `ChipThemeData.copyWith` في الثيم؛ الكثافة المضغوطة تبقى مطبقة من `ThemeData.visualDensity`، ما يعالج فشل تجميع Flutter الذي ظهر على جهاز المالك.
- أُصلح تصدير العربية في PDF من الجذر بإعلان Tajawal العادي والعريض كـassets، وإلغاء Helvetica fallback، وتطبيق ثيم خط عربي موحد على كل `pw.Document` وصفحاته.
- عولجت الأنماط المتقادمة والتحذيرات الظاهرة في سجل `flutter analyze` الذي أرسله المالك، من دون ترقية حزم الاعتماديات جماعيًا.
- أُعيد بناء الثيم العام والمكونات المشتركة إلى تصميم مسطح ومضغوط: AppBar كحلي، أخضر للإجراء، خلفية فاتحة هادئة، خطوط أصغر، بطاقات بحدود خفيفة وأيقونات خطية.
- أُزيلت التدرجات والظلال الثقيلة المتبقية من واجهات Flutter، وضُغطت الشاشات الأعلى استخدامًا مع الحفاظ على تدفقات العمل والبيانات.
- وُحدت متغيرات CSS الأساسية في الويب مع الهوية الجديدة.
- أُجل تحميل بيانات الرئيسية الثقيلة إلى ما بعد الإطار الأول لمعالجة التقطيع المرصود على الجهاز عند البداية.
- وُثقت دراسة الواجهات العامة لتطبيق «قرضة» في Google Play كمصدر إلهام للمبادئ فقط، دون نسخ شاشة أو الوصول إلى المصدر.
- أكد المالك نجاح SQL الخاص بـP1.22 وفحصه؛ الشرطان `closing_respects_activity_exemption` و`closing_respects_full_pause` ظهرا `true`.
- أضيفت بوابة P1.23 لحماية الخط العربي في PDF والهوية الجديدة ومنع رجوع APIs المتقادمة المعالجة.
- نجحت 50 من 51 بوابة مصدر قابلة للتشغيل في الحزمة النظيفة؛ بوابة P1.21 وحدها تحتاج حزمة `typescript` غير المضمّنة في `node_modules`.

### قاعدة البيانات

- لا يوجد migration جديد في P1.23.
- migration P1.22 منفذ ومتحقق لدى المالك بتاريخ 2026-08-08.

## [غير مُصدر] — المرحلة P1.22: تشغيل يومي مرن وتلقين وتسميع ممتد — 2026-08-08

- رُفعت النسخة إلى `4.3.0-alpha.15+66 (P1.22)` ومخطط SQLite إلى `22`.
- أضيفت فعاليات اليوم للطلاب: محاضرة، نشاط، دوري، عشاء، رياضة، مسابقة ثقافية وأخرى؛ تسجل حضورًا مع إعفاء من إلزام التسميع لذلك اليوم، من دون توليد «لم يسمّع».
- توسع إيقاف الطالب إلى إيقاف تسميع فقط أو توقف كامل للدراسة/العمل/الظرف، مع استثنائه من الغياب والتسميع والإغلاق اليومي خلال المدة.
- أضيف مسار تلقين مستقل بسجل يومي ونطاق قرآني متصل، وأضيف التسميع والحفظ من سورة إلى سورة أخرى للسور المتتابعة.
- ضُمنت تهيئة بيانات القرآن قبل احتساب نطاقات المراجعة، وقُسمت المراجعة الإلزامية في التثبيت الخماسي حسب الصفحات على أيام متوازنة.
- أضيف ترتيب يدوي بالسحب والإفلات لقائمة طلاب التسميع، وتحذير موحد قبل تسجيل تسميع لطالب مسجل غائبًا مع خيار تحضيره صراحةً.
- صُحح استدراك الغياب التلقائي عند المرور على أيام معلقة/إجازات متتالية.
- أصبح تسجيل نقاط السلوك يدعم عدة طلاب ذريًا، وأضيفت موازنة قيم المخالفات من الإعدادات وسجل إداري للإنذارات والتنبيهات والتعهدات والتواصل مع ولي الأمر.
- أضيفت نسخة تشغيلية أولى لتقييم «الحلقة النموذجية» بثمانية معايير ودرجة يومية.
- أصبح تقرير WhatsApp يذكر اسم اليوم والتاريخ والحالة اليومية مع النشاط والتلقين والتوقف والإعفاء.
- أضيف Google OAuth في Flutter والويب مع deep link للهاتف؛ التفعيل الفعلي يحتاج إعداد موفر Google وRedirect URLs في Supabase/Google.
- أضيف migration غير مدمر لـSupabase للنشاط والتلقين والإداريات والتوقف الكامل، مع مسار توافق محافظ للبيئات الأقدم؛ أكد المالك لاحقًا تنفيذ migration ونجاح فحصه.
- نجحت `50/50` بوابة إصدار، وفحوص صياغة TypeScript وAndroid/iOS، وفحص ساكن لـ184 ملف Dart. تعذر تشغيل `flutter analyze` و`flutter test` لغياب Flutter/Dart SDK في بيئة العمل.

### قاعدة البيانات

- SQLite يترقى تلقائيًا إلى `22`.
- أكد المالك تنفيذ `website/supabase/migrations/20260808000100_p1_22_activity_talaqqin_admin.sql` بنجاح في 2026-08-08.
- أكد المالك نجاح `website/supabase/verify_p1_22.sql`؛ لقطة النتيجة تثبت شرطي الإغلاق الخاصين بالنشاط والتوقف الكامل.

## [غير مُصدر] — المرحلة P1.21: تقارير الأسرة الدورية وتقوية التبادل المحلي — 2026-07-28

- رُفعت النسخة إلى `4.3.0-alpha.14+64 (P1.21)` من دون تغيير مخطط SQLite.
- أضيف اشتراك صريح لكل عائلة في تقرير أسبوعي أو شهري، مع حفظ الموافقة وإمكان الإيقاف.
- أصبحت التقارير تُنشأ كلقطات تاريخية وتُنشر داخل بوابة ولي الأمر، ويعرض الموقع سجل آخر التقارير لكل الأبناء.
- أضيف صف إرسال خارجي بخمس محاولات ووظيفة Edge محمية بسر مستقل؛ لا تغادر البيانات إلى مزود خارجي إلا عند اختيار القناة وإعداد عنوان HTTPS ومفتاح HMAC.
- قُوّي التبادل دون إنترنت بكود عشوائي من 12 رمزًا، وصلاحية 30 دقيقة، وفصل الكود عن الملف، واستبعاد إعدادات الجهاز وقوالب الرسائل وسجل التدقيق.
- رُقّيت حزم الويب الأمنية إلى Next.js `16.2.12` وPostCSS `8.5.23`.
- أضيف migration غير مدمر وفحص عقد P1.21، وارتفع مجموع بوابات الإصدار إلى 49.

## [غير مُصدر] — المرحلة P1.20: تشغيل شامل وتقارير وتبادل محلي — 2026-07-27

- رُفعت النسخة إلى `4.3.0-alpha.13+63 (P1.20)` ومخطط SQLite إلى 21.
- تحولت المهام اليومية إلى شبكة متكيفة، وأصبح Readex Pro خط الواجهة المضمن للهاتف والويب.
- أضيفت لوحة أداء للحلقة وأُصلحت مسارات التقرير اليومي والأسبوعي والشهري والتقرير الشامل وسند الاستلام.
- أصبح إجمالي محفوظ الطالب يُعاد اشتقاقه بدقة من النطاق الابتدائي وسجلات التسميع عند الحفظ أو تعديل الملف.
- أضيفت أنظمة المراجعة المتباعدة والتثبيت الخماسي وسبق/سبقي/منزل والنظام المخصص.
- أضيفت وحدة مسابقات محلية كاملة مرتبطة بمولد الأسئلة ونظام التحكيم والترتيب.
- أضيف تبادل مشفر بين أجهزة Android عبر مشاركة النظام المحلية وكود ربط، مع دمج السجلات الأحدث.
- توسعت بوابة الأسرة بالسلوك والتحديث الدوري، وتوسعت الجهة الإشرافية بجدولة الزيارات وتوثيقها.
- أضيفت نقطة توسعة غير مفعلة لأكواد التفعيل؛ جميع الميزات تبقى مفتوحة.
- فُعل تصغير كود وموارد Android وأضيف بناء APK منفصل حسب المعمارية.

## [غير مُصدر] — الإصلاح P1.19.1: استعادة بناء Flutter — 2026-07-27

- رُفعت هوية نسخة التطوير إلى `4.3.0-alpha.12+62 (P1.19.1)`.
- استُبدل `Center` بـ`Align` في غلاف المحتوى المشترك لأن `Center` لا يقبل المعامل `alignment`.
- أصبحت دالة صف تفاصيل نتيجة الامتحان تستقبل `BuildContext` صراحةً لاستخدام ألوان الثيم.
- أضيف فحص عقد يمنع رجوع الخطأين اللذين أوقفا `kernel_snapshot`.
- لا يوجد أي تغيير في SQLite أو Supabase.

## [غير مُصدر] — المرحلة P1.19: إعادة بناء الهوية وتجربة الاستخدام — 2026-07-27

- رُفعت هوية نسخة التطوير إلى `4.3.0-alpha.11+61 (P1.19)` دون تغيير SQLite أو Supabase.
- بُني التحديث بعد بحث واجهات ترتيل وQuranly وآية ومنظومة Quran.com والمحفظ، مع توثيق ما استُلهم وما استُبعد.
- أُنشئ نظام تصميم مشترك للهاتف والويب: ألوان فاتحة وداكنة عالية التباين، ومسافات متكيفة، وزوايا أصغر، وظلال أخف، وحالات دلالية موحدة.
- تحولت الرئيسية إلى مساحة عمل يومية تبدأ بالحضور والتسميع وإغلاق اليوم، وأزيلت منها الترويسات والبطاقات التسويقية الكبيرة.
- أُعيد تصميم الحفظ المباشر كتسلسل واضح للطالب والنطاق والتقييم والاعتماد، مع ملخص حفظ ومراجعة أكثر إحكامًا.
- صُنّف تنقل الويب إلى عمل اليوم، والطلاب والأسر، والتعلم والمتابعة، والإدارة، وطُبقت الهوية من غلاف واحد على جميع المسارات.
- أزيلت درجات Tailwind غير الموجودة، واستُبدلت الرماديات الثابتة في Flutter بألوان الثيم لمنع اختفاء النصوص في الوضع الداكن.
- نُفذت طلبات البيانات المستقلة في الرئيسية والحفظ بالتوازي، وعُزل اشتراك React في الرئيسية والحفظ والغلاف إلى أجزاء الحالة المطلوبة فقط.
- أضيف فحص P1.19 يمنع رجوع الرماديات غير المتكيفة ودرجات الألوان غير الصالحة والقالب القديم.

## [غير مُصدر] — المرحلة P1.18: تكامل الخطة ومسارات التسميع — 2026-07-27

- رُفعت هوية نسخة التطوير إلى `4.3.0-alpha.10+60 (P1.18)` دون تغيير SQLite أو Supabase.
- أصبحت بطاقة الخطة تشرح أن الإنجاز محسوب من سجلات التسميع المنفذة والمدخلة داخل المدة، وتعرض نسبة مستقلة للحفظ والمراجعة والسرد.
- استُبدل رمز التأشير النصي في PDF بمربع مرسوم ثابت، مع بقاء جداول الخطة باتجاه من اليمين إلى اليسار.
- أصبح إتمام السورة ينشئ إشعارًا دائمًا غير مكرر يوضح إدراجها تلقائيًا ضمن المراجعة المستحقة.
- أضيف قفل موحد يمنع فتح الحفظ المباشر وجلسة التسميع للطالب نفسه بالتزامن، ويتوقف ماسح QR فور قبول الرمز.
- ضُغطت الرئيسية ومساحة الحفظ والمراجعة، وقُلّل ازدحام بطاقة الطالب مع ألوان نصوص متوافقة مع الوضع الداكن.
- نجحت 45 بوابة إصدار، وESLint وTypeScript، وبناء 31 مسارًا للويب.

## [غير مُصدر] — الإصلاح P1.17.2: مركز إعدادات منظم — 2026-07-22

- رُفعت هوية نسخة التطوير إلى `4.3.0-alpha.9+59 (P1.17.2)` دون تغيير SQLite أو Supabase.
- قُسمت وجهة الإعدادات إلى «إعدادات الحلقة اليومية» و«الإدارة والحماية»، وأضيف بحث يوجه المستخدم إلى القسم المناسب.
- قُسمت البيانات داخليًا إلى: النسخ والحماية، والمزامنة والاتصال، والخصوصية والسجل؛ فلا تظهر العمليات الحساسة كلها دفعة واحدة.
- تحول التشخيص إلى مركز «إدارة النظام والدعم» بمهام واضحة: جاهزية التشغيل، والتشخيص الفني، وسجل التدقيق الإداري.
- بقيت وظائف النسخ والتشفير والرفع والتنزيل والخصوصية نفسها دون تغيير في البيانات أو السلوك.
- أضيف فحص عقد مستقل لواجهة الإعدادات، وأصبح مجموع فحوص الإصدار 44.

### قاعدة البيانات

- لا يوجد SQL أو migration جديد لهذه الدفعة.

## [غير مُصدر] — الإصلاح P1.17.1: تسجيل الحفظ المباشر عبر QR — 2026-07-22

- رُفعت هوية نسخة التطوير إلى `4.3.0-alpha.8+58 (P1.17.1)` دون تغيير SQLite أو Supabase.
- أصبح مركز QR في Android يعرض مسارين مستقلين: «جلسة تسميع» و«حفظ مباشر»؛ يفتح الثاني نموذج النطاق والتقييم مباشرة دون تشغيل الجلسة أو المؤقت.
- يطبق المساران قيود اليوم الحالي والتوقيف، ويمنعان الحفظ الجديد للخاتم مع إبقاء المراجعة متاحة.
- أصبح ماسح الويب يعرض بعد التعرف على الطالب: حفظًا مباشرًا، أو جلسة تسميع، أو الاكتفاء بالحضور ومسح الطالب التالي.
- ينتقل الطالب من QR إلى نموذج الويب مملوءًا تلقائيًا، ويبدأ التسجيل المباشر من موضعه التالي وبمقداره المقرر مع إمكان تعديل النطاق قبل الاعتماد.
- نجح TypeScript وESLint و43 فحص عقد وبناء 31 مسارًا.

### قاعدة البيانات

- لا يوجد SQL أو migration جديد لهذه الدفعة.

## [غير مُصدر] — المرحلة P1.17: مسار الخاتمين ومساحة العمل اليومية — 2026-07-22

- رُفعت هوية نسخة التطوير إلى `4.3.0-alpha.7+57 (P1.17)` دون تغيير مخطط SQLite أو Supabase.
- وُحّد تعريف خاتم القرآن بين حالة `graduated` وإجمالي 6236 آية، وأُغلق عنه الحفظ الجديد من كل مداخل Android المباشرة.
- أصبح الخاتمون يظهرون في تبويب المراجعة ولا يظهرون في تبويب الحفظ الجديد، وتدعم الخطط الطلاب النشطين والمتخرجين.
- حُذف مقرر الحفظ وعموده من شاشة وخطة PDF الخاصة بالخاتم، وأصبح إنجاز خطته يحسب المراجعة والسرد فقط دون نسبة ابتدائية وهمية.
- أُعيد تصميم تسجيل المراجعة ليعطي السور والآيات معظم الشاشة، مع بحث وإعدادات منفصلة وشريط اعتماد ثابت وآمن.
- ضُغطت إحصاءات الرئيسية والتاريخ والترويسة لتظهر المهام اليومية دون تمرير طويل.
- أضيف إغلاق تلقائي ذري للأيام المنتهية عند منتصف الليل أو أول تشغيل تالٍ، مع سجل لنوع الإغلاق واستثناء العطلات والإجازات ومراعاة تاريخ الانضمام.
- أضيف تكافؤ لخطة الخاتم في الويب وبوابة عقد P1.17، ونجح TypeScript وESLint و42 فحصًا وبناء 31 مسارًا.

### قاعدة البيانات

- لا يوجد SQL جديد لهذه المرحلة.

## [غير مُصدر] — الإصلاح P1.16.2: استعادة بيئة الويب على Windows — 2026-07-22

- رُفعت هوية نسخة التطوير إلى `4.3.0-alpha.6+56 (P1.16.2)` دون أي تغيير جديد في SQLite أو Supabase.
- أكدت نتيجة Supabase المرفقة نجاح فحوص `plan_recitation_table` و`plan_recitation_columns` و`plan_recitation_rls` و`plan_recitation_scope_trigger` كلها.
- أضيف `SETUP_WEB_WINDOWS.cmd` في جذر المشروع لتثبيت وفحص بيئة React وNext.js وTypeScript بالنقر المزدوج.
- أصبحت أداة PowerShell تستخدم `npm.cmd` على Windows، وتحذف ذاكرة TypeScript القديمة، وتتحقق صراحةً من React وNext وLucide وتعريفات React وخادم TypeScript قبل الفحص والبناء.
- أضيف إعداد TypeScript مستقل داخل `website/.vscode` حتى تعمل الأنواع سواء فتح المستخدم مجلد `Halaqah` كاملًا أو مجلد `website` وحده.
- صُرّحت أنواع عناصر التنقل و`ReactNode` وحدث النقر في `DashboardLayout.tsx`، فتوقفت رسائل `React namespace` و`implicit any` الثانوية عن حجب السبب الأصلي.
- نجح ESLint بلا تحذيرات، وفحص TypeScript، وبناء Next.js الإنتاجي لـ31 مسارًا بعد الإصلاح.

### قاعدة البيانات

- لا يوجد SQL جديد. نتيجة الفحص المرفقة تؤكد جاهزية migration P1.16 (`4/4 true`).

## [غير مُصدر] — الإصلاح P1.16.1: الخطط القرآنية الدقيقة وصمود التشغيل — 2026-07-22

- رُفعت هوية نسخة التطوير إلى `4.3.0-alpha.5+55 (P1.16.1)` دون تغيير مخطط SQLite أو Supabase.
- استُبدلت قيمة كائن الشهر الهجري في قوائم التقارير بمفتاح ثابت وفريد، مع إزالة الأشهر المكررة من المصدر، فانتهى خطأ `DropdownButtonFormField` في التصدير الجماعي.
- أصبحت طباعة الخطة تشترط صفًا لكل تاريخ في المدة ونطاقًا صريحًا بالسورة والآية للحفظ والمراجعة والتلاوة؛ أُلغي نهائيًا الرجوع إلى عبارات عامة مثل «صفحتان».
- تظهر أيام الجمعة وتعليق الدراسة وإجازة الطالب والتوقيف داخل الجدول بسبب واضح بدل إسقاطها من الخطة.
- يبدأ السرد في الخطة التالية من آخر جلسة سرد للطالب عبر جميع خططه، لا من أول المصحف.
- يسبق توليد الجدول حفظ الخطة الجماعية؛ الطالب الذي محفوظُه إجمالي بلا سورة وآية يُتجاوز مع إظهار اسمه وسبب النقص، ولا تُنشأ له خطة مضللة.
- عُزلت خدمات السحابة والنسخ الخلفي وتهيئة النظام عن مسار التشغيل الحرج، وأصبحت أخطاء الواجهة محلية ومسجلة بدل تحويل التطبيق كله إلى شاشة تعذر التشغيل.
- أضيف اختبار تفرد الأشهر الهجرية وعُزز فحص P1.16 لمنع عودة مسار المقادير العامة.

### قاعدة البيانات

- لا يوجد SQL جديد لهذا الإصلاح؛ لا تعِد تنفيذ migration P1.16 إذا كان فحصه السابق ناجحًا.

## [غير مُصدر] — المرحلة P1.16: السرد الرقمي وإصلاح إعداد الويب — 2026-07-22

- رُفعت هوية نسخة التطوير إلى `4.3.0-alpha.4+54 (P1.16)` وقاعدة SQLite إلى الإصدار 20.
- أضيفت جلسة مستقلة للسرد/التلاوة مرتبطة بالطالب والخطة والتاريخ؛ لا تُسجّل حفظًا جديدًا ولا ترفع إجمالي المحفوظ.
- يدعم السرد نطاقًا متصلًا يعبر السور، ويُقسَّم إلى مقاطع مرتبة تشترك في معرّف جلسة واحد حتى يمكن حذف الجلسة كاملة بأمان.
- أصبح إنجاز الخطة يحسب ثلاثة محاور: الحفظ والمراجعة والسرد، ولا يصف الخطة بأنها منجزة حسابيًا حتى تُستوفى المحاور الثلاثة.
- أضيفت شاشة خفيفة لاختيار نقطة بداية السرد وتكوين المقرر تلقائيًا بحسب الآيات أو الأسطر أو الصفحات أو الأحزاب، مع التاريخ والجودة والملاحظات والسجل والحذف.
- أصبحت طباعة الخطة الفردية والجماعية تولد لكل يوم نطاقًا قرآنيًا فعليًا للحفظ والمراجعة والسرد، يبدأ من آخر موضع موثق ويتجاوز العطلات وتعليق الدراسة والإجازات والتوقيف.
- أضيف رفع وتنزيل سجلات السرد إلى Supabase مع حماية الحلقة، ومنع خلط الطالب أو الخطة أو التاريخ، وترحيل وفحص جاهزية من أربعة أسطر.
- أصبحت النسخ الاحتياطية المحلية تتضمن جلسات السرد، مع قبول النسخ الأقدم التي لا تحتوي الجدول الجديد.
- أضيف `tools/setup_web.ps1` لمعالجة أخطاء VS Code من نوع `Cannot find module 'react'` بعد فك المصدر، وإعداد TypeScript الخاص بمجلد الويب.
- نجح TypeScript وESLint والبناء الإنتاجي لـ31 مسارًا، وأضيف فحص عقد P1.16 ليرتفع مجموع الفحوص إلى 41.

### قاعدة البيانات

- نفّذ **محتوى** `website/supabase/migrations/20260722000400_p1_16_plan_recitation_tracking.sql`.
- بعده نفّذ **محتوى** `website/supabase/verification/20260722000400_p1_16_plan_recitation_tracking_readiness.sql`؛ يجب أن تكون القيم الأربع `passed=true`.

## [غير مُصدر] — المرحلة P1.15: الخطط الذكية والتقرير الإداري — 2026-07-22

- رُفعت هوية نسخة التطوير إلى `4.3.0-alpha.3+53 (P1.15)` وقاعدة SQLite إلى الإصدار 19.
- أصبح إنجاز الخطة محسوبًا من الحفظ والمراجعة المسجلين فعليًا مقارنةً بالمقرر وأيام الدراسة الحقيقية، مع استبعاد تعليق الدراسة والعطلات والإجازات والتوقيف.
- أضيف مقرر مستقل للسرد/التلاوة إلى إنشاء الخطة وتعديلها والطباعة الفردية والجماعية A4 والكاشير، وإلى مزامنة Android والويب وSupabase.
- تبدأ الفترة التالية من آخر موضع تسميع فعلي، حتى إذا أُغلقت الفترة السابقة قبل إكمال مقدارها؛ ويبقى اختبار التجاوز بوابة إلزامية قبل الخطة الجديدة.
- أضيف اقتراح مراجعة افتراضي بحسب حجم المحفوظ، مع حفظ المقرر النهائي في ملف الطالب وإمكانية تعديله.
- أصبح تقرير الفترة يفصل الغياب والاستئذان والتأخر والمخالفات، ويعرض إجمالي دقائق التأخر والتقييم اليومي للحفظ والمراجعة بألوان مستقلة.
- أضيف ترتيب الأول والثاني والثالث بحسب نسبة الأداء ثم الحضور والحفظ، ويظهر في الشاشة وPDF وقالب WhatsApp.
- أضيف مركز إجراءات سريع بعد مسح QR للتحضير والغياب والاستئذان وتسجيل الحفظ والمراجعة والإجازة.
- أصبحت نقاط التسميع متدرجة بحسب نسبة الإنجاز وحجم المقرر، مع تجميع تسجيلات اليوم كلها قبل احتساب الزيادة.
- أصبح سداد الغرامة في الصندوق قادرًا على تسوية جزء من الرصيد السلبي أو كله مع إبقاء المخالفة الأصلية محفوظة في السجل.
- حُسنت شاشة الإشعارات ببحث ومرشحات للحضور والأداء والخطط والمقروء، مع حالات فارغة أوضح.
- أضيف migration غير مدمر وفحص جاهزية P1.15، وفحص عقد جديد ليرتفع مجموع فحوص المصدر إلى 40.

### قاعدة البيانات

- نفّذ **محتوى** `website/supabase/migrations/20260722000300_p1_15_urgent_plans_reports.sql` داخل Supabase SQL Editor.
- بعده نفّذ **محتوى** `website/supabase/verification/20260722000300_p1_15_urgent_plans_reports_readiness.sql`؛ يجب أن تكون كل قيم `passed=true`.

## [غير مُصدر] — المرحلة P1.14: الإغلاق السحابي ودوام الحلقة — 2026-07-22

- رُفعت هوية نسخة التطوير إلى `4.3.0-alpha.2+52 (P1.14)`.
- أصبح إغلاق اليوم متاحًا من الويب عبر RPC ذري في Supabase؛ الحضور والإجازات والنقاط والإيصال تنجح كلها أو تتراجع كلها.
- أضيف قفل معاملات وإيصال ثابت يجعل إعادة الطلب بعد انقطاع الشبكة آمنة ولا يكرر الغياب أو العقوبات.
- أصبحت معاينة اليوم تقرأ جلسات `homework_grades` الحديثة وسجل `memorization` المتوافق، بدل إظهار الحاضرين الذين سمّعوا على أنهم «لم يسمّع».
- أضيفت مزامنة سحابية لتعليق الدراسة مع سبب موثق يظهر في شاشة الحضور ويُستبعد من التقارير والإغلاق.
- أضيف وقت نهاية الدوام والمنطقة الزمنية والإجازات الأسبوعية إلى إعدادات المركز؛ الجمعة إجازة افتراضية.
- أصبحت عقوبة الغياب موزونة تلقائيًا لتبقى أشد من اجتماع التأخر وعدم إتمام المقرر وعدم لبس الثوب.
- يراعي الإغلاق الإجازات المعتمدة وإيقاف التسميع؛ الموقوف يعفى من عدم التسميع فقط ولا يعفى من الحضور.
- أضيف SQL تحقق للقراءة فقط وفحص عقد جديد، ليرتفع مجموع بوابات المصدر إلى 39.

### قاعدة البيانات

- يلزم تنفيذ **محتوى** `website/supabase/migrations/20260722000200_p1_14_atomic_web_daily_closing.sql` في SQL Editor.
- بعده يُنفذ **محتوى** `website/supabase/verification/20260722000200_p1_14_atomic_daily_closing_readiness.sql`؛ يجب أن تكون كل قيم `passed=true`.

## [غير مُصدر] — المرحلة P1.13: إغلاق اليوم والتشغيل الآمن — 2026-07-22

- رُفعت هوية نسخة التطوير إلى `4.3.0-alpha.1+51 (P1.13)`.
- أضيف «مركز إغلاق اليوم» لمراجعة الحضور الناقص والغياب وعدم التسميع والاستثناءات قبل الاعتماد.
- أصبح الطالب الذي لم يُنشأ له سجل حضور ظاهرًا بوضوح، ويُحوّل إلى غياب فقط بعد انتهاء الدوام وموافقة المعلم.
- تُكتب سجلات الحضور والنقاط وعلامة الإغلاق داخل معاملة SQLite واحدة؛ الفشل لا يترك نتائج جزئية ولا يمكن تكرار الإغلاق.
- تُستثنى أيام تعليق الدراسة والعطلات الأسبوعية والإجازات المعتمدة، ويُعفى الموقوف من عقوبة عدم التسميع دون إعفائه من الحضور.
- أضيفت لوحة ويب لتدقيق عمليات اليوم والانتقال لاستكمال الحضور أو التسميع، مع ترتيب الطلاب أبجديًا.
- اكتمل إرسال متابعة الانضباط إلى ولي الأمر عبر واتساب بدل زر غير منفذ.
- أصبح دخول الويب مغلقًا بأمان عند نقص إعداد Supabase، وأزيل المستخدم التجريبي الوهمي والاعتماد على زخارف خارجية.
- أصبحت إحصاءات الرئيسية وشبكة المهام تتكيف مع عرض الهاتف وحجم خط النظام لتقليل overflow.
- صُحح مفتاح «اليوم» في لوحة الويب ليستخدم تاريخ الجهاز المحلي بدل UTC، وفُعّلت أزرار الإشعارات والتحفيز والإجراء الفوري التي كانت بلا انتقال.
- ثُبتت إصدارات `sharp` و`postcss` الآمنة عبر overrides، وأصبح `npm audit` يعرض صفر ثغرات معروفة.
- أضيف اختبار تصنيف يومي وفحص عقد P1.13، ليرتفع مجموع بوابات المصدر إلى 38.
- لا يوجد SQL جديد في هذه المرحلة؛ نتيجة P1.12 المرفقة تؤكد حماية `6/6` من جداول البوابات الحساسة.

## [غير مُصدر] — المرحلة P1.12: جاهزية التشغيل والقبول الفعلي — 2026-07-22

- رُفعت هوية نسخة التطوير إلى `4.2.0-alpha.8+50 (P1.12)`.
- أضيفت لوحة «جاهزية التشغيل والإطلاق» داخل الإعدادات تجمع مخطط SQLite وسلامة البيانات والنسخ واتصال Supabase والجلسة والحوادث.
- أصبحت النتيجة تميز بوضوح بين الناجح والملاحظة والعائق واختبار القبول المتبقي.
- أضيفت قائمة قبول فعلية للجهازين والاستعادة المشفرة وعزل البوابات والطباعة وQR، وتُحفظ لكل إصدار على حدة حتى لا تُورث موافقات نسخة قديمة.
- يمكن نسخ تقرير الجاهزية أو مشاركته دون أسماء الطلاب أو هواتفهم أو معرفاتهم أو ملاحظاتهم أو رموز الجلسات.
- أضيف استعلام قراءة فقط مخصص لجداول اعتماد وجلسات البوابات؛ يعد `RLS=true` و`policies=0` وسحب الوصول المباشر نجاحًا أمنيًا.
- أضيف فحص عقد P1.12، ليرتفع مجموع بوابات المصدر إلى 37.
- لا يوجد SQL تعديلي في هذه المرحلة.

## [غير مُصدر] — المرحلة P1.11: سلامة البيانات وقبول النسخة — 2026-07-21

- رُفعت هوية نسخة التطوير إلى `4.2.0-alpha.7+49 (P1.11)`.
- أضيف فحص محلي للقراءة فقط داخل مركز التشخيص للعلاقات المفقودة والحضور المكرر وهوية الطالب والمقررات والإجازات والنطاقات القرآنية.
- لا يعرض تقرير سلامة البيانات أسماء الطلاب أو هواتفهم أو معرفاتهم أو ملاحظاتهم؛ بل نوع المشكلة وعدد الحالات فقط.
- أضيفت اختبارات لمحرك تقييم السلامة وربطه بتقرير الدعم المنقح.
- أصبح فحص Windows يقبل بناء `debug` أو `release`، ويتحقق من توقيع APK عند توفر `apksigner`، ويحفظ تقرير قبول وبصمة SHA-256.
- يمكن تمرير CSV الناتج من فحص Supabase للقراءة فقط، ويتوقف القبول إذا كانت أي نتيجة `passed=false`.
- أضيف فحص عقد P1.11، ليرتفع مجموع بوابات المصدر إلى 36.
- لا يوجد SQL جديد في هذه المرحلة؛ يُستخدم فحص الجاهزية الحالي للقراءة فقط.

## [غير مُصدر] — المرحلة P1.10: تثبيت الويب والنسخة المرشحة — 2026-07-20

- رُفعت هوية نسخة التطوير إلى `4.2.0-alpha.6+48 (P1.10)`.
- أُغلقت ملاحظات ESLint الثلاث والعشرون، وأصبح فحص الويب الصارم يرفض أي تحذير جديد.
- ثُبتت مراجع React في الحضور والتقارير والحفظ والمراكز والمعلمين والتنقل لمنع تكرار التأثيرات بطريق الخطأ.
- أصبحت تهيئة لوحة التحكم آمنة مع التصيير الخادمي دون `setState` متزامن بعد التحميل.
- أصبحت تهيئة القرعة، ونموذج التسميع، وقوالب الرسائل، والعائلات، وخريطة المصحف قابلة للإلغاء عند مغادرة الشاشة.
- وُثق أن جداول اعتماد وجلسات بوابة الطالب والعائلة تستخدم `deny-all` مقصودًا: RLS مفعّل، صفر سياسات، ولا وصول مباشر لـ`anon` أو `authenticated`.
- أضيف فحص عقد P1.10، ليرتفع مجموع بوابات المصدر إلى 35.
- لا يوجد SQL جديد في هذه المرحلة.

## [غير مُصدر] — المرحلة P1.9: مرونة الشاشات وتكبير الخط — 2026-07-20

- رُفعت هوية نسخة التطوير إلى `4.2.0-alpha.5+47 (P1.9)`.
- أضيفت مكونات مشتركة لعناوين النوافذ وصفوف المعلومات ومجموعات الأزرار تتكيف مع عرض الشاشة ومقياس خط النظام.
- أصبحت أزرار التسميع السابق/التالي/التوقف والطباعة A4/A5 تتحول إلى ترتيب عمودي عند ضيق المساحة.
- عولجت صفوف تقدم التسميع وملخص المراجعة وسجل حضور الطالب وبيانات الإشعارات لتلتف دون شريط أصفر وأسود.
- اختُزلت إجراءات سجل المخالفات وقواعد النقاط في قوائم منبثقة بدل حجز مساحة كبيرة داخل `ListTile.trailing`.
- أصبحت عناوين تصدير التقارير وWhatsApp وختم المصحف مرنة مع العربية والخط الكبير.
- أضيفت ثلاثة اختبارات Widget بمقياس خط 200% وفحص عقد جديد، ليرتفع مجموع بوابات المصدر إلى 34.
- لا يوجد SQL جديد في هذه المرحلة.

## [غير مُصدر] — المرحلة P1.8: النسخ الاحتياطي الخلفي وإصلاح التشخيص — 2026-07-19

- رُفعت هوية نسخة التطوير إلى `4.2.0-alpha.4+46 (P1.8)`.
- أضيفت جدولة Android WorkManager لإنشاء النسخة المحلية المشفرة يوميًا بعد الساعة المحددة حتى عند إغلاق التطبيق.
- تتزامن الجدولة عند تشغيل التطبيق وعند تفعيل النسخ أو تغيير وقته، وتُلغى تلقائيًا عند تعطيل الميزة.
- بقي فحص أول فتح بعد الموعد مسارًا احتياطيًا لمنع تفويت النسخ عند تأخير النظام للمهمة الخلفية.
- أضيفت حالة آخر تشغيل خلفي وخطأ الجدولة إلى الإعدادات ومركز التشخيص وتقرير الدعم المنقح.
- وُحد هدف Java/Kotlin على الإصدار 17 المتوافق مع WorkManager وبيئة البناء الحالية.
- رُفع حد بناء iOS إلى 14.0 لتوافق تبعية WorkManager، مع بقاء الجدولة مفعلة لأندرويد فقط في هذه المرحلة.
- صُححت قراءة إصدار SQLite باستبدال `Database.getVersion()` غير المتاح بـ `PRAGMA user_version`.
- أضيف اختبار لوقت الجدولة وفحص عقد جديد، فأصبح مجموع بوابات المصدر 33.
- لا يوجد SQL جديد في هذه المرحلة.

## [غير مُصدر] — المرحلة P1.7: تكافؤ التسميع وملف الطالب وتنظيم الإجازات في الويب — 2026-07-19

- رُفعت هوية نسخة التطوير إلى `4.2.0-alpha.3+45 (P1.7)`.
- أضيف للويب تسميع مفتوح حقيقي يعبر السور، مع السابق/التالي، تقييم اختياري لكل آية، إظهار النص أو إخفائه، وزر مستقل «التوقف هنا».
- يدعم نطاق الويب المقرر اليومي بالآيات والأسطر والصفحات والأحزاب، مع حدود نهاية الصفحة ونهاية الحزب.
- أضيف RPC ذري يحفظ كل مقاطع السور في معاملة Supabase واحدة، ولا يكرر الأخطاء أو الملاحظة في كل مقطع.
- أضيف توافق رجعي: الجلسة ذات السورة الواحدة تستعمل دالة الحفظ السابقة إذا لم يُنفذ SQL الجديد، أما الجلسة متعددة السور فتتوقف بأمان حتى تنفيذ migration.
- أضيفت صفحة ويب تفصيلية للطالب تعرض QR والكود والمحفوظ المثبت وآخر حفظ ومراجعة والحضور والغياب والنقاط والخطة، مع تعديل مقرر الحفظ والمراجعة.
- أضيف انتقال مباشر من بطاقة الطالب إلى الملف التفصيلي، ومن الملف إلى جلسة تسميع الطالب.
- نُظمت إجازات الويب بمرشحات الأسبوع الحالي والشهر والفترة المخصصة والحالة والطالب والسبب، مع ترتيب الطلاب أبجديًا بالعربية.
- أضيف فحصان جديدان، ليرتفع عدد فحوص الجودة الآلية إلى 32، ونجح بناء Next.js بجميع المسارات الثلاثين.
- يتطلب التسميع متعدد السور تنفيذ محتوى `20260719000100_p1_web_connected_recitation.sql` في Supabase.

## [غير مُصدر] — المرحلة P1.6: الحفظ والتسميع المتصل — 2026-07-19

- رُفعت هوية نسخة التطوير إلى `4.2.0-alpha.2+44 (P1.6)`.
- أصبح التسميع المفتوح يستمر إلى السورة التالية حسب اتجاه حفظ الطالب، بدل التوقف القسري عند نهاية السورة الحالية.
- فُصل زر «التالي» عن زر «التوقف هنا» حتى يعتمد المعلم آية النهاية بدقة دون سباق الانتقال التلقائي.
- يعرض التطبيق مقرر الطالب بالآيات أو الأسطر أو الصفحات أو الأحزاب، ويمكن تحويله إلى نطاق جلسة متصل بضغطة واحدة.
- أصبحت «نهاية الصفحة» و«نهاية الحزب» في شاشة التسميع تستخدمان الحدود الفعلية عبر السور.
- تُقسّم الجلسة المتصلة إلى سجل مستقل لكل سورة داخل معاملة SQLite واحدة، مع سجل يومي وإجمالي محفوظ متسقين.
- لا يسجل تقييم «غائب» آيات محفوظة ولا يرفع نقاط التسميع، مع بقاء سجل التقييم.
- عُمّم النطاق المتصل على شاشة التسجيل اليدوي للحفظ، وأضيف HomeworkGrade متوافق لكل مقطع.
- حُسنت مرونة صفوف المؤقت والتقييم وأزرار الأسفل، وأضيف Safe Area لشريط إجراءات التسميع.
- أضيف فحص P1.6، ليرتفع عدد فحوص الجودة الآلية إلى 30.
- لا يوجد SQL جديد لهذه المرحلة؛ فهي تستخدم جداول التسميع والمراجعة الحالية.

## [غير مُصدر] — المرحلة P1.5: المراجعة المتصلة وخطط الأحزاب — 2026-07-18

- رُفعت هوية نسخة التطوير إلى `4.2.0-alpha.1+43 (P1.5)` حتى لا تختلط بحزمة RC2 السابقة.
- أضيف محرك واحد لنطاقات الآيات والأسطر والصفحات والأحزاب يستطيع الاستمرار بين السور.
- يقيّد محرك المراجعة النطاق بالمحفوظ المسجل ويتوقف عند أول فجوة بدل إدخال مراجعة غير صحيحة.
- أضيف اختيار وحدة جلسة المراجعة ومقدارها، مع تطبيق سريع على آخر موضع ومراعاة ترتيب الدورة.
- أصبح زرا نهاية الصفحة ونهاية الحزب في المراجعة يحددان السور المتتابعة تلقائيًا.
- أضيفت وحدة «أحزاب» إلى ملف الطالب والخطط والنقاط والتكريم والويب وبوابة المتابعة.
- أضيف migration غير مدمر يوسع قيود Supabase ودالة التكريم لقبول `hizbs`.
- أضيف فحص P1.5، ليرتفع عدد فحوص الجودة الآلية إلى 29.

## [غير مُصدر] — المرحلة P6.5: سلامة الحزم وقبول RC2 — 2026-07-18

- أضيف فاحص PowerShell يقارن ZIP أو APK بملف SHA-256 ويمنع استخدام الملف عند اختلاف البصمة.
- يتحقق الفاحص أن اسم الملف المسجل في `.sha256` يطابق الحزمة المحددة، لتجنب مقارنة بصمة إصدار آخر.
- أصبح فحص RC2 يحفظ APK وملف بصمته معًا داخل `build/release-artifacts` باسم يتضمن الإصدار.
- أضيف دليل عربي يوضح أن ملف البصمة لا يستبدل أي ملف في المشروع، وإنما يحفظ مع أرشيف الإصدار.
- أضيف فحص عقد P6.5، ليرتفع مجموع بوابات المصدر إلى 28.
- لا يوجد SQL جديد لهذه المرحلة.

## [غير مُصدر] — المرحلة P6.4: التشخيص الآمن والجاهزية التشغيلية — 2026-07-18

- أضيف مركز تشخيص داخل الإعدادات يعرض الإصدار وSQLite والنظام وأعداد السجلات وحالة النسخ وآخر رفع وتنزيل واتصال Supabase.
- أصبح تقرير الدعم قابلًا للنسخ والمشاركة، ومصممًا دون أسماء الطلاب أو الهواتف أو الملاحظات أو كلمات المرور أو رموز الجلسات.
- أضيف التقاط عالمي لأخطاء Flutter والمنصة كبصمة SHA-256 من 16 خانة، دون تخزين الرسالة أو stack trace أو مسار الجهاز.
- يمنع النظام تكرار البصمة نفسها مدة خمس دقائق حتى لا يتضخم سجل التدقيق.
- أضيفت شاشة تعافٍ عند فشل بدء التطبيق، تعرض رمز حادثة وتحذر من حذف البيانات أو إعادة التثبيت قبل النسخ.
- وُحدت هوية الإصدار في `AppBuildInfo` ورُفعت إلى `4.1.0-alpha.2+42 (RC2)`.
- أضيف اختبارا الخصوصية والبصمة وفحص عقد P6.4، ليرتفع مجموع بوابات المصدر إلى 27.
- لا يوجد SQL جديد لهذه المرحلة.

## [غير مُصدر] — المرحلة P6.3: الإطلاق المرحلي — 2026-07-18

### تشخيص السحابة
- أضيف فحص Supabase من داخل الإعدادات يختبر DNS وTLS واستجابة الخادم دون مصادقة أو نقل بيانات.
- أصبحت رسائل الفشل تميز بين فقد DNS وانتهاء المهلة وخطأ الشهادة والشبكة وتعطل الخادم، مع إرشادات عربية.
- يمكن بناء Android لمشروع Supabase مرحلي أو إنتاجي عبر `SUPABASE_URL` و`SUPABASE_PUBLISHABLE_KEY` من `dart-define`.

### APK والتوقيع
- فُصل سير GitHub Actions إلى اختيار `staging` للاختبار الداخلي و`production` للإصدار الموقع.
- يتوقف الإنتاج إذا غاب keystore أو هوية التطبيق الدائمة، أو بقيت الهوية ضمن `com.example`.
- يفحص السير توقيع APK بـ`apksigner`، وينتج SHA-256، ويحتفظ برموز فك تتبع الإصدار المموه.
- أضيف بناء APK debug إلى بوابة CI المعتادة، وارتفع عدد فحوص العقد إلى 26.
- حُدّث إصدار Flutter إلى `4.1.0-alpha.1+41` واسم التطبيق الظاهر إلى «حلقتي».
- أضيف فحص Supabase للقراءة فقط يعرض وجود الامتداد والجداول والدوال وRLS والسياسات، ونموذج موحد لتوثيق نتائج قبول الجهازين والطباعة والمزامنة.
- صُحح فحص الجاهزية حتى لا يعتبر غياب السياسات عن جداول PIN والجلسات فشلًا؛ الوضع الآمن المقصود لها هو RLS مفعّل وصفر سياسات مع سحب كل صلاحيات anon وauthenticated.
- أضيف مسار RC1 سريع على Windows يشغل تحليل Flutter واختباراته وبناء APK وفحوص الويب ويطبع SHA-256، مع دليل تشغيل مختصر يتوقف عند أول فشل.
- لا يوجد SQL جديد لهذه المرحلة.

## [غير مُصدر] — إصلاح تشغيل Android — 2026-07-18

- أصبح عنوان الإشعار واسم الطالب يتمددان ضمن المساحة المتاحة مع اختصار آمن، مما يمنع `RenderFlex overflow` في الهواتف الضيقة وعند تكبير الخط.
- أضيف إذن `INTERNET` إلى Android manifest الرئيسي، حتى تعمل مصادقة Supabase والمزامنة في نسخ الإصدار كما تعملان في `debug`.
- أضيف فحص عقد يمنع رجوع صفوف الإشعارات غير المرنة أو فقدان إذن الشبكة.

## [غير مُصدر] — المرحلة P7.2.1: حساب ولي الأمر متعدد الأبناء — 2026-07-14

### إصلاح توافق 2026-07-18
- أضيف `extensions` إلى `search_path` في migrations البوابة لأن Supabase قد يثبت `pgcrypto` داخله، وهو ما كان يمنع إنشاء `generate_family_portal_code()` برسالة فقد `gen_random_bytes(integer)`.
- يصلح migration العائلة أيضًا مسار دوال بوابة الطالب المثبتة سابقًا دون الحاجة إلى إعادة تنفيذ P7.2.
- أصبحت حزمة Hotfix تضم `recitation_points_policy.dart` ونموذجي الطالب والصندوق، فلا تختلط خدمات P7 الحديثة مع ملفات Models أقدم عند التحديث الجزئي.

### الهوية والأمان
- أضيف `family_code` عشوائي عالمي من 20 خانة، ثابت وفريد، بدل الكود القصير المشتق من UUID.
- أضيف PIN عائلي مجزأ بـbcrypt، وحد محاولات، وقفل مؤقت، وجلسات 12 ساعة لا تخزن إلا بصمة الرمز.
- يلغي تغيير PIN أو إيقاف الحساب جميع جلسات العائلة القديمة فورًا.

### البوابة والإدارة
- أضيف نوع دخول مستقل لولي الأمر في `/portal` مع تبديل الأبناء النشطين من الحساب نفسه.
- يتحقق الخادم من عضوية الطالب في العائلة داخل كل قراءة ولا يعتمد على معرف يرسله المتصفح.
- أضيفت إدارة التفعيل والتوليد والنسخ والتغيير والإيقاف في شاشة العائلات على Android والويب.
- رُفعت SQLite إلى v18 لحفظ كود العائلة السحابي، مع إبقاء دخول الطالب القديم متوافقًا.

### الجودة
- أضيف migration مستقل وفحص عقد للعائلة، ونجح ESLint وTypeScript وبناء Next.js لـ30 مسارًا.
- يلزم تشغيل تحليل Flutter واختباراته في CI لعدم توفر SDK في بيئة التنفيذ الحالية.

## [غير مُصدر] — المرحلة P7.3: الجهة الإشرافية متعددة المراكز — 2026-07-14

### الصلاحيات والربط
- أضيفت عضويات إشرافية بأدوار مالك ومدير ومحلل، مع بقاء المحلل للقراءة التجميعية فقط.
- استُبدل كود الجهة الدائم بدعوات عشوائية مؤقتة؛ تخزن القاعدة تجزئة الرمز فقط، وتربط دعوة الفريق بالبريد المسجل.
- أضيف سجل إشرافي لربط المراكز وفصلها والدعوات وتغييرات الفريق، ورُحّل مالكو الجهات القديمة تلقائيًا.

### اللوحة والتقارير
- أضيف مسار `/supervision` لإجمال المراكز والحلقات والطلاب والحضور والحفظ والمراجعة والنقاط خلال فترة مخصصة.
- أضيفت إدارة فريق الإشراف ودعوات المراكز وفصل الربط من واجهة واحدة، مع طباعة RTL وحفظ PDF.
- أضيف الوصول إلى اللوحة من اختيار المركز والقائمة والإعدادات، مع قبول دعوة الفريق دون اشتراط اختيار مركز.

### الجودة
- أضيف migration مستقل P7.3 وفحص عقد يمنع إعادة كود الربط الضعيف أو منح المحلل صلاحيات المدير.
- نجح ESLint ضمن الحد المعروف، ونجح TypeScript وبناء Next.js لـ30 مسارًا.

## [غير مُصدر] — المرحلة P7.2: بوابة الطالب وولي الأمر — 2026-07-14

### الأمان والوصول
- أضيف PIN مجزأ بـbcrypt، وحد محاولات مزدوج، وجلسات 12 ساعة لا تخزن إلا بصمة الرمز.
- أضيفت Edge Function تفصل مفتاح الخدمة عن المتصفح، مع CORS قابل للتقييد وبصمة عميل مملحة.
- أضيفت إدارة التفعيل والتغيير والإيقاف من Android والويب، ويلغي تغيير PIN جميع الجلسات القديمة.

### لوحة المتابعة
- أضيف مسار `/portal` العام للطالب وولي الأمر، يعرض الخطة والحضور والحفظ والمراجعة والنقاط للقراءة فقط.
- تدعم اللوحة فترات من أسبوع إلى سنة، والتاريخ الهجري والميلادي، والطباعة RTL إلى PDF.
- ارتفع بناء الويب إلى 29 مسارًا، وأضيف فحص عقد مستقل للبوابة.

## [غير مُصدر] — المرحلة P7.1: هوية الطالب والتقارير والنقاط والخطط الجماعية — 2026-07-14

### الهوية والتقارير
- أضيف `student_code` عام مستقل بقيد فريد في SQLite وSupabase، ولم يعد الكود المعروض اختصارًا قصيرًا من رمز QR.
- أضيف QR إلى رأس تقرير الفترة وبطاقات A4 لجميع الطلاب، مع منع اعتباره سر دخول.
- أضيف ملخص إدارة لجميع الطلاب في صفحة أفقية واحدة، واختيار الأشهر الهجرية الفعلية، ونطاقات الحفظ والمراجعة في رسالة واتساب.

### العدالة والنقاط
- أعيد توزيع نسبة الأداء بحيث لا تعاقب الطالب على تعليق الدراسة أو الإجازة الأسبوعية، ويصل إتمام المقرر الجيد إلى نسبة تشجيعية عادلة.
- أصبحت نقاط التسميع محسوبة على مجموع اليوم لا على السجل الأخير، ومتدرجة حتى عشر نقاط، وتُعاد بعد التعديل والحذف.
- رفعت عقوبة الغياب بلا عذر فوق مجموع المخالفات اليومية المعتادة، وربطت الغرامة المالية بسجل المخالفة اختياريًا.

### الخطط وتجربة الاستخدام
- أضيف مقرر مراجعة افتراضي لملف الطالب، وتملأ الخطة الفردية مقاديرها تلقائيًا منه.
- وُحّد ملف الطالب في الويب مع الهاتف لكود الطالب ومقرري الحفظ والمراجعة، وأضيفت الحقول إلى مخطط التثبيت النظيف.
- أضيف إنشاء وطباعة خطط جميع الطلاب في ملف واحد مع تجاوز أصحاب الخطط النشطة أو اختبار التجاوز المعلّق.
- أضيف دليل استخدام قابل للبحث، وخيار مستقل للمستأذنين في القرعة، ومعالجات overflow في الصندوق والنماذج.

### قاعدة البيانات والتحقق
- رُفعت SQLite إلى v17.
- أضيفت migrations هوية الطالب وربط الغرامة ومقرر المراجعة.
- نجحت بوابات الإصدار الـ22 و`git diff --check`؛ يتطلب Flutter SDK الخارجي تشغيل التحليل والاختبارات قبل APK.

## [غير مُصدر] — مزامنة سحابية باتجاه واضح — 2026-07-13

### الرفع والتنزيل
- استُبدل الأمر المبهم بثلاثة أوامر صريحة: رفع من الجهاز فقط، وتنزيل من السحابة فقط، ومزامنة ثنائية ترفع ثم تنزّل.
- تعرض الواجهة سهم الاتجاه ووقت آخر رفع ووقت آخر تنزيل كلًا على حدة، وتؤكد النتيجة بعبارة «رفع فقط» أو «تنزيل فقط».
- أُلغيت المزامنة الصامتة بعد تسجيل الدخول؛ يسجل المستخدم دخوله أولًا ثم يختار اتجاه نقل البيانات بنفسه.

### سلامة البيانات
- لا ينفذ مسار الرفع أي كتابة تشغيلية محلية، ولا ينفذ مسار التنزيل أي رفع للسجلات المحلية.
- يسبق كل تنزيل أول في اليوم ملف حماية محلي مشفر، وتتوقف العملية إذا لم تُعد عبارة حماية أو تعذر إنشاء النسخة.
- أصبحت تنزيلات الحضور وخريطة المصحف والخطط مقصورة على طلاب الحلقة المحددة بدل تنزيل جميع سجلات المركز.
- أضيف اختبار لسياسة الاتجاه وتوثيق مستقل في `docs/cloud_sync_policy.md`؛ لا يوجد SQL جديد لهذه المرحلة.

## [غير مُصدر] — إصلاح نطاق مراجعة الطالب — 2026-07-13

### مصادر المحفوظ
- أصبحت قائمة المراجعة تجمع المحفوظ من ملف الطالب، وسجلات الحفظ اليومية، وخريطة المصحف القديمة بدل الاعتماد على السجلات اليومية وحدها.
- تستخدم الشاشة أحدث نسخة من ملف الطالب، وتستعيد الطلاب القدماء الذين حُفظ رصيدهم كأثمان مسبقة في خريطة المصحف.
- إذا بقي إجمالي محفوظ بلا بداية ونهاية، تظهر أداة إصلاح تفتح ملف الطالب مباشرة بدل منع تسجيل المراجعة برسالة مبهمة.

### سلامة النطاق والمزامنة
- أصبح اختيار الآيات والسلايدر محصورًا داخل أول وآخر آية محفوظة، وتشمل الحماية أزرار نهاية الصفحة والحزب والسورة كاملة والتحقق النهائي عند الحفظ.
- تستأنف دورة المراجعة من أول آية محفوظة في السورة الجزئية ولا تقفز إلى آيات غير محفوظة.
- أصبحت مزامنة الطلاب تحذف فقط العمود السحابي القديم المفقود من الطلب، فلا يؤدي غياب حقل عائلة مثلًا إلى إسقاط نطاق المحفوظ كله.
- أضيف SQL توافقي مستقل لأعمدة نطاق المحفوظ في Supabase: `20260713000400_p6_revision_profile_range_compat.sql`.

## [غير مُصدر] — إصلاح استقرار بناء Android — 2026-07-13

### Gradle وKotlin
- خُفض الحد الأعلى لذاكرة Gradle من 8GB إلى 3GB لتجنب استنزاف ذاكرة Windows وملف الترحيل أثناء ضغط أصول التطبيق.
- أصبح مترجم Kotlin يعمل داخل عملية Gradle بدل الاعتماد على Kotlin daemon منفصل تعذر تشغيله على بعض الأجهزة.
- حُدّد عدد Gradle workers بعمليتين لتقليل قمم استهلاك الذاكرة أثناء البناء الأول.
- لا يتطلب الإصلاح أي SQL أو تغييرًا في بيانات التطبيق.

## [غير مُصدر] — المرحلة P6.2.1: الهوية الموحدة والمساحة الآمنة — 2026-07-13

### الهوية والخط
- أعيد ضبط الهوية إلى أخضر قرآني هادئ وعاجي دافئ مع ذهبي خافت، مع دعم مقابل متوازن للوضع الداكن.
- أصبح Tajawal خط الواجهة الوحيد في Android والويب، ووُحّدت عليه الأرقام التي كانت تستعمل Outfit والعناوين التي كانت تستعمل Cairo.
- ضُمّنت أوزان 400 و500 و700 و800 داخل تطبيق Flutter، وأضيفت نسخة ويب ذاتية الاستضافة حتى لا يعتمد الخط على تنزيل وقت التشغيل.
- بقي خط النص القرآني مستقلًا عن خط الواجهة حتى لا تتأثر هيئة الآيات.

### الخفة وسهولة الاستخدام
- خُففت البطاقات والظلال والألوان، ووُحدت الحواف والمسافات وارتفاعات الأزرار والحقول وأشرطة التنقل والقوائم والنوافذ السفلية.
- أزيل رمز فلترة غير عامل من حقل البحث المشترك، وأعيد حوار الخروج إلى مكونات الثيم بدل ألوان ثابتة لا تناسب الوضع الفاتح.
- بُسّطت هوية القائمة الجانبية وشريط الجوال في الويب، وأزيل التكبير والظل الثقيل من العنصر النشط.

### Safe Area والتحقق
- أضيفت Safe Area مركزية أسفل Navigator لكل مسارات Flutter، مع تلوين شريط النظام وتبديل لون رموزه حسب الوضع.
- فُعل Edge-to-Edge مع حجز فعلي لأزرار Android ومنطقة الإيماءة، فيشمل الإصلاح الشاشات القديمة والحوارات والـbottom sheets.
- أضيف `viewport-fit=cover` ومسافات `env(safe-area-inset-*)` للويب/PWA وشريط الجوال السفلي.
- أضيف فحص عقد يمنع حذف الخطوط المضمنة أو رجوع Cairo/Outfit أو فقدان Safe Area؛ لا يوجد SQL لهذه المرحلة.

## [غير مُصدر] — المرحلة P6.2: حماية البيانات والنسخ السحابي المشفر — 2026-07-13

### النسخ والاستعادة
- أصلح تعارضًا كان يجعل التطبيق يصدّر نسخة `2.1` ثم يرفض استعادتها لأنه كان يقبل `2.0` فقط.
- أصبحت النسخ الجديدة بصيغة `3.0` داخل غلاف AES-256-GCM موثق، مع مفتاح مشتق من عبارة حماية وملح وnonce جديدين لكل ملف.
- تحفظ عبارة الحماية في مخزن مفاتيح النظام، ولا تدخل SQLite أو سجل التدقيق أو ملف النسخة.
- أضيف تحقق SHA-256 داخلي وحد حجم 100MB وفحص البنية قبل بدء معاملة الاستعادة.
- بقي استيراد نسخ `2.0` و`2.1` والنسخ القديمة متاحًا للترقية، مع تمييزها بوضوح كملفات غير مشفرة.

### السحابة والتدقيق
- أضيف رفع واستعادة تلقائية ويدوية للملف المشفر إلى bucket خاص في Supabase مع احتفاظ 10/30/60 نسخة.
- إذا فشل الرفع تبقى النسخة المحلية سليمة ولا تتراجع عملية النسخ.
- رُفعت SQLite إلى الإصدار 14 وأضيف سجل تدقيق محلي وتلقائي لتغييرات الطلاب والحضور والتسميع والنقاط والخطط وغيرها دون نسخ الأسماء أو الهواتف أو الملاحظات.
- أضيف جدول تدقيق سحابي غير قابل للتعديل وسياسات RLS ودالة RPC وهوية فاعل مأخوذة من الجلسة لا من العميل.

### الخصوصية والواجهات
- أضيف قسم إعداد عبارة الحماية وحالة التشفير وإدارة النسخ السحابية وسياسة الاحتفاظ إلى Android.
- أضيفت شاشة سجل تدقيق وسياسة خصوصية وإدارة بيانات في Android والويب.
- أضيف مسار `/audit-log` بفلترة النتائج ومسار `/privacy`، وربطهما بالإعدادات والقائمة الجانبية للمسؤولين.
- عُطّل Android Auto Backup غير المشفر، وأصبح الحد الأدنى للنظام Android 6.0 لاستخدام مخزن المفاتيح الآمن.

### التحقق وSQL
- نجحت بوابة الويب كاملة: صفر أخطاء و23 تحذيرًا معروفًا، 19 فحص عقد، وبناء 28 مسارًا.
- نجح تشغيل migration مرتين على PostgreSQL مصغّر يحاكي مخطط Supabase، بما في ذلك الدوال والسياسات والـbucket و11 trigger.
- أضيف اختبار تشفير وفك وتحقق من رفض العبارة الخاطئة وكشف العبث؛ تشغيله ينتظر Flutter CI لعدم توفر SDK محليًا.
- يلزم تنفيذ migration `20260713000300_p6_data_privacy_cloud_backup.sql` قبل تفعيل النسخ السحابي.

## [غير مُصدر] — المرحلة P6.1: الحماية وبوابات جودة الإصدار — 2026-07-13

### حماية الويب
- أضيفت سياسة CSP ورؤوس منع التضمين والتخمين ومنع تسريب نوع الخادم وسياسة صلاحيات تقصر الكاميرا على التطبيق.
- أصبح رابط Supabase يقبل HTTPS أو localhost فقط، ولا ينشأ العميل دون رابط ومفتاح عام صالحين.
- أضيف نموذج بيئة موثق يمنع وضع مفتاح `service_role` في متغير عام.
- أضيفت صفحات عربية متجاوبة لأخطاء المسارات والخطأ الجذري و404 مع إعادة محاولة آمنة ورمز تتبع.

### الاعتمادات وجودة المصدر
- حدث Next.js من 16.2.4 إلى 16.2.10 وSupabase JS إلى 2.110.2، فاختفت جميع تنبيهات `npm audit` العالية وبقي تنبيهان متوسطان بلا إصلاح غير كاسر.
- عولجت أخطاء ESLint الـ51 بالكامل، وخفض مجموع نتائج lint من 144 إلى 23 تحذير hooks معروفًا.
- أزيلت أنواع `any` الصريحة من الويب وأضيفت نماذج صفوف واضحة لبيانات Supabase.
- أصلحت مشكلات نوعية في اختيار المركز والحلقة وترتيب لوحة الشرف وربط أفراد العائلة.

### CI والإطلاق
- أضيف سير GitHub Actions للويب وFlutter عند الدفع وطلبات الدمج والتشغيل اليدوي.
- تجمع بوابة الويب تدقيق الاعتمادات وlint وفحوص العقود الثمانية عشر والبناء في أمر واحد.
- حدث سير APK إلى Java 17 وFlutter stable وArtifact v4 مع analyze واختبارات قبل البناء.
- لا تتطلب هذه المرحلة SQL أو migration جديدًا.

## [غير مُصدر] — المرحلة P5.8: نظام التصميم وتجربة الاستخدام — 2026-07-13

### Android
- أضيفت رموز موحدة للمسافات والحواف ونقاط التحول بدل القيم المتفرقة بين الشاشات.
- أعيد بناء الثيم الفاتح والداكن ليشمل البطاقات والحقول والأزرار والحوارات والشرائح وأشرطة التنقل والتنبيهات.
- أضيفت مكونات مشتركة لرأس الصفحة والعناوين الفرعية والبحث والحالات الفارغة والمؤشرات.
- طبقت المكونات على شاشات الطلاب والتقارير والحضور، ووُحدت بطاقة الطالب بدل تكرار تصميمها.

### الويب والتنقل
- أضيفت طبقة مكونات مشتركة للرؤوس والأسطح والبحث والمؤشرات والحالات الفارغة.
- طبقت على صفحات الطلاب والحضور والتسميع مع تحسين الاستجابة والوضع الداكن وحالات عدم وجود نتائج.
- أصبح شريط الجوال السفلي يفتح الوظائف اليومية الأهم: الرئيسية والطلاب والحضور والحفظ والتقارير.
- حسنت تسميات الوصول، وحالة الصفحة الحالية، وحركة الواجهات مع احترام خيار تقليل الحركة.

### التحقق
- أضيف فحص عقد مستقل لنظام التصميم ونجح مع فحوص المصدر و`git diff --check`.
- نجح بناء Next.js الإنتاجي وفحص TypeScript لكل المسارات الـ26.
- لا تتطلب هذه المرحلة أي SQL أو migration جديدًا.

## [غير مُصدر] — المرحلة P5.7: خريطة المصحف والاختبارات المتقدمة — 2026-07-13

### توليد الأسئلة
- أضيف اختيار بصري من 240 ربعًا مرتبة على 60 حزبًا مع حفظ النطاق داخل قالب الاختبار.
- أصبح مولد السؤال قادرًا على إكمال الإجابة في السورة التالية إذا بقي داخل الفئة المختارة.
- أضيف إلى محرر السؤال خيار «نهاية الصفحة» و«نهاية الحزب» مع دعم عبور حد السورة.
- صُححت بيانات الآيات 11–25 من سورة يونس من الربع 48 إلى الربع 84، وأضيف فحص يربط الربع بحزبه ويضمن وجود الأرباع الـ240.

### الرصد والطباعة
- أضيف رصد رقمي مستقل لأخطاء الحفظ والتشكيل والتلاوة ومرات التنبيه لكل سؤال.
- تحسب الدرجة من 10 بسياسة موحدة: خطأ الحفظ درجتان، وبقية الأخطاء والتنبيه درجة واحدة، مع حد أدنى صفر.
- تحفظ نتيجة الرصد في قالب الاختبار وتظهر في PDF عند رصد السؤال فقط، مع كود الطالب والنتيجة الإجمالية.

### التخزين والسحابة
- رُفعت SQLite إلى الإصدار 13 مع توافق القوالب القديمة التي كانت تنتهي داخل سورة واحدة.
- أضيفت حقول النطاق متعدد السور والرصد إلى مزامنة `exam_questions` وترحيل Supabase محمي بسياسات الحلقة.
- أضيف ملف توافق مرتب قبل P5.6 لعلاج غياب `memorization.halaqa_id` في المخطط السحابي القديم.

### التحقق
- نجحت جميع فحوص العقود الستة عشر وسلامة بيانات القرآن و`git diff --check`.
- نجح بناء Next.js الإنتاجي لكل المسارات الـ26.
- يلزم تشغيل اختبارات Flutter والطباعة والمزامنة على جهاز وقاعدة مرحلية قبل الإنتاج.

## [غير مُصدر] — إصلاح توافق عمود حلقة التسميع السحابي — 2026-07-13

- أضيف ترحيل توافق مستقل ينشئ `memorization.halaqa_id` قبل استعماله.
- يملأ الترحيل الحلقة والمركز من سجل الطالب دون حذف أي بيانات.
- أصبح علاج الخطأ `column progress.halaqa_id does not exist` واضحًا وقابلًا لإعادة التنفيذ.

## [غير مُصدر] — المرحلة P5.6: تكافؤ القرعة والتسميع في الويب — 2026-07-13

### القرعة المستمرة
- أصبحت حالة القرعة منفصلة لكل حلقة وتشمل المسحوبين والمستبعدين والطالب الحالي ونتيجة السحب الجاري.
- أضيف استبعاد اختياري للغائب والمستأذن من حضور اليوم.
- أضيف ترتيب دفعة واحدة مرقم، مع استكمال السحب وفق القائمة ومنع التكرار.
- يفتح الطالب المختار مباشرة في نموذج التسميع، وتبقى الدورة بعد التنقل.

### السجل والتصحيح
- أضيفت فلاتر الطالب والنوع والفترة إلى سجل الويب.
- أضيف تعديل وحذف للسجل مع تحقق نطاق السورة ومنع مضاعفة مكافأة الزيادة عند التعديل.
- تعاد خريطة المصحف من السجلات الفعلية بعد كل إضافة أو تصحيح أو حذف.

### سلامة البيانات والمزامنة
- أصبح حفظ التسميع في الويب ذريًا بين `homework_grades` و`memorization`، فأصبحت سجلات الويب تدخل التقارير.
- أضيف حذف لين ووقت آخر تعديل وسياسات RLS على مستوى الحلقة ودالتا RPC محميتان.
- رُفعت SQLite إلى الإصدار 12 وأصبحت مزامنة سجلات الحفظ ثنائية الاتجاه مع قاعدة «الأحدث يفوز» و«الحذف يفوز».
- أضيف backfill غير مكرر لسجلات الويب القديمة، وتوافق استعادة النسخ الاحتياطية القديمة.
- صُحح migration ليضيف `memorization.halaqa_id` قبل استخدامه، توافقًا مع المخطط السحابي الفعلي المرفق الذي لم يكن يحتوي العمود.

### التحقق
- نجح فحصا P5.6 وسجل التسميع، ونجح بناء الويب الإنتاجي لـ26 مسارًا.
- migration جديد مطلوب قبل نشر واجهة P5.6.

## [غير مُصدر] — المرحلة P5.5: التقارير المرئية والتجميعية — 2026-07-13

### تقرير الحلقة الحقيقي
- أضيف محرك تقرير للحلقة من تاريخ إلى تاريخ يجمع تقارير الطلاب الفردية نفسها دون الاعتماد على الإجماليات العمرية.
- يحسب الحفظ والمراجعة والصفحات والأجزاء والحضور والغياب وعدم التسميع والنقاط ومستوى الحلقة.
- يعرض المتميزين وطلاب المتابعة مع استبعاد الطالب الذي لا توجد له بيانات من التنبيه والترتيب.

### Android
- استبدلت نافذة «إحصائيات الحلقة» بتقرير كامل بفلاتر اليوم والأسبوع والشهر والفترة المخصصة.
- أضيف قالب صورة عربي احترافي مستقل عن الوضع الداكن وقابل للمشاركة كـPNG.
- أضيف PDF تجميعي RTL يحتوي ملخص الحلقة وجدول جميع الطلاب والتنبيهات.

### الويب
- أضيف اختيار فترة وتنزيل صورة PNG بحجم مشاركة، وتوحيد المؤشرات مع البيانات الفعلية.
- أزيلت نسب الاختبارات والحضور والتحسن التجريبية من صفحة التقارير.
- أصبحت خلاصة الحلقة، المتميزون، الحضور وطلاب المتابعة محسوبة من الفترة المختارة.

### التحقق
- أضيف اختبار تجميع وفحص عقد يمنع عودة النسب الوهمية.
- نجح بناء الويب الإنتاجي. لا تتطلب المرحلة SQL جديدًا.

---

## [غير مُصدر] — المرحلة P5.4: العائلات وأولياء الأمور — 2026-07-12

### نموذج عائلي صريح
- أضيف كيان عائلة مستقل برمز ثابت واسم مرجعي للجد أو الفرع العائلي لتمييز الأسماء المتشابهة.
- أصبح بالإمكان ربط عدة طلاب بعائلة واحدة ونقل الطالب بين العائلات بإجراء واضح.
- أضيف عدة أولياء أمر للعائلة مع صلات موسعة وولي أساسي واحد يستخدم افتراضيًا للتواصل.
- بقي رقم ولي الأمر القديم داخل الطالب للتوافق مع التقارير والرسائل، ويحدّث من الولي الأساسي.

### Android والويب
- أضيفت شاشة متكاملة للعائلات من قائمة الطلاب والقائمة الجانبية وملف الطالب.
- أضيف اختيار العائلة في نموذج الطالب بدل الاعتماد على اقتراح تشابه الاسم فقط.
- أزيلت بيانات أولياء الأمور الوهمية من الويب وربط مسار `/parents` فعليًا بـSupabase.
- تظهر تحذيرات واضحة عند نقل طالب مرتبط بعائلة أخرى.

### البيانات والحماية
- رُفعت SQLite إلى الإصدار 11 وأضيفت العائلات والأولياء إلى النسخة الاحتياطية والاستعادة.
- أضيفت مزامنة ثنائية وحذف متزامن مع توافق مؤقت قبل تطبيق migration.
- أضيف migration مكتفٍ ذاتيًا بسياسات RLS ومنع ربط عائلة وطالب من نطاقين مختلفين.
- أضيفت اختبارات نموذج وفحص عقد، ونجح بناء الويب الإنتاجي.

---

## [غير مُصدر] — المرحلة P4.2: تقرير عربي احترافي وتصدير جماعي — 2026-07-12

### تصميم التقرير
- أعيد بناء تقرير الفترة بصريًا من اليمين إلى اليسار، مع عكس ترتيب أعمدة الجدول فعليًا ليتوافق مع القراءة العربية.
- أضيف رأس احترافي يبرز اسم الحلقة والمسجد، وكود الطالب المختصر، وفترة التقرير.
- أضيف تقييم أداء واضح كنسبة مئوية في رأس الصفحة، مع لون متغير حسب المستوى.
- أضيف ترقيم للصفحات وتوقيع المعلم وولي الأمر وخلاصة الحفظ والمراجعة والحضور.

### التصدير الجماعي
- أضيف زر لإنشاء ملف PDF واحد لجميع الطلاب، يبدأ فيه تقرير كل طالب من صفحة مستقلة.
- يدعم اختيار فترة مخصصة وA4 أو A5، مع خيار الطلاب النشطين فقط أو تضمين الأرشيف.
- أضيف مؤشر تقدم يمنع تكرار العملية ويوضح عدد التقارير المكتملة.

### التنقل
- أضيفت قائمة جانبية شاملة لأهم أقسام التطبيق وأدوات الحلقة.
- أصبحت القائمة متاحة من الرئيسية والطلاب والحضور والتقارير والإعدادات.

### التحقق
- أضيف اختبار ثبات كود الطالب وفحص عقد لتصميم PDF والتصدير الجماعي والقائمة الجانبية.
- لا تتطلب هذه المرحلة أي تعديل SQL أو migration جديد.

---

## [غير مُصدر] — المرحلة P5.3: متميزو اليوم وملخص الطالب — 2026-07-12

### متميزو اليوم
- أضيفت واجهة مستقلة يومية في Android والويب، مختلفة عن لوحة الشرف التراكمية.
- يظهر تلقائيًا من تجاوز مقرره الفعلي بالآيات أو الصفحات أو الأسطر، مع منع تكرار النطاقات المتداخلة.
- يمكن للمعلم إضافة متميز يدويًا بسبب موثق واختيار تاريخ سابق.

### التحفيز والمكافآت
- أضيفت مكافآت النقاط والشهادات والهدايا والوجبات الجماعية والتكريم الحر.
- تحفظ المكافأة ونقاطها ذريًا، وتُصحح نقاط التكريم عند تغيير نوع المكافأة أو قيمتها.
- تدخل سجلات التميز ضمن النسخ الاحتياطي والمزامنة الثنائية وسياسات RLS.

### ملف الطالب
- أضيف ملخص لإنجاز اليوم وآخر حفظ وآخر مراجعة والخطة النشطة.
- أضيف وصول مباشر لتعديل المقرر اليومي من بطاقة المسار.
- أصبح الويب يميز الحفظ الجديد عن المراجعة عند حساب الإنجاز اليومي.

---

## [غير مُصدر] — المرحلة P5.2: أرشيف الطلاب وتدقيق النقاط — 2026-07-12

### أرشيف الطلاب
- أضيفت شاشة مستقلة للمفصولين والمتخرجين والطلاب السابقين مع البحث والفلترة وإعادة التفعيل.
- اختفت حالات الأرشيف من قائمة الطلاب التشغيلية، مع بقاء البيانات والتقارير كاملة.
- أصبح كل تغيير حالة، بما فيه الفصل التلقائي، يسجل الحالة السابقة والجديدة والسبب والوقت.

### سلامة النقاط والمخالفات
- وُحد إدخال النقاط في نموذج مركزي يعرض هوية الطالب ورقم ولي الأمر قبل التأكيد.
- أضيف تصحيح إسناد السجل لطالب آخر وحذف موثق بسبب إلزامي وسجل تدقيق.
- أزيلت عقوبة يومية كانت تظهر حسابيًا في واجهة المخالفات دون تسجيلها في البيانات.
- تمنع قاعدة البيانات الإشارة المخالفة لنوع النقاط وإضافة نقاط لطالب مؤرشف.

### Android وSupabase
- صُححت مزامنة المخالفات السلبية؛ لم تعد القيمة السالبة تتحول إلى موجبة قبل الرفع.
- أضيف حذف سحابي لسجل النقاط المصحح ومزامنة سجل التصحيحات.
- أضيف migration مكتفٍ ذاتيًا للنطاق، وسجل الحالات، وسجل التصحيحات، وإصلاح القيم التاريخية.

---

## [غير مُصدر] — المرحلة P5.1: الإعدادات وحماية البيانات — 2026-07-12

### تنظيم الإعدادات
- قسمت شاشة الإعدادات إلى أقسام الحلقة والأوقات والقواعد والمظهر والبيانات وحول التطبيق.
- استبدل وصف مزامنة WiFi/Bluetooth غير المنفذ بحالة اتصال Supabase الفعلية.

### النسخ المحلي
- أضيف تذكير قابل للضبط عند غياب نسخة حديثة، مع خيار النسخ مباشرة من النافذة.
- أضيف نسخ تلقائي محلي عند أول فتح بعد الموعد المحدد ولمرة واحدة في اليوم.
- أضيف اختيار وقت النسخ وعدد النسخ التلقائية المحتفظ بها، مع عدم حذف النسخ اليدوية.
- تعرض شاشة البيانات آخر نسخة وعدد الملفات وتنبيه فشل آخر محاولة تلقائية.

### التحقق والتوثيق
- أضيف محرك سياسة مستقل واختبارات لموعد النسخ وفترة التذكير وفحص عقد للربط الكامل.
- أضيف ملف تسليم للمرحلة وخريطة محدثة للمراحل المتبقية.

---

## [غير مُصدر] — المرحلة P1.4: استمرارية المراجعة — 2026-07-12

### موضع المراجعة
- أصبحت شاشة المراجعة تقرأ آخر سجل مراجعة للطالب وتبدأ من الآية التالية تلقائيًا.
- عند نهاية السورة تنتقل الدورة إلى السورة المحفوظة التالية حسب ترتيب المراجعة، وتلتف إلى بداية الدورة عند النهاية.
- يظهر للمعلم موضع الاستئناف ومقدار المراجعة المقترح بوضوح.

### ربط الخطة والحدود
- يستخدم النطاق المقترح وحدة ومقدار المراجعة من خطة الطالب النشطة، سواء كانت آيات أو صفحات أو أسطرًا.
- أضيفت للمراجعة اختصارات نهاية الصفحة ونهاية الحزب والسورة كاملة.
- بقيت المراجعة الإلزامية بعد إتمام السورة مقدمة على دورة المراجعة العادية.

### سلامة السجل
- تحفظ سجلات المراجعة وتقييماتها وملخص اليوم داخل transaction واحدة لمنع النجاح الجزئي.
- أصبح لكل مراجعة تقييم `HomeworkGrade` متزامن زمنيًا، وتُحدث خريطة المصحف بعد الحفظ.
- أضيف محرك مستقل لاستمرارية المراجعة واختبارات للمتابعة داخل السورة والانتقال والالتفاف التنازلي.

---

## [غير مُصدر] — المرحلة P1.3: التسميع المفتوح وحدود الصفحة والحزب — 2026-07-12

### التسميع المفتوح
- أصبح تسميع Android يبدأ افتراضيًا من آية بداية فقط دون إلزام المعلم بتحديد النهاية مسبقًا.
- أضيف زر «التوقف هنا» لاعتماد الآية الحالية بوصفها نهاية الجلسة، مع منع الاعتماد قبل تقييمها.
- عند الرجوع والتوقف عند آية سابقة تُحذف تقييمات الآيات اللاحقة من الجلسة ولا تدخل في المتوسط أو الحفظ.
- تحول مؤشر التقدم والوقت المقترح إلى وضع ديناميكي أثناء الجلسة المفتوحة.

### حدود المقرر
- أضيف زرا «نهاية الصفحة» و«نهاية الحزب» في Android والويب اعتمادًا على بيانات المصحف الفعلية.
- أضيف وضع آية مفردة متزامن بين حقل الإدخال والسلايدر لاختيار بداية التسميع المفتوح.
- أضيف محرك مستقل لحد الصفحة والحزب مع اختبارات رجوع للحالات الحدّية.

### قاعدة البيانات السحابية
- صُحح migration الخطط ليكون مكتفيًا ذاتيًا وينشئ دوال نطاق الصلاحيات المتوافقة مع المخطط السحابي المرفق.
- وُثق تدقيق المخطط والفجوات وترتيب migrations الآمن دون تنفيذ أي تغيير على السحابة.

---

## [غير مُصدر] — المرحلة P2.3: الخطط واختبار التجاوز — 2026-07-12

### إنشاء الخطط وتعديلها
- أعيد بناء شاشة الخطط في Android لإزالة الخلل البنيوي وإتاحة الإنشاء والتعديل والحذف والفلترة.
- أضيفت خطة أسبوعية وشهرية، مع أزرار زيادة ونقصان حقيقية لمقرر الحفظ والمراجعة.
- أصبح تفعيل الخطة أو تعديلها يحدّث وحدة ومقدار المقرر الافتراضي للطالب.
- تحولت خطط الويب من حالة مؤقتة تختفي عند تحديث الصفحة إلى سجلات Supabase فعلية.

### اختبار التجاوز
- عند إكمال الخطة تنتقل إلى «بانتظار اختبار التجاوز» بدل السماح بخطة جديدة مباشرة.
- لا يعتمد التجاوز إلا باختبار ناجح للطالب بعد تاريخ إكمال الخطة، مع حفظ معرف الاختبار.
- أضيف حارس مماثل داخل SQLite وداخل PostgreSQL لمنع تجاوز القاعدة من واجهة أخرى.
- تعاملت الترقية مع الخطط التاريخية دون مطالبتها بأثر رجعي باختبار غير موجود.

### الطباعة والمزامنة
- أضيف قالب خطة يومي قابل للطباعة على A4 وكاشير 80 مم مع استثناء يوم العطلة الأسبوعية.
- أصبحت المزامنة ثنائية الاتجاه للخطط وفق `updated_at`، مع حذف سحابي لين يمنع عودة خطة حذفها الويب عند مزامنة Android.
- أضيف migration سحابي تراكمي غير هدام للحقول والفهارس وRLS والتحقق من اختبار التجاوز.

### التحقق
- أضيف فحص عقد مستقل للخطط، ونجحت فحوصات الخطط والسجل والانضباط والتقارير والأمان والقرآن وTypeScript.

---

## [غير مُصدر] — المرحلة P1.2: سجل التسميع القابل للتصحيح — 2026-07-12

### الوصول والفلترة
- أضيف سجل موحد للحفظ الجديد والمراجعة مع البحث بالطالب أو السورة، والفلترة حسب الطالب والنوع والفترة.
- أصبح فلتر شاشة الحفظ يعمل في تبويبي الحفظ والمراجعة، بعناوين «سمّع اليوم» و«لم يسمّع اليوم».
- أضيف مدخل للسجل من شاشة الحفظ ومن صفحة تفاصيل الطالب.

### التعديل والحذف الآمن
- يمكن تعديل السورة ونطاق الآيات والتاريخ والتقييم ونوع الجلسة والملاحظات.
- يمكن حذف السجل بعد تأكيد يعرض الطالب والتاريخ، مع حذف التقييم المصاحب إذا كان مرتبطًا بالجلسة نفسها.
- يعاد حساب حالة ومقدار الحفظ والمراجعة في اليوم المتأثر، وإجمالي محفوظ الطالب، وخريطة المصحف بعد كل تعديل أو حذف.
- يحافظ حساب الإجمالي على المحفوظ التاريخي غير المفصل ويطبق فرق السجل فقط، لمنع رجوع مشكلة اختفاء المحفوظ.

### الحضور والمزامنة
- زرا الحفظ والمراجعة في بطاقة الحضور يفتحان جلسة الطالب المحدد ويعيدان تحميل البطاقة بعد الحفظ.
- يمنع فتح جلسة من تاريخ حضور قديم أو أثناء إيقاف التسميع المؤقت، برسالة واضحة.
- أصبحت المراجعة تُرسل إلى Supabase بنوع `review`، وتزامن الحذف يستخدم tombstones حتى لا تعيد السحابة السجل المحذوف.

### التحقق
- أضيف اختبار لحساب فرق الإجمالي مع رصيد محفوظ تاريخي، وفحص ثابت لعقد التعديل والحذف والمزامنة.

---

## [غير مُصدر] — المرحلة P3.1: الإجازات والانضباط — 2026-07-11

### الإجازة الجماعية
- أصبحت شاشة الإجازات تسمح باختيار طالب واحد أو عدة طلاب أو جميع الطلاب النشطين لنفس السبب والفترة والملاحظة.
- تحفظ المجموعة داخل transaction واحدة، وتحوّل سجلات الغياب الموجودة إلى «مستأذن» دون نجاح جزئي.

### إيقاف التسميع المؤقت
- أضيف سجل مستقل للإيقاف المؤقت بمدة وسبب وملاحظات؛ لا يغيّر حالة الطالب ولا يخفيه من شاشة الحضور.
- تظهر علامة الإيقاف في الحفظ والمراجعة، وتغلق الأزرار مع بيان السبب وتاريخ النهاية.
- أضيف منع داخل طبقة قاعدة البيانات نفسها حتى لا يمكن تجاوز الإيقاف من شاشة أخرى.
- يدخل سجل الإيقاف في النسخ الاحتياطي والمزامنة، مع migration سحابي وسياسات RLS مشتقة من الطالب.

### التنبيهات والفصل
- لا تنشأ تنبيهات «لم يسمّع» قبل انتهاء دوام الحلقة أو في يوم معلق/عطلة أسبوعية.
- أضيف تنبيه عند عدم التسميع في يومين دراسيين متتاليين، وتُقطع السلسلة عند الاستئذان أو وجود إيقاف مؤقت.
- أصبح تحذير الغياب يستخدم حد الإعدادات، وأصبح الفصل التلقائي ينقل الطالب إلى حالة «مفصول» عند بلوغ الحد إذا كان الخيار مفعلًا.
- تستثني سلاسل الغياب الجمعة وأيام تعليق الدراسة.

### التحقق
- أضيف اختبار لسريان الإيقاف ونهايته التاريخية، وامتد اختبار التقرير للتأكد من عدم احتساب يوم الإيقاف ضمن «لم يسمّع».
- أضيف فحص ثابت لعقد الانضباط بسبب عدم توفر Flutter SDK في البيئة الحالية.

---

## [غير مُصدر] — المرحلة P4.1: تقارير الطالب للفترة — 2026-07-11

### حساب الفترة
- أضيف تقرير لطالب من تاريخ إلى تاريخ، مع اختصاري آخر سبعة أيام والشهر الحالي.
- يحسب التقرير الحفظ والمراجعة والآيات والأسطر وما يعادلها من صفحات وأجزاء داخل الفترة فقط.
- يفصل الحضور والتأخير والغياب والاستئذان و«حضر ولم يسمّع»، ولا يحسب أيام تعليق الدراسة أو العطلة الأسبوعية غيابًا.
- تظهر ملاحظات الإجازات وأسباب تعليق الدراسة والنقاط الإيجابية والسلبية ضمن اليوم نفسه.

### العرض والتصدير
- أضيفت معاينة يومية قابلة للتوسيع ومؤشر أداء يجمع المواظبة والتسميع والمراجعة والجودة والنقاط.
- أضيف PDF مفصل بمقاسي A4 وA5، يتضمن ملخص الفترة وأشرطة الأداء وجدول الأيام.
- أصبح قالب WhatsApp الأسبوعي أو الشهري يستخدم الفترة المختارة فعلًا، مع إحصاءات ورموز تشجيعية.

### التحقق
- أضيف اختبار وحدة لحساب الأسبوع واستبعاد الجمعة واليوم المعلق من الغياب وإظهار ملاحظة الإجازة.
- أضيف فحص ثابت لعقد التقارير بسبب عدم توفر Flutter SDK في بيئة العمل الحالية.

---

## [غير مُصدر] — المرحلة P2.2: اعتماد نماذج الاختبارات — 2026-07-11

### اعتماد الأسئلة وتحريرها
- أضيف حفظ دائم لنموذج الاختبار وأسئلته في SQLite، مع تضمين الجداول الجديدة في النسخة الاحتياطية والاستعادة الآمنة.
- أصبح كل سؤال قابلًا للتحريك للأعلى أو الأسفل، والتعديل، والاستبدال ضمن المعايير، أو الحذف قبل الاعتماد.
- أضيف سجل لنماذج الاختبارات يتيح إعادة فتح النموذج وتحديثه أو حذفه وطباعته على A4 أو A5.

### المزامنة والأمان
- أضيفت مزامنة القوالب والأسئلة إلى Supabase بعد مزامنة الطلاب، مع نطاق المركز والحلقة والطالب.
- أضيف migration تراكمي يفرض RLS على القوالب والأسئلة ويشتق نطاق القالب من الطالب بدل الثقة في قيم الجهاز.
- لم يُطبّق migration على قاعدة الإنتاج، ويلزم اختباره على قاعدة مرحلية قبل النشر.

### التحقق
- وُسع فحص عقد الأمان ليتحقق من migration القوالب ومسار مزامنة Android.
- يظل تشغيل تحليل Flutter واختبارات الجهاز والطباعة الفعلية مطلوبًا لعدم توفر Flutter SDK في بيئة العمل الحالية.

---

## [غير مُصدر] — المرحلة P2.1: مولد الاختبارات — 2026-07-11

### توليد الأسئلة في Android
- أضيف محرك مستقل يولد من محفوظ الطالب أو نطاق أجزاء أو نطاق أحزاب أو سورة ونطاق آيات.
- أضيفت معايير عدد الأسئلة، والأسطر التقريبية للإجابة، والصعوبة، ومنع تكرار بدايات الأسئلة بين عمليات التوليد.
- تلتزم الإجابة بحدود السورة أو الجزء أو الحزب المحدد، وتستثني البسملة رقم 0.
- أضيفت معاينة تتضمن السورة والآية والإجابة الكاملة وإعادة التوليد.

### نموذج الطباعة
- أضيف قالب A4 وA5 قريب من النموذج المرجعي، بأعمدة أخطاء الحفظ والتشكيل والتلاوة والتنبيه ودرجة السؤال، مع النتيجة والملاحظات والتوقيعات.

### الاختبارات
- أضيفت اختبارات وحدة لترشيح الجزء والصعوبة ومحفوظ الطالب ونطاق الآيات ومنع التكرار.
- تشغيل اختبارات Flutter والطباعة الفعلية ما زال مطلوبًا على جهاز تطوير يحتوي Flutter SDK.

---

## [غير مُصدر] — المرحلة P0.2: الأمان وQR وثبات بناء الويب — 2026-07-11

### الدعوات والصلاحيات
- أضيف migration تراكمي يلغي وظيفتي الدعوة القديمتين اللتين كانتا تكشفان بيانات الدعوة أو تقبلان `user_id` من الجهاز.
- أصبحت الدعوة عالية العشوائية، مرتبطة ببريد المستخدم الموثق، أحادية الاستخدام، وتنتهي بعد 14 يومًا.
- أضيفت سياسات RLS تقصر المعلم على الحلقة المسندة إليه، مع فصل صلاحيات مدير المركز عن المعلم.
- أصبحت حقول المركز والحلقة في الحضور والحفظ والنقاط والإجازات مشتقة من الطالب داخل قاعدة البيانات لمنع تزوير النطاق من العميل.

### QR والحضور
- وُحد عقد QR بين Android والويب باستخدام رمز الطالب العشوائي بدل كشف UUID صف الطالب.
- احتفظ Android بدعم قراءة البطاقات المطبوعة بالإصدار القديم.
- أصبح حضور الويب يستخدم `upsert` مع قيد طالب/تاريخ، ويحدّث حالة الواجهة بدل تكرار الطالب.
- أصلح ماسح الويب دورة React، وحالة تكرار المسح، والتاريخ المحلي، ووقت الوصول، والتحقق من الحلقة الحالية.

### بناء الويب
- أزيل تنزيل Cairo من Google Fonts أثناء البناء واستبدل بخطوط نظام عربية، فأصبح بناء Next.js قابلًا للتكرار دون اتصال خارجي.

### التحقق
- نجح فحص بيانات القرآن وTypeScript وESLint للملفات الجديدة/المعدلة في QR.
- نجح بناء Next.js الإنتاجي بجميع المسارات الـ25.
- أضيف اختبار Flutter لعقد QR الجديد والتوافق مع بطاقات Android القديمة، ويحتاج Flutter SDK لتشغيله.

> لم يطبق migration على قاعدة حقيقية؛ يلزم نسخة احتياطية وقاعدة مرحلية واختبار مدير ومعلمين في حلقتين قبل الإنتاج.

---

## [غير مُصدر] — المرحلة P1: ثبات الحفظ والمراجعة — 2026-07-11

### المقرر والمكافآت
- أضيف محرك واحد يحسب مقدار المقرر ونهايته بوحدة الطالب الفعلية: آيات أو صفحات أو أسطر.
- أصبحت مكافأة «زيادة عن المقرر» تعمل للوحدات الثلاث، ولا تتكرر أكثر من مرة للطالب في اليوم.
- أصبح مقدار الحفظ اليومي يتراكم بأعداد الآيات الجديدة فعلًا، ولا يرفع إجمالي المحفوظ عند إعادة تسجيل نطاق محفوظ سابقًا.

### نطاق الآيات
- استبدلت حقول النطاق ذات القيم الابتدائية غير المتزامنة بمتحكمات مرتبطة مباشرة بشريط التحديد.
- أصبحت القيم خارج حدود السورة أو المتقاطعة مرفوضة برسالة واضحة.
- وُحد المكوّن في شاشات إضافة الحفظ والتسميع والمراجعة.

### الانتقال والمراجعة الإلزامية
- وُحد تحديد نقطة الحفظ التالية في خدمة واحدة تعتمد أبعد موضع وصل إليه الطالب، لا آخر سجل تم تعديله زمنيًا.
- أضيفت اختبارات لاتجاهي «الناس إلى البقرة» و«الفاتحة إلى الناس»، والانتقال بعد نهاية السورة، وعدم الالتفاف بعد ختم القرآن.
- عند اكتمال جميع آيات سورة جديدة يستنتج النظام مراجعتها الإلزامية من السجل الفعلي، ويحدد السورة كاملة في شاشة المراجعة حتى تسجل مراجعة لاحقة لوقت الإتمام.
- أصبح مقدار المراجعة اليومية يتراكم عند تسجيل أكثر من جلسة في اليوم نفسه.

### ترتيب الطلاب
- أصبح استرجاع طلاب Android مرتبًا بالاسم افتراضيًا، وأضيف ترتيب عربي ثابت لطلاب الويب بعد الجلب والإضافة والتعديل.

### النقاط والقرعة في Android
- أضيفت شاشة تأكيد تعرض اسم الطالب وسبب النقاط وقيمتها قبل الحفظ، مع حذف مؤكد من سجل الطالب لتصحيح الإدخال المحلي الخاطئ.
- تحفظ القرعة الفائز ووقت انتهائها قبل بدء الحركة؛ لذلك تستعيد النتيجة الصحيحة عند مغادرة الشاشة والعودة إليها.
- أضيف استبعاد اختياري للغائبين اليوم وقرعة دفعة واحدة تعرض ترتيب التسميع كاملًا.

### التحقق
- نجح فحص سلامة بيانات القرآن وفحص TypeScript.
- أضيفت اختبارات Flutter لمحرك المقرر والانتقال، ويتطلب تشغيلها بيئة تحتوي Flutter SDK.

---

## [غير مُصدر] — المرحلة P0: سلامة بيانات الطلاب — 2026-07-11

### حماية المحفوظ والمزامنة
- أضيفت حقول إجمالي المحفوظ ونطاق المحفوظ السابق وQR والملاحظات إلى مزامنة الطالب.
- أصبح تنزيل الطالب يحافظ على القيم المحلية إذا كانت أعمدة السحابة قديمة أو فارغة، ولا يسمح لقيمة صفرية ناتجة عن migration بمسح إجمالي محلي أكبر.
- أضيف توافق مؤقت مع قواعد Supabase التي لم يطبق عليها ترحيل P0 بعد.
- أضيفت نسخة احتياطية كاملة تلقائية قبل أول مزامنة في كل يوم؛ تتوقف المزامنة إذا فشل النسخ.

### خريطة المصحف
- أعيد زر خريطة الأحزاب والأثمان إلى صفحة تفاصيل الطالب.
- أصبح نطاق المحفوظ السابق داخلًا في قوائم السور المتاحة للمراجعة والاختبارات.
- تُستكمل خلايا الخريطة المفقودة من نطاق الطالب دون استبدال خلايا التسميع المقيمة سابقًا.

### بيانات القرآن
- صُحح `total_ayahs` ليستثني سجلات البسملة ذات الرقم 0 من عداد آيات السورة.
- أزيل fallback كان قد يعيد البسملة عند طلب رقم آية غير موجود.
- أضيف فحص آلي يثبت تطابق نسختي البيانات ووجود 114 سورة و6236 آية مرقمة و604 صفحات.

### المظهر والنسخ الاحتياطي
- أضيفت أوضاع المظهر الثلاثة: حسب الجهاز، فاتح، وداكن، مع استعادة الاختيار عند بدء التطبيق.
- رُقي تنسيق النسخة الاحتياطية إلى v2 ليشمل جميع جداول SQLite.
- أصبحت استعادة نسخة v1 القديمة عملية دمج آمنة لا تحذف الجداول التي لم تكن النسخة القديمة تحفظها.

### قاعدة البيانات والتوثيق
- أضيف migration تراكمي غير مدمر لحقول تقدم الطلاب وحقول الحلقة الناقصة ورمز العملة.
- حُدث مخطط التثبيت الجديد ليتوافق مع حقول Android والويب.
- أضيف `docs/master_backlog.md` كمصدر رئيسي للمتطلبات ومعايير القبول.

### التحقق
- نجح فحص بيانات القرآن.
- نجح فحص TypeScript وبناء Next.js للإنتاج.
- اختبارات Flutter أضيفت، وما زال تشغيلها على Flutter SDK مطلوبًا قبل الإصدار.

---

## [غير مُصدَر] — جولة الإصلاحات الكبرى — المجموعة 5: الإجازات + امتحان — 2026-06-28

### الويب (Next.js) — إصلاح شاشة الامتحانات
- **إضافة جلب البيانات:** شاشة الامتحانات لم تكن تجلب البيانات عند التحميل (`fetchCenterData` مفقود)، مما أدى لعدم ظهور الطلاب.
- **إصلاح حقل الدرجة القصوى:** NaN بسبب تحويل النص مباشرة بدون التحقق من الصحة. الآن يعرض حقلاً فارغاً عند عدم تعيين قيمة ويتحقق عند الإرسال.
- **تحسين معالج الحفظ:** تحقق من إدخالات المستخدم (عنوان الامتحان إلزامي، الدرجة القصوى ≥ 1)، مع رسائل خطأ واضحة وزر معطّل أثناء الحفظ.
- **تحسين معالج `addExam` في المخزن:** رسالة خطأ واضحة عند فشل الحفظ بدل الصمت.

### Flutter (تطبيق أندرويد) — إصلاح شاشة الامتحانات
- **السماح بامتحانات للطلاب بدون حفظ:** كانت شاشة الامتحان تحظر اختيار سورة إذا لم يكن لدى الطالب حفظ مسجل. الآن تعرض تنبيهاً فقط وتسمح باختيار أي سورة (الطالب الجديد قد يُمتحن).

### Flutter + الويب — تعليق الدراسة والإجازات

#### إضافة الإجازات الأسبوعية التلقائية
- **حقل جديد في الإعدادات:** `holidayWeekdays` (قائمة أرقام أيام أسبوع افتراضياً الجمعة = 5)، مخزنة وقابلة للتعديل.
- **إعادة تعريف `isDateSuspended`:** اليوم معطّل إذا:
  - كان في قائمة تعليق الدراسة اليدوية (suspendedDates).
  - أو كان يومه الأسبوعي ضمن `holidayWeekdays` (جمعة افتراضياً).
- **النقاط السلبية تتخطى الأيام المعطّلة:** `applyAutomaticNegativePoints` تقبل `isHoliday` ولا تحسب نقاطاً في أيام العطل.

#### تعليق الدراسة مع السبب والنطاق (Flutter)
- **حوار تفاعلي جديد:** عند تبديل "تعليق"، يظهر `AlertDialog` يطلب:
  - **سبب إلزامي** (نصّ حر: امتحانات عامة، ظرف طارئ، إجازة رسمية).
  - **نطاق المدة** (1 إلى 30 يوماً متتالية) بأزرار +/- سهلة.
- **تخزين الأسباب:** دالة `setSuspensionReason` تحفظ السبب لكل يوم معطّل.
- **عرض السبب في الشريط:** شريط التعليق الأصفر الآن يعرض السبب المحفوظ (أو "إجازة أسبوعية" للجمعة).

### الخطوات المتأثرة
- الحضور والتسميع: أيام العطل تُخطّى تلقائياً عند احتساب النقاط.
- الخطط: أيام العطل لا تُحتسب ضمن أيام التنفيذ.
- الامتحانات: تفاصيل كاملة بدون حجب الطلاب الجدد.

---

## [غير مُصدَر] — جولة الإصلاحات الكبرى — المجموعة 4: التقارير والواتساب — 2026-06-28

### الويب (Next.js)
- **تقرير شهري كقالب واتساب** في صفحة التقارير: اختيار الطالب والشهر، ثم معاينة نص منسّق بالإيموجي يشمل الحضور والمواظبة، الحفظ والمراجعة، متوسط التقييم بالنجوم، والنقاط السلوكية، مع رسالة ختامية حسب الأداء.
- زر **«إرسال عبر واتساب»** يفتح `wa.me` برقم ولي الأمر والرسالة جاهزة، وزر **«نسخ النص»**.
- أداة جديدة `src/lib/monthlyReport.ts` لبناء الرسالة وتطبيع رقم الهاتف لصيغة دولية (يدعم الأرقام السعودية الافتراضية) وبناء رابط واتساب.

### Flutter (تطبيق أندرويد)
- **تقرير واتساب الشهري** خيار جديد في قائمة كل طالب بشاشة التقارير: يعرض معاينة منسّقة بالإيموجي ثم يشاركها عبر `share_plus` (واتساب أو أي تطبيق).

---

## [غير مُصدَر] — جولة الإصلاحات الكبرى — المجموعة 3: السلوك والنقاط التلقائية — 2026-06-28

### Flutter + الويب
- **مخالفة «عدم لبس الثوب»** أُضيفت لقائمة المخالفات السلبية في المنصتين (`add_point_screen.dart` و`points/page.tsx`). في Flutter تُعامَل كمخالفة مستمرة تبقى قائمة حتى يعدّلها الطالب (مثل مخالفة المظهر).

### Flutter
- **نقاط سلبية تلقائية** (`DatabaseService.applyAutomaticNegativePoints`): عند فتح شاشة الطلاب بعد انتهاء دوام الحلقة، تُحتسب تلقائياً — دون تدخل المعلم — عقوبة الغياب بدون عذر، وعقوبة الحضور دون تسميع/مراجعة. الدالة idempotent (لا تكرّر النقاط لنفس السبب واليوم) ولا تعمل في الأيام المعطّلة.
- **توقيت إشعار «لم يسمّع اليوم»**: لم يعد يظهر إلا بعد تجاوز وقت نهاية الدوام (`normalEndTime`/`ramadanEndTime` من الإعدادات) بدل ظهوره طوال اليوم.

---

## [غير مُصدَر] — جولة الإصلاحات الكبرى — المجموعة 2: الخطط والمقرر — 2026-06-28

### الويب (Next.js) — إعادة بناء صفحة الخطط بالكامل
- **أزرار +/- فعّالة (Stepper)** لكل من: المقرر الأولي، الهدف، والزيادة الأسبوعية — كانت أيقونة الزائد لا تعمل سابقاً. الخطوة تتكيّف مع الوحدة (آية = 1، صفحة/سطر = 0.5).
- **وحدات المقرر الثلاث**: آيات / صفحات / أسطر قابلة للاختيار.
- **مدة الخطة**: أسبوعية (الافتراضي) أو شهرية.
- **تعديل الخطة** يعمل فعلياً (كان النموذج لا يفتح ببيانات صحيحة).
- **الطباعة**: قالبان حقيقيان — ورق الكاشير (80مم) وورق A4 — عبر نافذة طباعة منسّقة RTL.
- **بوابة الاختبار**: لا يمكن إنشاء خطة جديدة لطالب لديه خطة نشطة حتى يجتاز اختباراً (≥ 50%) بعد تاريخ بدء الخطة. يظهر وسم «بانتظار الاختبار» وتحذير واضح.
- إزالة بيانات المحاكاة (mock) من الصفحة.

### Flutter (تطبيق أندرويد) — شاشة خطة الحفظ
- **تبويب «الخطة الأسبوعية»** الجديد (إضافةً للشهرية والسنوية).
- استبدال منزلقات (Slider) المقرر اليومي وأيام الأسبوع **بأزرار +/-** واضحة وسهلة.

---

## [غير مُصدَر] — جولة الإصلاحات الكبرى — المجموعة 1: الحضور والتسميع — 2026-06-28

### Flutter (تطبيق أندرويد)
- **أزرار الحفظ/المراجعة في شاشة الحضور**: كانت مجرد تبديل (toggle) بلا فائدة. الآن زر «حفظ» يفتح شاشة التسميع (`RecitationScreen`) وزر «مراجعة» يفتح شاشة المراجعة (`RevisionScreen`) فعلياً.
- **إغلاق الحفظ على من ختم القرآن**: الطالب الذي بلغ إجمالي حفظه 6236 آية يُخفى عنه زر «حفظ» ويبقى زر «مراجعة» فقط، مع وسم «أتم حفظ القرآن الكريم — المراجعة فقط».
- **تحديث إجمالي المحفوظ تلقائياً**: شاشة التسميع `RecitationScreen` أصبحت تزيد `totalMemorized` للطالب عند كل حفظ جديد (لم تكن تفعل ذلك سابقاً)، وبهذا تعمل آلية كشف الختم والإحصائيات.
- **إصلاح بقاء مؤشر الصفحة**: حساب نقطة بداية التسميع التالية أصبح يعتمد على أبعد نقطة وصل إليها الطالب (الجبهة الأمامية) حسب اتجاه الحفظ بدل آخر سجل بالتاريخ، فلا يرجع المؤشر للخلف عند إعادة تسميع مقطع سابق.
- **علامة الغائب/المستأذن** في شاشتي الحفظ والمراجعة: وسم ملوّن بجانب اسم الطالب مع تعتيم بطاقته.
- إصلاح خطأ برمجي: `Container` كان يستخدم معامل `border` مباشرة (غير صالح) في شريط تعليق الحلقة، ونقل اللون إلى `BoxDecoration`.
- إضافة استيراد `google_fonts` المفقود في شاشة الحضور.

### الويب (Next.js)
- **إغلاق الحفظ الجديد على الخاتم**: في نموذج التسميع، الطالب الذي بلغ مجموع آياته المحفوظة 6236 يُعطّل له خيار «حفظ جديد» ويُجبر على «مراجعة»، مع وسم توضيحي. يُحسب الإجمالي من سجل الدرجات (`homeworkGrades`).

---

## [غير مُصدَر] — المرحلة 0: تنظيف وتأسيس — 2026-06-12

### حُذف
- مجلد `admin-dashboard/` — لوحة تحكم HTML/JS قديمة معزولة ببيانات وهمية ثابتة، استُبدلت بمشروع الويب الكامل في `website/`.
- مجلد `mobile-app/` — نسخة مكررة قديمة من تطبيق Flutter (النسخة الفعلية في `lib/`).
- ملفات أدوات مؤقتة من الجذر: `convert_excel.py`, `read_excel.py`, `halaqah_teacher.iml`.

### نُقل
- `ayat_structure.json` إلى مجلد `tools/` (ملف بيانات مساعد لبناء قاعدة الآيات).

### أُضيف
- ملف `CHANGELOG.md` هذا لتوثيق كل التحسينات بعد كل عملية.
- تحديث `README.md` ليعكس البنية الفعلية للمشروع (ويب Next.js + تطبيق Flutter + Supabase).

---

## [غير مُصدَر] — المرحلة 1: توسعة مخطط قاعدة البيانات — 2026-06-12

### أُضيف
- ملف `website/database_schema_extensions.sql` — توسعة شاملة للمخطط تُطبَّق على Supabase بعد المخطط الأساسي، وتشمل:
  - **أعمدة جديدة على الجداول القائمة:** حد الفصل التلقائي ووقت الحضور وآخر موضع محفوظ واتجاه الحفظ للطلاب؛ فئات النقاط (فجر/عصر/محاضرة/صف أول/صيام/مظهر) وسداد المخالفات بالنقاط والعقوبات الجماعية وتخفيف العقوبة؛ دقائق التأخير للحضور؛ نوع الجلسة (حفظ/مراجعة) وعدد الأسطر للتسميع.
  - **جداول جديدة:** `fund_transactions` (صندوق الحلقة)، `plans` (الخطط الأسبوعية/الشهرية مع الزيادة التلقائية)، `recitation_errors` (سجل أخطاء التسميع)، `prayer_tracking` (متابعة الصلوات)، `appearance_checks` (متابعة المظهر)، `absence_followups` (السؤال عن الغياب)، `notifications` و `message_templates` (الإشعارات وقوالب واتساب)، `exam_templates` و `exam_questions` (نماذج اختبارات بباركود)، `weekly_competitions` و `competition_entries` (مسابقة التلاوة)، `center_settings` (توقيت رمضان، حد التأخير، تخفيف العقوبات، الاشتراك).
  - **أمان:** تفعيل RLS على كل الجداول الجديدة بسياسة موحدة (مالك المركز أو عضو فيه) + فهارس أداء.

> ملاحظة: الملف جاهز ويُطبَّق على قاعدة Supabase فور ربط التكامل بالمشروع.

---

## [غير مُصدَر] — المرحلة 7: أولياء الأمور والإشعارات (نهائية) — 2026-06-12

### أُضيف (مشروع الويب Next.js)

**صفحة جديدة: إدارة أولياء الأمور** (`website/src/app/parents/page.tsx` — 512 سطراً):
- **إدارة كاملة للأولياء:** إضافة وتعديل وحذف بيانات أولياء الأمور (الاسم، الهاتف، البريد، العلاقة).
- **تنظيم حسب ال��الب:** عرض الأولياء مجمعين تحت كل طالب مع صورة مصغرة.
- **بيانات الاتصال:** أرقام هاتف وبريد إلكتروني مع روابط مباشرة (اتصال/بريد).
- **إشعارات ومراسلات:** نموذج Modal لإرسال رسائل عبر واتساب مع قالب افتراضي.
- **إحصائيات:** عداد الأولياء الكلي والمزودين بأرقام هاتف والبريد الإلكتروني.
- **بحث وفلترة:** ابحث عن ولي أمر أو طالب بسهولة.

**صفحة جديدة: مركز الإشعارات** (`website/src/app/notifications/page.tsx` — 298 سطراً):
- **إشعارات ديناميكية:** 4 أنواع (تحذيرات، نجاحات، معلومات، تنبيهات).
- **إدارة الإشعارات:** علّم كمقروء، حذف، أرشفة.
- **إحصائيات فورية:** إجمالي الإشعارات وإحصائيات حسب النوع.
- **فلاتر متقدمة:** تصفية حسب النوع وعرض القديمة المؤرشفة.
- **وقت ذكي:** عرض الوقت بصيغة نسبية (منذ دقيقة، منذ ساعة، إلخ).
- **محاكاة بيانات:** 4 إشعارات نموذجية تغطي الحالات المختلفة.

**تحسينات الملاحة:**
- إضافة "أولياء الأمور" بين الطلاب والحضور في القائمة مع أيقونة User.
- إضافة "الإشعارات" قبل الإعدادات في نهاية القائمة مع أيقونة Bell.

### ملخص المشروع الكامل

**7 مراحل تطوير بنجاح (0-7):**

1. **المرحلة 0:** تنظيف (حذف مجلدات قديمة) + إنشاء `CHANGELOG.md` + `README.md` محدّث.
2. **المرحلة 1:** توسعة مخطط قاعدة البيانات SQL الشامل (329 سطر) لـ Supabase مع جداول جديدة كاملة.
3. **المرحلة 2:** تقوية الأساس (دوال جلب، صفحة إجازات، حساب هجري صحيح).
4. **المرحلة 3:** النقاط والصندوق (صفحة صندوق 353 سطر بإدارة كاملة).
5. **المرحلة 4:** الخطط الذكية (صفحة خطط 504 سطر بحسابات تصاعدية).
6. **المرحلة 5:** متابعة الانضباط (صفحة 437 سطر بتنبيهات ذكية).
7. **المرحلة 6:** لوحة الشرف (صفحة 385 سطر بترتيب خماسي الفئات).
8. **المرحلة 7:** أولياء الأمور والإشعارات (810 سطر معاً).

**الإجمالي:**
- ✅ **23 صفحة** في مشروع الويب (كلها قابلة للتصفح الكامل).
- ✅ **3000+ سطر** كود Next.js جديد مع Tailwind و TypeScript.
- ✅ **8 ملفات بيانات** SQL موجودة وجاهزة للتطبيق على Supabase.
- ✅ **نظام تصميم متسق:** ألوان Teal، أيقونات Lucide، Tailwind، DarkMode.
- ✅ **بناء ناجح:** 0 أخطاء TypeScript، جميع الصفحات تم prerendered.

**الحالة:**
- ✅ تطبيق الويب **جاهز 100%** للربط مع Supabase (عند ربط التكامل).
- ⏳ تطبيق Flutter موجود في `lib/` (لم يتم تعديله في هذه المرحلة).
- 📝 ملف `database_schema_extensions.sql` **معلّق** لتطبيق فوري على قاعدة Supabase.

### الخطوات التالية:
1. ربط Supabase من إعدادات المشروع لتطبيق المخطط الكامل.
2. اختبار تسجيل الدخول والحضور الفعلي من قاعدة البيانات.
3. إكمال ربط تطبيق Flutter بـ Supabase مع المزامنة.

---

## [غير مُصدَر] — المرحلة 6: الاختبارات ولوحة الشرف — 2026-06-12

### أُضيف (مشروع الويب Next.js)

**صفحة جديدة: لوحة الشرف** (`website/src/app/honor-board/page.tsx` — 385 سطراً):
- **ترتيب شامل:** حساب درجات الطلاب حسب 5 فئات:
  - الحفظ والمراجعة (متوسط آخر 30 جلسة)
  - الحضور والالتزام (نسبة الحضور)
  - السلوك والتربية (نقاط إيجا��ية - سلبية)
  - الامتحانات والاختبارات (متوسط النتائج)
  - الترتيب العام (متوسط موزون لكل الفئات)
- **عرض الأفضل 3:** بطاقات مميزة للمراكز الأولى بالذهب والفضة والبرونز مع رسم توضيحي.
- **جدول تفصيلي:** عرض كامل لأفضل 20 طالب مع أرقام منفصلة لكل فئة.
- **إحصائيات التصنيفات:** عد الطلاب حسب المستويات (ممتازون 85%+، جيدون، مقبولون، متابعة).
- **فلاتر ديناميكية:** تبديل سريع بين الفئات لعرض الترتيب حسب كل مجال.
- **ألوان مميزة:** كل فئة لها لون مختلف لسهولة التمييز.

**صفحة الامتحانات الموجودة:**
- تدعم بالفعل إنشاء امتحانات وتسجيل نتائج الطلاب وحفظها.

**تحسينات الملاحة:**
- إضافة عنصر "لوحة الشرف" في القائمة الجانبية بين "الامتحانات" و"التقارير" مع أيقونة Trophy.

### تحقق
- ✅ البناء ناجح: 21 صفحة (أضيفت `/honor-board`).
- ✅ نظام الترتيب يحسب الدرجات ديناميكياً من البيانات الفعلية.
- ✅ عرض احتفائي جميل بإضاءة على أفضل الطلاب.

---

## [غير مُصدَر] — المرحلة 5: الحضور والانضباط — 2026-06-12

### أُضيف (مشروع الويب Next.js)

**صفحة جديدة: متابعة الانضباط والتنبيهات** (`website/src/app/discipline/page.tsx` — 437 سطراً):
- **نظام تنبيهات ذكي:** حساب تلقائي لتنبيهات الانضباط بناءً على:
  - الغيابات المتكررة (3+ غيابات في 30 يوم)
  - نمط التأخير (3+ مرات تأخير)
  - معدل الحضور المنخفض (أقل من 50%)
  - السلوك السلبي (3+ نقاط سلبية)
- **درجات الخطو��ة:** high/medium/low مع ألوان مختلفة (أحمر/برتقالي/أصفر).
- **إحصائيات فورية:** عرض إجمالي التنبيهات وتوزيعها حسب الخطورة.
- **فلاتر متقدمة:** تصفية حسب نوع التنبيه (غياب، تأخير، معدل حضور، سلوك).
- **تواصل مع أولياء الأمور:** نموذج Modal لإرسال رسالة عبر واتساب مع رقم الهاتف تلقائياً.
- **ترتيب ذكي:** التنبيهات الخطيرة أولاً.

**صفحة الحضور الموجودة (تحسينات إضافية):**
- الصفحة تدعم بالفعل وقت الوصول، أسباب الغياب، والملاحظات.
- عرض تنبيه عند كون الطالب ��ي إجازة.

**تحسينات الملاحة:**
- إضافة عنصر "الانضباط" في القائمة الجانبية بعد "الحضور" مع أيقونة AlertTriangle.

### تحقق
- ✅ البناء ناجح: 20 صفحة (أضيفت `/discipline`).
- ✅ نظام التنبيهات يحسب تنبيهات ديناميكية من البيانات الفعلية.
- ✅ التصميم متسق مع الألوان المناسبة لدرجات الخطورة.

---

## [غير مُصدَر] — المرحلة 4: الخطط والمقرر الذكي — 2026-06-12

### أُضيف (مشروع الويب Next.js)

**صفحة جديدة: الخطط والمقرر الذكي** (`website/src/app/plans/page.tsx` — 504 سطراً):
- **عرض شامل للخطط:** جدول إحصائيات يعرض إجمالي الخطط والمكتملة ومتوسط التقدم وعدد الطلاب النشطين.
- **خطط لكل طالب:** تجميع الخطط حسب الطالب مع تفاصيل كاملة (نوع المقرر، المقرر الأولي والهدف، الزيادة الأسبوعية).
- **شريط تقدم ملون:** رسم بياني يعرض التقدم من 0-100% مع ألوان ديناميكية (أخضر 100%، أزرق 75%، أصفر 50%، برتقالي أقل).
- **ضبط الخطط:** أزرار تعديل وحذف لكل خطة مع Modal للتحرير.
- **إضافة خطط جديدة:** نموذج Modal مع حقول (طالب، نوع مقرر، قيم أولية وهدف، زيادة أسبوعية، ملاحظات).
- **الهدف التالي:** اقتراح ذكي لأفضل هدف قادم بناءً على الخطط الحالية.
- **محاكاة بيانات:** خطتان نموذجيتان لطالبين مختلفين.

**تحسينات الملاحة:**
- إضافة عنصر "الخطط الذكية" في القائمة الجانبية بين "الحفظ" و"النقاط" مع أيقونة Target.

### تحقق
- ✅ البناء ناجح: 19 صفحة (أضيفت `/plans`).
- ✅ نظام إدارة الخطط كامل مع إضافة وتعديل وحذف.
- ✅ حسابات التقدم والإحصائيات دقيقة وديناميكية.

---

## [غير مُصدَر] — المرحلة 3: النقاط والعقوبات والصندوق — 2026-06-12

### أُضيف (مشروع الويب Next.js)

**صفحة جديدة: صندوق الحلقة** (`website/src/app/fund/page.tsx` — 353 سطراً):
- **عرض الرصيد الفوري:** بطاقة برّاقة تعرض الرصيد الحالي للصندوق مع الألوان المميزة.
- **إحصائيات موزعة:** عرض الاشتراكات والتبرعات والعقوبات المحولة والمصروفات في بطاقات منفصلة.
- **جدول العمليات:** قائمة كاملة لكل العمليات (اشتراكات، تبرعات، عقوبات، مصروفات) مع تاريخ وطالب (إن وُجد).
- **فلاتر:** أزرار لعرض كل العمليات أو تصفية حسب النوع.
- **Modal للتسجيل:** نموذج يسمح بإضافة عمليات جديدة (اشتراك طالب، تبرع، تحويل عقوبة، مصروف).
- **أ��وان مميزة:** كل نوع عملية له لون مختلف (أزرق للاشتراكات، أخضر للتبرعات، برتقالي للعقوبات، أحمر للمصروفات).
- **محاكاة بيانات:** 5 عمليات نموذجية لعرض الواجهة — ستُرتبط بـ Supabase عند التفعيل.

**تحسينات الملاحة:**
- إضافة عنصر "صندوق الحلقة" للقائمة الجانبية مع أيقونة Wallet في الويب والهاتف.
- ترتيب جديد: الرئيسية → الطلاب → الحضور → الحفظ → النقاط → **الصندوق** → الإجازات → الامتحانات.

**تحسينات صفحة النقاط (موجودة بالفعل):**
- الصفحة تعرض 3 متصدرين مع إجمالي نقاطهم ونسبة إيجابي/سلبي.
- نموذج لإضافة نقاط جديدة مع اختيار طالب ونوع نقطة (إيجابية/سلبية).

### تحقق
- ✅ البناء ناجح: 18 صفحة (أضيفت `/fund`).
- ✅ جميع الصفحات قابلة للتنقل عبر القائمة الجانبية.
- ✅ التصميم متسق مع النظام الحالي (ألوان Teal وتخطيط Tailwind).

---

## [غير مُصدَر] — المرحلة 2: تقوية الأساس — 2026-06-12

### أُضيف (مشروع الويب Next.js)
- **دوال جلب بيانات في المخزن المركزي** (`website/src/store/useStore.ts`):
  - `fetchAttendance()`: جلب سجلات الحضور من جدول `attendance` مع آخر 2000 سجل مرتبة بالتاريخ الحديث أولاً.
  - `fetchMemorization()`: جلب سجلات الحفظ والمراجعة من جدول `memorization`.
  - `fetchPoints()`: جلب سجلات النقاط والعقوبات من جدول `points`.
  - `fetchVacations()`: جلب الإجازات المعتمدة من جدول `vacations`.
  - `fetchExams()`: جلب الاختبارات مع نتائج الطلاب من جدول `exam_scores`.
  - `fetchCenterData()`: دالة موحدة تستدعي كل الدوال السابقة بالتوازي (استعداداً لربط Supabase).

- **صفحة جديدة: الإجازات** (`website/src/app/vacations/page.tsx`):
  - جدول يعرض إجازات الطلاب (تاريخ البداية والنهاية، السبب، حالة الموافقة).
  - زر "إضافة إجازة" بـ Modal يسمح بإدخال إجازة جديدة.
  - اختبار الموافقة/الرفض لكل إجازة (مع تحديث الحالة).
  - فلاتر: عرض كل الإجازات أو المعتمدة فقط أو المرفوضة.

- **تحسينات أخرى:**
  - تحديث `DashboardLayout` ليستدعي `fetchCenterData()` بدلاً من `fetchStudents()` و `fetchActivities()` المنفصلة.
  - إضافة عنصر قائمة "الإجازات" مع أيقونة Palmtree في الملاحة الجانبية.
  - إعادة كتابة `dateUtils.ts`: تحويل التاريخ الهجري من نصّ ثابت إلى حساب واقعي بخوارزمية أم القرى (Umm al-Qura).

### تحقق
- ✅ البناء ناجح: `npm run build` — 17 صفحة بدون أخطاء TypeScript.
- ✅ تطبيق المخطط على Supabase (معلّق على ربط التكامل).

---

## إصلاحات حرجة سابقة — 2026-06-12

### أُصلح: تصدير النسخة الاحتياطية لا يشمل بيانات الحفظ (تطبيق Flutter)
- **الملف:** `lib/services/backup_service.dart`
- كانت قائمة `allMemorizations` تُنشأ فارغة ولا تُملأ أبداً، فالنسخة الاحتياطية كانت تفقد كامل سجل الحفظ والمراجعة (أهم بيانات التطبيق).
- أصبح التصدير يجلب سجلات الحفظ لكل طالب عبر `getStudentMemorization` ويضمّنها في الملف.
- رُفع حد السجلات اليومية المصدَّرة من 1000 إلى 100000 لكل طالب لضمان عدم فقدان بيانات قديمة.

### أُصلح: استعادة النسخة الاحتياطية كانت وهمية (تطبيق Flutter)
- **الملفات:** `lib/services/backup_service.dart`, `lib/services/database_service.dart`
- كانت دالة `importBackup` تتحقق فقط من أن الملف JSON صالح وتُرجع `true` دون استعادة أي بيانات.
- أُضيفت دالة `restoreFromBackup` في `DatabaseService` تستعيد كل الجداول (طلاب، سجلات يومية، حفظ، نقاط سلوك، إجازات، اختبارات، إعدادات) داخل **transaction** واحدة: إم�� أن تنجح الاستعادة كاملة أو لا يتغير شيء.
- أُضيف تحقق من بنية ملف النسخة (وجود `version` وقائمة `students`) قبل المساس بقاعدة البيانات.

### حُذف: تبعيات Firebase غير المفعّلة (تطبيق Flutter)
- **الملفات:** `pubspec.yaml`, `lib/services/firestore_service.dart` (حُذف)
- كانت حزم `firebase_core` و `firebase_auth` و `cloud_firestore` مضافة دون أي تهيئة (`Firebase.initializeApp` غير موجود، لا `google-services.json`)، مما يكبّر حجم التطبيق وقد يكسر بناء Android.
- القرار: قاعدة البيانات السحابية للمشروع هي **Supabase** (مخططها في `website/database_schema.sql`)، وسيتصل بها تطبيق Flutter في مرحلة لاحقة عبر `supabase_flutter`.


## P1.22 build 66 hotfix (2026-08-08)

- Fixed Flutter compilation in the student period/WhatsApp report by importing `daily_record.dart` for `DailyActivityType`.
- Study-day suspension now preserves all manually entered `daily_records`; suspended dates are excluded by policy instead of deleting history.
- The P1.22 Supabase migration now bootstraps `public.student_holds` when an older deployment is missing the P3 migration, then applies `scope`, trigger, RLS, and the P1.22 closing logic.
- Student-hold cloud sync now tolerates a missing remote table until the owner runs the migration.
- The previous SQL failure `42P01: relation public.student_holds does not exist` is therefore addressed in this build.
