# P1.27 Build 81 Hotfix 6 — Install note

هذا التصحيح مخصص لبطء/توقف مرحلة رفع سجلات الحذف.

## لا يوجد SQL جديد

لا تعِد تشغيل migrations السابقة. أبقِ نتيجة Hotfix 3 VERIFY الناجحة كما هي.

## التثبيت

يفضل تطبيق حزمة modified-files فوق Build 80 العامل حتى تبقى ملفات الخطوط و`pubspec.lock` كما هي.

```powershell
PowerShell -ExecutionPolicy Bypass -File .\tools\verify_source_prerequisites.ps1
flutter clean
flutter pub get
flutter analyze
flutter test
flutter run
```

لا تشغل `flutter run` إذا فشل analyze أو test.

## ما تغير

- versionCode = 81.
- حذف الطالب يولد outbox واحدًا بدل cascade كامل.
- outbox ينظف REPLACE markers جماعيًا ويرفع UUID deletes بدفعات.
- المفاتيح المركبة تنفذ بتوازٍ محدود.
- يظهر عداد تقدم داخل مرحلة الحذف.
