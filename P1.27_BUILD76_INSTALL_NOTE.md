# P1.27 Build 76 — ملاحظة التثبيت

**الإصدار:** `4.3.0-alpha.22+76`  
**قاعدة SQLite المحلية:** `25`  
**قاعدة الانطلاق:** P1.27 Build 75 Hotfix 3

## لماذا Build 76؟

هذه الدفعة لا تضيف قائمة وعود جديدة؛ هي **جولة إكمال** للنقاط التي أثبت تدقيق Build 75 أنها كانت جزئية: الحذف ثنائي الطرف، التعمق الإشرافي، منافسة المجموعات، تشخيص ErrorWidget، تباين Light/Dark، الربط المنظم للاختبار الشهري، وسياسة تقريب النقاط.

## طريقة التثبيت الموصى بها

1. خذ نسخة احتياطية من مجلد المشروع ومن قاعدة Supabase.
2. استخدم حزمة `modified-files-only` فوق **Build 75 Hotfix 3** مع الاستبدال.
3. **احتفظ بملف `pubspec.lock` الموجود عندك.** Build 76 لا يغيّر إصدارات الاعتماديات التي ثبت نجاحها على جهازك؛ تغير فقط رقم التطبيق.
4. نفّذ SQL الجديد فقط:

```text
website/supabase/P1.27_BUILD76_APPLY.sql
```

هذا الملف **incremental** ويفترض أن Build 75 APPLY/VERIFY نجحا بالفعل. لا تعاود Build 75 ولا P7.3 ولا `database_schema.sql`.

5. بعد نجاح APPLY نفّذ:

```text
website/supabase/P1.27_BUILD76_VERIFY.sql
```

يجب أن تكون كل قيم `passed = true`.

6. على جهاز Flutter شغّل:

```powershell
flutter clean
flutter pub get
flutter analyze
flutter test
flutter run
```

بما أن الاعتماديات لم تتغير، لا يُفترض أن تحدث دورة خفض/رفع حزم مثل التي حدثت قبل Hotfix 3.

## ترقية SQLite

عند أول تشغيل، ينتقل المخطط تلقائيًا من 24 إلى 25. لا تستبدل ملف SQLite يدويًا. الإصدار 25 يضيف:

- `exams.template_id` لربط الاختبار بالقالب المنظم؛
- `sync_delete_outbox` كدفتر حذف محلي دائم؛
- triggers للحذف على الجداول المتزامنة، مع حراسة تمنع echo عند تطبيق حذف قادم من السحابة.

## اختبارات قبول مهمة بعد التثبيت

### 1. حذف بجهازين

- أضف سجل حفظ/حضور/نقطة على الجهاز A دون إنترنت ثم أعد الاتصال: يجب أن يصل للسحابة.
- احذف سجلًا من A، واترك B دون إنترنت، ثم افتح B بعد رجوع الاتصال: يجب أن يطبّق tombstone قبل الرفع وألا يعيد السجل.
- كرر على سجل `mushaf_progress` وطالب إن أمكن في بيانات اختبارية.

### 2. الإشراف

من حساب owner/admin:

`الجهة → المركز → تفاصيل الحلقات والطلاب`

وتأكد أن analyst يستطيع القراءة ولا يحصل على أدوات الإدارة.

### 3. التنافس

افتح **مجموعات المستوى المتقارب** واختر 2–10 مجموعات. يجب أن يبقى التجميع حسب المحفوظ الكلي، ويظهر ترتيب أسبوعي داخل كل مجموعة حسب الحفظ الجديد + ربع وزن المراجعة + النقاط السلوكية.

### 4. التباين

اختبر الحفظ والمراجعة وبيان الطالب والمسابقات في الوضعين Light/Dark، خصوصًا تبويبات AppBar والشرائح وأزرار الحالات.

### 5. الاختبار الشهري

أنشئ اختبار خطة شهرية ثم افتح نموذج الاختبار/الأسئلة على جهاز ثان بعد المزامنة؛ يجب أن تنتقل `exam_templates` و`exam_questions` مع ربط `template_id`.

## لا يوجد تغيير في الاعتماديات

احتفظ بالمجموعة التي نجحت على جهازك في Hotfix 3، ومنها:

- `supabase_flutter 2.14.2`
- `printing 5.14.3`
- `share_plus 10.0.3`
- `pdf 3.12.0`
- `path 1.9.1`

Build 76 لا يغير هذه القيود.

## Build 76 Hotfix 1 — 2026-08-15

إذا كانت نتيجة `P1.27_BUILD76_VERIFY.sql` لديك كلها `true` فلا تشغّل أي SQL إضافي لهذا التصحيح.

Hotfix 1 يصلح فقط أخطاء Flutter التي ظهرت على جهاز المالك في:

- `peer_level_groups_screen.dart` (`num` إلى `int` + callback غير صالح)،
- `memorization_screen.dart` (استيراد `AppSemanticColors`).

بعد نسخ ملفات Hotfix 1 شغّل:

```powershell
flutter clean
flutter pub get
flutter analyze
flutter test
flutter run
```

