# P1.27 Build 78 Hotfix 4 — Install Note

1. لا تشغّل أي SQL جديد لهذا Hotfix. نتيجة Hotfix 3 VERIFY الناجحة كافية.
2. طبّق ملفات Hotfix 4 فوق مشروع Build 78 Hotfix 3 الحالي مع الاحتفاظ بملفات الخطوط و`pubspec.lock` الموجودة عندك.
3. شغّل:

```powershell
PowerShell -ExecutionPolicy Bypass -File .\tools\verify_source_prerequisites.ps1
flutter clean
flutter pub get
flutter analyze
flutter test
flutter run
```

4. اختبر أولًا «رفع تغييرات الجهاز». ينبغي أن تمر مرحلة «قوالب الاختبارات» قبل «الاختبارات والدرجات».
5. إذا أردت مزامنة تلقائية ثنائية الاتجاه، اضبط عبارة حماية النسخ من الإعدادات. بدونها سيعمل الرفع التلقائي فقط لحماية الجهاز من تنزيل غير مسبوق بنسخة احتياطية.
