# P1.27 Build 87 Hotfix 12 — Installation note

1. خذ نسخة احتياطية من البيانات.
2. نفّذ `P1.27_BUILD87_HOTFIX12_APPLY.sql` في Supabase SQL Editor.
3. نفّذ `P1.27_BUILD87_HOTFIX12_VERIFY.sql` وتأكد أن كل `passed=true`.
4. أعد نشر Edge Function من جذر المشروع (يتطلب Supabase CLI وتسجيل الدخول):

```powershell
PowerShell -ExecutionPolicy Bypass -File .\tools\deploy_student_portal.ps1 `
  -ProjectRef mcckekgvwtqtpwtslwqf `
  -AllowedOrigin https://halaqah-flame.vercel.app
```

5. أعد نشر `website` على Vercel بعد رفع Build 87 إلى فرع الإنتاج.
6. طبّق ملفات Build 87 فوق Build 86 الحالي ثم شغّل:

```powershell
PowerShell -ExecutionPolicy Bypass -File .\tools\verify_source_prerequisites.ps1
flutter clean
flutter pub get
flutter analyze
flutter test
flutter run
```

ملاحظة: حزمة `flutter_slidable` أزيلت من Flutter في Build 87؛ ظهورها ضمن "no longer depended on" بعد `pub get` طبيعي.
