# Halaqah P1.27 Build 84 Hotfix 9 — installation note

Build: `4.3.0-alpha.27+84`  
SQLite: `28`

## Required order

1. Take a current local/cloud backup.
2. Run `P1.27_BUILD84_HOTFIX9_APPLY.sql` in Supabase SQL Editor.
3. Run `P1.27_BUILD84_HOTFIX9_VERIFY.sql`; every row must show `passed = true`.
4. In Supabase Authentication → URL Configuration keep the production Site URL and add the exact redirect `https://halaqah-flame.vercel.app/auth/callback` (the current `/select-center` entry is not a replacement for the callback).
5. In Vercel/website deployment set `NEXT_PUBLIC_APP_URL=https://halaqah-flame.vercel.app` and redeploy the website. Keep Google Client Secret/JSON private.
6. Overlay the modified-files ZIP on the currently working Build 83 source, preserving local font binaries and `pubspec.lock`.
7. Run prerequisite verification, `flutter clean`, `flutter pub get`, `flutter analyze`, `flutter test`, then `flutter run` only if analyze/tests are fully green.

## Performance validation

The first bidirectional sync after Build 84 is a cursor baseline and may pull existing rows. Immediately run a second bidirectional sync without changing data; unchanged domains should log `stage_skipped_no_delta`. Then change one attendance record and run a third sync; only attendance and dependency stages should do material cloud work.

## No historical migration replay

Do not rerun P7.3. Build 84 contains the narrow current supervision RPC repair and PostgREST schema-cache reload needed by the current website.
