# الحالة الحالية: P1.27 Build 78 — 2026-08-15

النسخة الحالية هي `4.3.0-alpha.24+78` فوق Build 77، وSQLite 26. راجع `P1.27_BUILD78_INSTALL_NOTE.md` و`docs/P1.27_BUILD78_COMPLETION.md`. **يوجد SQL جديد في Build 78:** نفّذ `P1.27_BUILD78_APPLY.sql` ثم `P1.27_BUILD78_VERIFY.sql`. dependency pins لا تتغير عن Build 77.

---

# الحالة السابقة: P1.27 Build 77 — 2026-08-15

النسخة الحالية هي `4.3.0-alpha.23+77` فوق Build 76 Hotfix 1. راجع `P1.27_BUILD77_INSTALL_NOTE.md` و`docs/P1.27_BUILD77_UX_REPORTS.md`. **لا يوجد SQL جديد في Build 77**، ولا تعاود Build 76 APPLY. SQLite يبقى 25 والاعتماديات المباشرة لا تتغير.

---

# الحالة السابقة: P1.27 Build 76 — 2026-08-14

الترقية الحالية موثقة في `P1.27_BUILD76_INSTALL_NOTE.md`. إذا كان مشروعك على Build 75 Hotfix 3 الذي نجح عندك، طبّق ملفات Build 76 ثم نفّذ **فقط** `P1.27_BUILD76_APPLY.sql` و`P1.27_BUILD76_VERIFY.sql`. احتفظ بـ`pubspec.lock`؛ لا توجد تغييرات اعتماديات في Build 76. SQLite يترقى تلقائيًا إلى 25.

---

# تنبيه Build 75 Hotfix 3 — 2026-08-13

إذا كنت على Build 75 / Hotfix 1 / Hotfix 2، استبدل ملفات Hotfix 3 ثم شغّل `flutter clean` و`flutter pub get`. هذا التصحيح يغيّر `share_plus` إلى `10.0.3` لحل تعارضه مع `printing 5.14.3`. **لا يوجد SQL جديد ولا تعِد تشغيل APPLY**؛ نتيجة VERIFY الحية أصبحت كلها true.

---

# P1.27 Build 75 — ملاحظة التثبيت

**الإصدار:** `4.3.0-alpha.21+75`  
**SQLite:** `24` (بلا ترقية مخطط محلي)  
**التاريخ:** 2026-08-12

> **Hotfix 1 — 2026-08-12:** إذا ظهر `flutter pub get` بخطأ أن `flutter_test` يثبت `path 1.9.1` بينما المشروع يطلب `path 1.8.3`، استخدم نسخة Hotfix 1 أو غيّر القيد إلى `path: 1.9.1` ثم أعد `flutter pub get`. لا تعاود SQL APPLY بسبب هذا التصحيح.

هذه الدفعة تُركب فوق **P1.26 Build 74** وتضم طلبات P1.27 كاملة: إنشاء المراكز من بوابة الجهة الإشرافية، النقاط النسبية، المحفوظ المستنتج، التاريخ الهجري/الميلادي، معالجة الإجازة المتأخرة، مزامنة حذف دائمة، التسجيل المتأخر ثلاثة أيام، مجموعات التنافس، الاختبار الشهري، وتحسينات التباين والتشخيص والهوية.

## المسار الموصى به

استخدم حزمة **modified-files-only** فوق مشروع Build 74 الحالي. حزمة المصدر الكاملة لا تحتوي ملفات خطوط Tajawal الثنائية عمدًا، لذلك الاستبدال الجزئي يحافظ على خطوط مشروعك وملفاتك المحلية.

1. خذ نسخة احتياطية من مجلد المشروع وقاعدة Supabase.
2. أغلق Flutter/Next.js وVS Code إن كان يحتفظ بملفات مقفلة.
3. فك حزمة الملفات المعدلة فوق جذر المشروع واسمح باستبدال الملفات المطابقة.
4. لا تحذف `assets/fonts/Tajawal-400.ttf` و`assets/fonts/Tajawal-700.ttf` من مشروعك الموجود.
5. نفّذ SQL Build 75 أدناه.
6. شغّل فحوص Flutter والويب ثم اختبر حساب جهة إشرافية على نسخة تجريبية قبل الإنتاج.

## SQL المطلوب

من Supabase SQL Editor، افتح الملف التالي وانسخ **محتواه كاملًا**:

```text
website/supabase/P1.27_BUILD75_APPLY.sql
```

هذا هو المسار الموحد للقاعدة الحالية. يبدأ تلقائيًا بإصلاح Build 74/P7.3 المحافِظ على البيانات، ثم يضيف عقود Build 75. لا تشغّل `website/database_schema.sql` على قاعدة موجودة، ولا تعاود P7.3 القديمة يدويًا.

بعد نجاح APPLY شغّل:

```text
website/supabase/P1.27_BUILD75_VERIFY.sql
```

النتيجة المطلوبة: **كل صف في عمود `passed` يساوي `true`**. إذا فشل صف، احتفظ بنتيجة الفحص؛ لا تحذف الجهة الإشرافية ولا تعاود إنشاء جداولها.

### ما يضيفه SQL

- `create_supervised_center` لإنشاء مركز تابع مباشرة بواسطة مالك الجهة أو الإداري.
- نسخة آمنة من `set_study_suspension` تتراجع فقط عن غياب/عقوبات/إغلاق اليوم التلقائي.
- نوع اختبار `monthly_plan`.
- `sync_tombstones` + triggers + `get_sync_tombstones` لضمان انتشار الحذف ومنع إحياء السجل من جهاز كان دون إنترنت.
- عقد `get_supervision_health` بإصدار Build 75.
- يتضمن أولًا إصلاح عضوية المالك/RLS/RPC والأسرة الذرية من Build 74 حتى تكون القاعدة الجزئية قابلة للمصالحة.

## فحص Flutter

على Windows:

```powershell
PowerShell -ExecutionPolicy Bypass -File .\tools\verify_source_prerequisites.ps1
flutter analyze
flutter test
flutter run
```

وعلى macOS/Linux:

```bash
./tools/verify_source_prerequisites.sh
flutter analyze
flutter test
flutter run
```

### فحص يدوي مهم على الهاتف

- جرّب الوضع الفاتح والداكن في الحفظ والمراجعة والتقييمات.
- جرّب تسميعًا بلا تقييم آية تفصيلي للتأكد من عدم ظهور ErrorWidget.
- جرّب إنشاء سجل لليوم السابق وثلاثة أيام سابقة، وتأكد أن اليوم الرابع القديم مرفوض.
- أنشئ إجازة عارضة على يوم أغلق تلقائيًا، وتأكد أن الغياب والنقاط التلقائية فقط حُذفت.
- افصل الإنترنت، أضف سجلًا، أعد الاتصال، ثم تأكد من وصوله للسحابة.
- احذف سجلًا من جهاز متصل، ثم أعد جهازًا آخر كان دون إنترنت، وتأكد من عدم عودة السجل.
- جرّب صورة/تقرير يولده التطبيق للمشاركة؛ اسم الملف يبدأ بـ`حلقتي_`.

> ملاحظة: اسم **لقطة شاشة خام يأخذها نظام الهاتف نفسه** خارج التطبيق تحدده Android/iOS ولا يستطيع التطبيق تغييره. Build 75 يضمن اسم `حلقتي` للصور/التقارير التي يولدها التطبيق ويشاركها من داخله، كما يوحد اسم المنتج في بيانات التطبيق.

## فحص الويب

```bash
cd website
npm ci
npm run quality:ci
```

أو استخدم `tools/setup_web.ps1` على Windows. افتح `/supervision` بحساب مالك جهة أو `admin` واختبر إنشاء مركز جديد، ثم افتحه بحساب `analyst` وتأكد أن المتابعة متاحة بينما الإدارة/الإنشاء ممنوعان حسب الصلاحية.

## حزمة الاستبدال الكاملة

إذا استعملت **full-source-upgrade** بدل الملفات المعدلة فقط، انسخ خطوط Tajawal من مشروعك الحالي قبل Flutter build. لا تحتاج إلى استبدال قاعدة SQLite يدويًا؛ schema version بقي 24.
