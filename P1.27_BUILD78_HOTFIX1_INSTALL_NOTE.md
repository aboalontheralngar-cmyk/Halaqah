# P1.27 Build 78 Hotfix 1 — التثبيت

هذا التصحيح يُركب **فوق Build 78 الحالي**.

## قاعدة البيانات

لا يوجد SQL جديد. نتيجة `P1.27_BUILD78_VERIFY.sql` التي كانت كلها `true` تبقى معتمدة. لا تعاود APPLY.

## Flutter

1. فك حزمة `modified-files-only` فوق جذر Build78 ووافق على الاستبدال.
2. لا تحذف `pubspec.lock`.
3. نفذ:

```powershell
flutter clean
flutter pub get
flutter analyze
flutter test
flutter run
```

## اختبار المزامنة

بعد فتح التطبيق:

1. جرّب «رفع فقط» على شبكة مستقرة.
2. جرّب المزامنة الثنائية.
3. أوقف الإنترنت أثناء المزامنة: يجب أن تنتهي برسالة مختصرة تحفظ البيانات محليًا، لا برسالة `ClientException` طويلة.
4. أعد الإنترنت ثم أعد المزامنة؛ الإجازات العارضة غير المتغيرة لا يعاد رفعها كلها كل مرة.
