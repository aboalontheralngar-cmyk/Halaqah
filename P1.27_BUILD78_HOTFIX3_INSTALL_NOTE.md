# P1.27 Build 78 Hotfix 3 — ملاحظة التثبيت

هذه دفعة إصلاح للمزامنة واستهلاك الموارد فوق `4.3.0-alpha.24+78`. لا تغيّر SQLite ولا dependency pins.

## 1) قاعدة Supabase أولًا

خذ نسخة احتياطية، ثم افتح SQL Editor ونفذ **محتوى** الملف التالي كاملًا:

```text
website/supabase/P1.27_BUILD78_HOTFIX3_APPLY.sql
```

إذا توقف بسبب duplicates فلا تحذف شيئًا عشوائيًا؛ صدّر الصفوف المشار إليها وحدد السجل الصحيح أولًا. بعد نجاح APPLY نفذ:

```text
website/supabase/P1.27_BUILD78_HOTFIX3_VERIFY.sql
```

لا تنتقل للتطبيق إلا عندما تكون كل صفوف `passed=true`.

## 2) تركيب ملفات Flutter

الأكثر أمانًا على مشروع المالك الذي نجح في البناء هو فك حزمة **modified-files-only** فوق المجلد الحالي، وبذلك يبقى `pubspec.lock` وملفا Tajawal الموجودان لديه.

ثم:

```powershell
PowerShell -ExecutionPolicy Bypass -File .\tools\verify_source_prerequisites.ps1
flutter clean
flutter pub get
flutter analyze
flutter test
flutter run
```

لا تشغّل `flutter pub upgrade` لهذا الإصلاح.

## 3) اختبار المزامنة

1. من الإعدادات اختر **رفع فقط**.
2. نافذة المزامنة ستعرض المرحلة الحالية بدل spinner ثابت.
3. إذا فشل الرفع، افتح مركز التشخيص وسجّل `مرحلة الفشل` و`رمز التشخيص`.
4. بعد نجاح الرفع، نفذ مزامنة ثنائية.
5. أخيرًا اختبر جهازين قبل اعتماد RC.

## ملاحظة الحزمة الكاملة

الحزمة الكاملة ذات اللاحقة `no-font-binaries` لا تحتوي ملفات Tajawal الثنائية، تمامًا مثل المصدر الذي أُرسل للمراجعة. استخدم الحزمة الجزئية فوق مشروعك العامل أو أعد الخطوط من نسختك المحلية قبل البناء.
