# P1.27 Build 86 Hotfix 11 installation

1. Back up the current local project and the already synchronized data.
2. No SQL is required for Build 86.
3. Apply the Build 86 modified-files package over Build 85.
4. If the old file `lib/screens/memorization/memorization_plan_screen.dart` still exists after an overlay install, delete it; the route/import no longer exists in Build 86.
5. Run `flutter clean`, `flutter pub get`, `flutter analyze`, and `flutter test`.
6. Continue to `flutter run` only when analyze reports no issues and all tests pass.
7. Test plan multi-selection (print/delete), Hijri picker on the smaller phone, aggregate report cards, and both physical swipe directions in the recitation list.

No dependency upgrade, SQLite migration, or Supabase migration is included.
