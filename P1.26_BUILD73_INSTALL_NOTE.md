# P1.26 build 73 — installation note

This archive is a **source upgrade** for the existing Halaqah project. Apply it over the current project tree (build 72 or the latest local tree) rather than extracting it into an empty directory.

## Preserve local font binaries

The delivery archive intentionally excludes font binary files. Keep the project's existing font files in place, including the Tajawal files already used by Flutter/PDF and the website font files. Do not delete the existing font directories when overlaying this source package.

## Flutter validation order

Run from the project root:

```powershell
flutter clean
flutter pub get
flutter analyze
```

Build 73 specifically fixes the seven `const_eval_method_invocation` errors from the owner's latest analyzer log. If `flutter analyze` is clean, continue with:

```powershell
flutter test
flutter run
```

Do not upgrade the 68 dependency versions as part of this hotfix; they are unrelated to the compile failure.

## Supabase order for the current confirmed schema

The guarded base bootstrap has already completed successfully. Do not run it again.

1. Back up Supabase.
2. Run `website/supabase/P1.26_POST_BOOTSTRAP_CORE.sql`.
3. Only if it succeeds, run `website/supabase/P1.26_POST_BOOTSTRAP_VERIFY.sql`.
4. The final verify recommendation must be `READY_FOR_APP_SYNC_TEST` before cloud-sync acceptance testing.

The core script is additive and does not target the unrelated operational tables that already exist in the same Supabase `public` schema.
