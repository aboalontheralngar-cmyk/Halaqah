# P1.26 build 73 validation

## Owner-reported Flutter failures addressed

The latest owner analyzer log contained exactly seven compile errors, all `const_eval_method_invocation`, at:

- `lib/screens/reports/reports_screen.dart:629`
- `lib/screens/reports/student_period_report_screen.dart:417`
- `lib/screens/settings/message_templates_screen.dart:191`
- `lib/screens/students/student_detail_screen.dart:365`
- `lib/screens/students/student_detail_screen.dart:1546`
- `lib/screens/vacations/vacations_screen.dart:526`
- `lib/screens/vacations/vacations_screen.dart:545`

All seven were caused by runtime `Theme.of(context)` lookups inside `const Text` expressions. Build 73 removes those seven invalid const qualifiers.

A source scanner across `lib/**/*.dart` reports **0 const constructors containing `Theme.of(...)`** and **0 missing relative Dart imports**.

## Contract validators

Passed in the editing environment:

- P1.22
- P1.23
- P1.24
- P1.25
- P1.26

The full validation chain passes through P1.20 and then stops at P1.21 because the source-only package does not contain the `typescript` npm package. This is an environment/dependency limitation of the source archive, not a Dart compile result.

## Supabase reconciliation

A structural parser over the owner-provided schema found:

- Halaqah base-scope problems: **0**
- Existing collisions with the Halaqah core tables to be created: **0**
- Current supplied public tables: **29**
- State safe for the guarded post-bootstrap core migration: **true**

`P1.26_POST_BOOTSTRAP_CORE.sql` is additive and transactional. Static SQL checks confirm no `DROP TABLE`, `TRUNCATE`, or `DELETE FROM` after comments are removed, and the P1.26 validator rejects mutation statements targeting the unrelated operational table names from the owner-provided schema.

## Remaining acceptance

Flutter SDK is not installed in the editing environment. The owner machine must rerun `flutter analyze`, then `flutter test`, then `flutter run` for build 73.
