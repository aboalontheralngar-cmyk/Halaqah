# P1.26 — Supabase execution path after owner schema reconciliation

## Current confirmed state

The owner ran `P1.26_GUARDED_BASE_BOOTSTRAP.sql` successfully. The subsequently supplied schema confirms the Halaqah base scope exists with UUID identifiers:

`profiles`, `supervisors`, `centers`, `halaqat`, `center_members`, `families`, `family_guardians`.

The same Supabase `public` schema also contains unrelated operational tables from another workload. P1.26 must not alter them.

## What to run now

1. Create a Supabase backup/snapshot.
2. Run `website/supabase/P1.26_POST_BOOTSTRAP_CORE.sql` in the SQL editor.
3. If it returns `Success. No rows returned`, run `website/supabase/P1.26_POST_BOOTSTRAP_VERIFY.sql`.
4. Send the complete verify results. The final `recommendation` must be `READY_FOR_APP_SYNC_TEST` before cloud sync acceptance testing.

Do **not** run the old P1.24 SQL or the pre-bootstrap P1.26 bootstrap again.

## Safety behavior

`P1.26_POST_BOOTSTRAP_CORE.sql` runs in one transaction and aborts before core-table creation when the Halaqah base scope is missing or has incompatible key types. It does not use `DROP TABLE`, `TRUNCATE`, or row deletion and it does not target the unrelated operational tables in the supplied schema.

The verify script is read-only.

## After SQL verification

Test in this order:

1. login/create or select the intended center and halaqah;
2. create one test student;
3. sync attendance;
4. sync one memorization record and one behavior point;
5. create an exam and score;
6. create a temporary study/work hold;
7. create a Quran course enrollment;
8. repeat from a second authenticated account to validate RLS boundaries.
