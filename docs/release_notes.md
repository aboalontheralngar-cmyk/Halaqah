# Build 83 Hotfix 8 — OAuth production + lightweight upload sync

- Web Google OAuth now uses PKCE and a dedicated `/auth/callback` exchange route instead of exposing session tokens in a URL fragment.
- Production OAuth callback origin is derived from the active HTTPS site or `NEXT_PUBLIC_APP_URL`, preventing stale localhost redirects.
- Cloud notification CHECK contract now includes the notification categories already emitted by Flutter.
- Upload-only synchronization journals locally changed domains and skips untouched domains after the first Build 83 baseline upload.
- Build 83 SQLite schema version is 27 and Android build number is 83.

## P1.27 Build 82 Hotfix 7 — البوابة ومصادقة Google ومزامنة الحضور (2026-08-17)

- عولج `SYNC_ATTENDANCE_23505`: Flutter يرفع الحضور على مفتاح العمل `student_id,date` ولا يفرض UUID جديدًا على صف سحابي قديم.
- أصبح إدخال كود الطالب/العائلة يضيف `HAL/FAM` والشرطات تلقائيًا كل خمس خانات، مع قبول اللصق بصيغته الكاملة.
- بوابة الطالب تعرض تشخيصًا يفرق بين Edge Function غير المنشورة، الأسرار الناقصة، وعقد SQL المفقود؛ وإصلاح SQL المستهدف لا يعيد P7.3 ولا scope helpers الحديثة.
- زر Google متاح في الدخول وإنشاء الحساب، ورسالة provider المعطل أصبحت عربية وعملية بدل JSON الخام.
- رسائل الإشراف تميّز العقد/الصلاحيات/العضوية وتؤكد استخدام Build 75/76 الحالي بدل إعادة P7.3 التاريخية.
- أضيف `P1.27_BUILD82_HOTFIX7_VERIFY.sql` لفحص الحضور والبوابة والإشراف، و`P1.27_BUILD82_HOTFIX7_PORTAL_REPAIR.sql` لإصلاح عقد بوابة الطالب فقط عند ثبوت نقصه.

## P1.27 Build 81 Hotfix 6 — تسريع سجلات الحذف ومنع تضخمها (2026-08-17)

- معالجة سبب توقف «رفع تغييرات الجهاز» طويلًا عند سجلات الحذف: Build 80 كان يرسل معظم عمليات الحذف طلبًا طلبًا، وقد يتحول حذف طالب واحد إلى مئات الصفوف بسبب SQLite cascade.
- قبل الشبكة، ينظف التطبيق دفعة واحدة أي delete markers ناتجة عن `INSERT OR REPLACE` إذا كان السجل المحلي ما يزال موجودًا.
- الحذف السحابي المباشر يُجمع حتى 80 معرفًا في طلب واحد باستخدام `inFilter`، مع إبقاء ترتيب الأولويات وحماية العلاقات التابعة للاختبارات والقوالب والدورات.
- حذف الطالب أصبح يولّد outbox واحدًا فقط للطالب، بينما RPC `delete_student_for_sync` يتكفل بسجل الطالب واعتماداته سحابيًا.
- المفاتيح المركبة مثل attendance وmushaf تُجمع في دفعات PostgREST `or(...)` صغيرة بدل طلب لكل سجل، مع fallback متوازٍ محدود للحالات غير القياسية، ويظهر عداد `(تم/الإجمالي)` أثناء مرحلة الحذف.
- صُححت مسارات الحذف التاريخية لتستخدم batch delete، مع تصحيح `exam_scores` من `id` إلى `exam_id`.
- Hotfix 6 لا يحتاج أي SQL جديد، ولا يغيّر SQLite schema أو dependency pins.

## P1.27 Build 80 Hotfix 5 — الاستعادة العاجلة وحماية بيانات الجهاز (2026-08-16)

- رفع build number إلى 80 بعد أن أثبت الجهاز أن تثبيت 78 فوق 79 دفع Flutter إلى إزالة التطبيق، وهو ما أدى إلى فقد SQLite المحلي.
- أضيف حاجز ADB في `verify_source_prerequisites.ps1` يمنع تشغيل مصدر أقدم من versionCode المثبت بدل السماح بمسار uninstall خطير.
- استعادة Supabase Storage تبحث الآن في جميع مجلدات النسخ الخاصة بالحساب، حتى لو فُقد `sync_center_id` بعد حذف التطبيق، مع بقاء التنزيل مقيدًا بجذر المستخدم الموثق.
- تتوقف المزامنة التلقائية أثناء استعادة snapshot، ثم تُمنع 15 دقيقة بعد النجاح حتى يتحقق المعلم من الأعداد قبل أي دمج سحابي.
- نجاح الاستعادة يعرض عدد الطلاب/الطالبات وسجلات الحفظ والمراجعة المستعادة.
- عولج `SYNC_MEMORIZATION_ARGUMENTERROR`: الرفع لا يعتمد على فك كل صف حفظ تاريخي، والتنزيل يتجاوز الصف غير القابل للتحويل منفردًا بدل إسقاط بقية البيانات، مع عداد تشخيصي.
- لا SQL جديد في Hotfix 5.

## P1.27 Build 78 Hotfix 4 — ترتيب اعتمادات الاختبارات واستقرار المزامنة التلقائية (2026-08-16)

- عولج توقف المزامنة برمز `SYNC_EXAMS_23503`: قوالب الاختبارات تُرفع الآن قبل سجلات الاختبارات التي تشير إليها، ثم تُحذف القوالب المحذوفة بعد تصفية مراجع الاختبارات.
- أي `template_id` قديم يشير إلى قالب محذوف يُحوّل إلى `null` قبل الرفع، وحذف القالب محليًا يصفر مراجع الاختبارات داخل transaction لمنع عودة خطأ المفتاح الخارجي.
- المزامنة التلقائية لم تعد تدخل تنزيلًا ثنائي الاتجاه إذا لم تُضبط عبارة حماية النسخ؛ في هذه الحالة ترفع تغييرات الجهاز فقط بدل الفشل المتكرر في مرحلة النسخة الوقائية. يبقى التنزيل اليدوي محميًا بطلب عبارة الحماية.
- أُعيد التوافق الخلفي لـ`DiagnosticSnapshot` بعد إضافة حقول تشخيص المزامنة، وأضيفت اختبارات تغطي رمز الفشل وقاعدة مراجع قوالب الاختبارات.
- لا يوجد SQL جديد في Hotfix 4؛ نتيجة `P1.27_BUILD78_HOTFIX3_VERIFY.sql` الناجحة تظل صالحة.
- لا تغيير في SQLite أو dependency pins أو رقم Build 78.

## P1.27 Build 78 Hotfix 3 — إصلاح المزامنة وتقليل الاستهلاك (2026-08-16)

- أضيف SQL تراكمي آمن لإصلاح conflict targets المفقودة في مخطط Supabase الحالي دون حذف أي صف؛ يتوقف APPLY إذا وجد تكرارات بدل اختيار سجل عشوائي.
- رفع خريطة المصحف ومتميز اليوم في Flutter يعتمد على المعرّف الثابت/المفتاح الأساسي ولا يشترط composite UNIQUE من الهاتف.
- المزامنة أصبحت مراحل مرئية مع رصد للبطء، وتخزن محليًا اسم مرحلة الفشل ورمزًا آمنًا ووقته لعرضه في مركز التشخيص.
- أزيل network probe الدوري كل 12 ثانية؛ أصبحت المزامنة الدورية foreground-only كل 5 دقائق، مع adaptive backoff يُنشأ فقط بعد فشل اتصال مؤقت.
- أضيف cache لنطاق طلاب الحلقة داخل دورة المزامنة وTTL ست ساعات لقراءة هوية المركز/الحلقة، وأصبحت أخطاء الرفع الأساسية توقف إعلان النجاح بدل الاستمرار الصامت.
- بدأ تنظيم مصدر المزامنة داخل `lib/services/sync/`، ووُحد حوار التقدم بين الرئيسية والإعدادات، وأضيف `docs/INDEX.md` كمرجع توثيق حالي.
- لا تغيير في SQLite أو dependency pins أو رقم Build 78.

## P1.27 Build 78 Hotfix 2 — طباعة الخطط وتسريع المزامنة (2026-08-16)

- صُححت طباعة الخطة عندما تحتوي الجمعة على وضع `catchup_recitation`: تعليمات التدارك للحفظ والمراجعة لم تعد تُرفض باعتبارها مقادير عامة، مع بقاء السرد نطاقًا قرآنيًا دقيقًا.
- رسالة الطباعة لم تعد تعرض بادئة Dart التقنية `Bad state:` للمستخدم.
- مزامنة `study_suspensions` أصبحت diff-based: قراءة سحابية واحدة ثم RPC للتواريخ المتغيرة فقط، بدل إعادة إرسال كل الإجازات العارضة التاريخية.
- أضيف قفل على مستوى `SupabaseService` لمنع تشغيل مزامنتين سحابيتين متوازيتين من المزامنة التلقائية واليدوية.
- لا SQL جديد ولا SQLite جديد ولا تغيير اعتماديات.
- حزمة full-source ذات اسم `no-font-binaries` تحتاج استعادة ملفي Tajawal من نسخة المشروع السابقة قبل Flutter build.

## P1.27 Build 78 Hotfix 1 — إصلاح التجميع والمزامنة الشبكية (2026-08-16)

- صُححت أخطاء التجميع الثمانية التي كشفها `flutter analyze` على جهاز المالك بعد Build78.
- أصبحت النقاط العشرية متوافقة في لوحة الشرف، إضافة النقاط، PDF، نماذج تقارير الفترة واختباراتها.
- صُحح استدعاء حفظ علامة مصالحة محفوظ الطلاب القدامى إلى `saveSetting`.
- أضيف فحص اتصال قبل المزامنة، ومهلة محدودة + retry واحد لـ`set_study_suspension`.
- لا تعيد المزامنة إرسال كل الإجازات العارضة إذا لم تتغير؛ يستخدم التطبيق fingerprint ناجح آخر رفع.
- عند انقطاع الشبكة تظهر رسالة عربية مختصرة ولا يظهر `ClientException` أو رابط Supabase الخام.
- لا يوجد SQL جديد ولا ترقية SQLite ولا تغيير اعتماديات.

# ملاحظات الإصدار

## P1.27 Build 78 — 4.3.0-alpha.24+78 — مجموعات وتقارير وخطط ومسابقات إشرافية (2026-08-15)

- طباعة/فعاليات/مسابقات لمجموعات المستوى، مع قصر التحكيم على أعضاء المجموعة.
- استثناءات قبل ملف PDF الجماعي، ومدفوعات الفترة، وتفصيل صفحات الحفظ والمراجعة والسرد، وملخص طالب من صفحة واحدة.
- اختيار الفترة هجريًا أو ميلاديًا، وسياسة مرنة للجمعة، ومصالحة محفوظ الطلاب القدامى.
- مقترحات المراجعة أصبحت اختيارية، والتسميع لطالب مستأذن يطلب تصحيح الحضور.
- نقاط عشرية حقيقية افتراضيًا مع بقاء سياسات التقريب الأخرى اختيارية.
- بوابة مسابقات للجهة الإشرافية بمواسم وفئات وترشيح المراكز والتحكيم والنتائج.
- SQLite 26 وSQL Build78 incremental؛ لا تغيير في dependency pins.

## P1.27 Build 77 — 4.3.0-alpha.23+77 — تقارير وتجربة استخدام واستقرار (2026-08-15)

- نقل مجموعات المستوى المتقارب إلى الشاشة الرئيسية مع تسمية تلقائية بالسور والأجزاء.
- إضافة استثناءات فترة التقرير من ترتيب الأوائل فقط، مع زر لاستبعاد المستجدين داخل الفترة دون حذف بياناتهم من الإجماليات.
- إعادة تصميم التقرير التجميعي وواجهة التقارير؛ PDF التجميعي وملخص الإدارة أصبحا صفحة A4 أفقية واحدة مع نصوص عالية التباين.
- رفع مقترح الخطة إلى مقدمة فهرس المراجعة مع وسم واضح.
- إضافة تعديل المبلغ/البيان/التاريخ لمعاملات صندوق الحلقة.
- معالجة overflows المبلغ عنها في QR والقرعة وإضافة قاعدة النقاط ومتميز اليوم.
- جعل بدء التطبيق أكثر تحمّلًا للأخطاء وإضافة إعادة محاولة من شاشة الفشل.
- تثبيت لون أيقونة المزامنة على لون AppBar المتباين.
- لا يوجد SQL جديد، ولا تغيير في الاعتماديات المعروفة نجاحها على جهاز المالك.


## P1.27 Build 76 — 4.3.0-alpha.22+76 — إكمال المواصفات (2026-08-14)

- أُكملت مزامنة الحذف بدفتر SQLite v25 عام، وتطبيق tombstones قبل upload، وغطاء `mushaf_progress`، وحذف طالب ذري محمي.
- أضيف drill-down إشرافي: جهة → مركز → حلقات → أداء الطلاب مع بقاء analyst للقراءة.
- توسعت مجموعات المستوى المتقارب إلى منافسة أسبوعية فعلية داخل كل مجموعة.
- أصبح الاختبار الشهري مرتبطًا بقالب وستة أسئلة منظمة و`template_id` مع sync ثنائي الاتجاه وحفظ ذري.
- أُغلقت مشاكل تباين AppBar tabs والحالات الدلالية Light/Dark وأضيف اختبار contrast.
- توسع ErrorWidget إلى رمز حادثة مع source/operation، وأصبحت سياسة تقريب نقاط الإنجاز قابلة للضبط.
- الاعتماديات المباشرة لم تتغير عن Hotfix 3 المعروف نجاحه على جهاز المالك.
- SQL الجديد incremental: `P1.27_BUILD76_APPLY.sql` ثم `P1.27_BUILD76_VERIFY.sql`.

## P1.27 Build 75 — Hotfix 3 (2026-08-13)

تم إصلاح تعارض Pub الذي منعه Hotfix 2 من الحل: `printing 5.14.3` يعمل على جيل `package:web 1.x` بينما بقي `share_plus 9.0.0` على `web 0.5.x`. أصبح `share_plus` مثبتًا على `10.0.3`، وهو متوافق مع `web 1.x` ويحافظ على API المشاركة الحالية في المشروع. لا يوجد SQL جديد؛ تحقق Supabase الحي ناجح بالكامل.

# ملاحظات الإصدار — حلقتي

## P1.27 Build 75 — Hotfix 2 (2026-08-13)

تم تأكيد نجاح SQL Build 75 بالكامل على قاعدة المالك. يعالج Hotfix 2 فشل تجميع Flutter بعد أن خفّض `flutter pub get` 41 اعتمادًا بسبب pins قديمة: يعيد مجموعة الاعتماديات الحديثة المتوافقة، يرفع `printing` إلى `5.14.3`، ويصحح `showDatePicker.confirmText` و`FontWeight.w900` وبقية ملاحظات analyzer الواردة من جهاز المالك. لا يوجد SQL جديد.

## P1.27 Build 75 — 4.3.0-alpha.21+75

Build 75 يركز على صحة العمل اليومي على أكثر من جهاز: نقاط التسميع أصبحت نسبية، المحفوظ يُستنتج من اتجاه الطالب، المراجعة ترى النطاق الحقيقي، والإجازة العارضة تستطيع التراجع الآمن عن إغلاق تلقائي سابق. كما أضيفت مزامنة تلقائية ودفتر حذف سحابي لمنع عودة السجل المحذوف من جهاز كان دون إنترنت.

بوابة الجهة الإشرافية تستطيع الآن إنشاء مركز تابع مباشرة لمالك الجهة/الإداري بعد تطبيق SQL، مع بقاء dashboard والزيارات والفريق. أضيف أيضًا الاختبار الشهري 30+30+20+20، مجموعات التنافس المتقاربة، التسجيل المتأخر ثلاثة أيام، التاريخ الهجري/الميلادي في الخطط، وتباين/تشخيص/هوية مشاركة أفضل.

**SQL:** `website/supabase/P1.27_BUILD75_APPLY.sql` ثم `P1.27_BUILD75_VERIFY.sql`. لا تعاود P7.3 القديمة ولا `database_schema.sql` على قاعدة قائمة.

## P1.26 Build 74 — 2026-08-12

- إصلاح بوابة الجهات الإشرافية في القواعد التي طُبقت عليها P7.3 جزئيًا، مع فحص جاهزية يوضح سبب المشكلة بدل الرسالة العامة.
- إصلاح تاريخ اليوم في الويب لمستخدمي المناطق الزمنية الموجبة، وتشديد إعدادات Supabase وQR والسجلات.
- عمليات الأسرة الحساسة أصبحت ذرية، وتحسن أداء واجهات الويب عبر Zustand selectors.
- قبل الاستخدام السحابي شغّل `website/supabase/P1.26_BUILD74_APPLY.sql` ثم فحص `P1.26_BUILD74_VERIFY.sql`.

## السجل التاريخي للميزات

يحتوي هذا المستند على تدوين وتوثيق شامل ومفصل لكافة الميزات والوظائف المدمجة في تطبيق **حلقة (Halaqah)** للهواتف المحمولة والويب منذ بداية التطوير وحتى اليوم، مصنفة حسب الإصدارات البرمجية.

---

## 🚀 نظرة عامة
تطبيق **حلقة** هو نظام تقني متكامل لإدارة حلقات تحفيظ القرآن الكريم وتسميعه. يهدف إلى تيسير مهمة المعلم، وتسهيل متابعة ولي الأمر لتقدم الطالب، وضمان الدقة والنزاهة الهيكلية للبيانات من خلال حسابات المصحف الفعلي والمزامنة السحابية الفورية.

---

## 📦 الإصدارات وتاريخ الميزات

### ✅ المرحلة P1.26 (خط موحد وتباين وصيانة المسابقات وتدقيق Supabase عميق)

* **نسخة التطوير**: `4.3.0-alpha.19+73 (P1.26)`، مع بقاء SQLite عند `24`.
* **الخط**: توحيد واجهة Flutter وPDF على Tajawal 400/700 الثابتين؛ أزيلت إحالات الخط غير المعلن `Amiri` من مواضع النص القرآني في الواجهة لتجنب اختلاف fallback بين الأجهزة.
* **الوضع الفاتح/الداكن**: ضبط TextTheme وChip/DataTable والشاشات الأعلى استخدامًا على `ColorScheme.onSurface/onSurfaceVariant` بدل درجات رمادية ثابتة ضعيفة التباين.
* **المسابقات**: إضافة تعديل اسم وفئة المسابقة، وحذفها بعد تأكيد مع حذف نتائجها التابعة داخل transaction، وجلب أعداد النتائج باستعلام تجميعي واحد.
* **Supabase**: نتيجة `STOP_BASE_SCOPE_MISSING` تمنع صراحةً تشغيل migration P1.25 أو verify. أضيف فحص P1.26 عميق للقراءة فقط يحدد هل المشروع فارغ أم جزئي، وbootstrap محروس لا يعمل إلا عند خلو مخطط حلقتي من جميع الجداول المستهدفة.
* **الجودة**: أضيف validator خاص بـP1.26 يمنع رجوع خط متغير/غير معلن، أو حذف غير ذري للمسابقة، أو bootstrap فوق مخطط قائم.
* **Hotfix build 73**: إصلاح سبعة أخطاء تجميع `const_eval_method_invocation` ظهرت في اختبار المالك الحقيقي؛ أزيل `const` فقط من النصوص التي تستدعي `Theme.of(context)` في التقارير والإجازات والقوالب وتفاصيل الطالب، مع إضافة regression guard يمنع عودتها.
* **حالة Supabase بعد bootstrap**: تم توثيق نجاح base bootstrap من مخطط المالك الفعلي. أضيف `P1.26_POST_BOOTSTRAP_CORE.sql` لإكمال جداول حلقتي فقط داخل المشروع السحابي المختلط، و`P1.26_POST_BOOTSTRAP_VERIFY.sql` للتحقق من UUID/RLS/helpers دون لمس جداول النظام الآخر الموجودة في نفس `public`.

### قاعدة البيانات

* تم تنفيذ `P1.26_GUARDED_BASE_BOOTSTRAP.sql` بنجاح في بيئة المالك، وأكد المخطط المرسل وجود base scope الخاص بحلقتي بمفاتيح UUID.
* الخطوة الحالية: تشغيل `website/supabase/P1.26_POST_BOOTSTRAP_CORE.sql` بعد نسخة احتياطية. السكربت لا يلمس جداول workload الآخر الموجودة في نفس `public`.
* بعد نجاحه فقط شغّل `website/supabase/P1.26_POST_BOOTSTRAP_VERIFY.sql`. الحالة النهائية المطلوبة هي `READY_FOR_APP_SYNC_TEST`.
* لا تعِد تشغيل bootstrap القديم، ولا تستخدم SQL P1.24 القديم على هذه القاعدة.

### ✅ المرحلة P1.25 (توافق سحابي آمن وPDF عربي ثابت وتسريع البيانات)

* **نسخة التطوير**: `4.3.0-alpha.18+70 (P1.25)`، مع ترقية SQLite إلى `24`.
* **Supabase**: استبدال افتراض اكتمال المخطط بمسار preflight قراءة فقط؛ لا يُنفذ الإصلاح إلا عند نتيجة `READY_...`. migration التوافق ينشئ الأسطح المفقودة بصورة إضافية ويتوقف عند تعارض النطاق أو أنواع المفاتيح بدل تحويلها.
* **PDF العربي**: فصل تقني مقصود بين خط الواجهة Readex Pro وخط PDF؛ يستخدم التصدير Tajawal 400/700 الثابتين فقط مع RTL صريح وعكس أعمدة الجداول، بعد أن أثبت الجهاز أن ملف Readex Pro المتغير غير موثوق مع محرك PDF المستخدم.
* **تشخيص PDF**: إضافة زر «اختبار PDF العربي» في مركز التشخيص قبل تجربة التقارير الكبيرة.
* **النقاط التلقائية**: قياس الصفحة والحزب بنسبة الأسطر الفعلية، إزالة تكرار الآيات المتداخلة، وتجميد snapshot لقواعد المكافأة تاريخيًا حتى لا يعيد تغيير الإعدادات كتابة نتيجة يوم سابق.
* **الأداء**: فهارس SQLite إضافية، تحميل تقارير الفترات جماعيًا، وتحويل تنزيلات الحضور والعائلات والسلوك والإجازات والاختبارات والتلقين والإداريات والصندوق والإشعارات والدورات إلى قراءات/كتابات مجمعة بدل N+1 كلما أمكن. كما أصبحت upsertات الجداول الأب تحديثًا آمنًا بدل `REPLACE` حتى لا تحذف العلاقات التابعة أثناء المزامنة.
* **تدقيق المتبقي**: البنود القديمة التي كانت موصوفة كمتبقية في المصدر نُفذت؛ ما بقي قبول خارجي على الجهاز/السحابة/الطابعة، مع اعتبار Google Drive API المباشر توسعة اختيارية لا نقصًا في النواة.

### ✅ المرحلة P1.24 (وحدات تعلم مستقلة وتقارير RTL ودورات قرآنية)

* **نسخة التطوير**: `4.3.0-alpha.17+69 (P1.24)`، مع ترقية SQLite إلى `23`.
* **هوية الألوان**: عادت ألوان حلقتي الخضراء/الذهبية الدافئة السابقة، مع الإبقاء على توزيع P1.23 المضغوط والمسافات والتنقل الخفيف.
* **PDF**: بدأت P1.24 توحيد RTL وهوية التقارير، لكن اختبار الجهاز كشف لاحقًا أن Readex Pro المتغير غير موثوق داخل محرك PDF؛ عولج ذلك نهائيًا في P1.25 باستخدام Tajawal ثابت للتصدير فقط.
* **النقاط والسلوك**: اختيار عدة أسباب في عملية واحدة وإسنادها لعدة طلاب ذريًا، مع تبسيط واجهة قائمة الطلاب.
* **الحضور**: الضغط على حالة الحاضر/المتأخر المحددة يعيد اليوم إلى «غير محدد» دون إجبار الطالب على غياب أو استئذان، مع المحافظة على بيانات التعلم والنشاط وإعفاء التسميع المسجلة يدويًا.
* **الحفظ والمراجعة**: وحدة قياس مستقلة للحفظ ووحدة مستقلة للمراجعة على مستوى الطالب والخطة.
* **التلقين**: مرحلة صريحة يحددها المعلم لكل طالب، مع إجراء «إنهاء مرحلة التلقين» داخل شاشة الطالب يحفظ السجل التاريخي ويخرجه من المسار مستقبلًا.
* **الاختبارات**: حذف وطباعة الامتحان الشفهي/التحريري، وإدخاله في تقرير الطالب للفترة أو الشهر وWhatsApp وPDF.
* **فهرس السور**: فتح منتقي السور حول سورة الطالب الحالية بدل البدء دائمًا من الفاتحة، بما يشمل الحفظ والتلقين ونطاقات الاختبار المرتبطة بالطالب.
* **الدورات القرآنية**: دورات حفظ/مراجعة/مختلطة محددة المدة والأيام، بمقررات مستقلة ومشاركين، ومدمجة مع عمل الحفظ والمراجعة اليومي.
* **النقاط التلقائية**: منع مكافأة المقرر الناقص، وفصل مكافأة الإتمام عن مكافأة الزيادة الحقيقية، وحساب الوجه/الحزب من مقدار الأسطر الفعلي بدل مجرد لمس صفحة، مع تقدير محافظ للآيات التي تأتي بيانات أسطرها صفرًا في مصدر القرآن.
* **التوقف وWhatsApp**: استمرار `full_pause` لظروف الدراسة/العمل، وتأكيد أسماء الأيام والتواريخ في رسالة WhatsApp.
* **Supabase**: migration P1.24 مجهز فقط للتنفيذ اليدوي لاحقًا؛ التطبيق يحتفظ بالعمل المحلي قبل تنفيذه.
* **الجودة**: نجحت 51 من 52 بوابة مصدر؛ P1.21 فقط لم تبدأ لغياب `website/node_modules/typescript` في بيئة المصدر، كما نجح فحص ساكن لـ186 ملف Dart. يبقى `flutter analyze/test/run` واختبار PDF على جهاز Flutter الحقيقي.

### قاعدة البيانات

* SQLite يترقى تلقائيًا إلى `23`.
* Supabase يحتاج `website/supabase/migrations/20260808000200_p1_24_courses_learning_units.sql` عند تفعيل مزامنة حقول P1.24 والدورات.
* خطوات التنفيذ في `docs/P1.24_SQL_AND_SETUP.md` والبحث المرجعي للدورات في `docs/P1.24_QURAN_COURSES_RESEARCH.md`.

### ✅ المرحلة P1.23 (إصلاح PDF وإعادة بناء UI/UX وتنظيف Flutter)

* **نسخة التطوير**: `4.3.0-alpha.16+68 (P1.23)`، مع بقاء SQLite عند `22` ودون migration جديد.
* **Hotfix build 68**: إزالة `visualDensity` من `ChipThemeData.copyWith` لأنه غير مدعوم في Flutter الحالي؛ يستمر ضغط الواجهة عبر `ThemeData.visualDensity` العام.
* **PDF العربي**: إعلان Tajawal العادي والعريض كـassets صريحة، وإلغاء fallback إلى Helvetica، وتطبيق `pw.ThemeData.withFont` على جميع عائلات التصدير حتى ترث الجداول والنصوص العربية الخط الصحيح.
* **Flutter analyzer**: معالجة الأنماط المتقادمة الظاهرة في سجل المالك (`withOpacity`، `DropdownButtonFormField.value`، إدارة `RadioListTile` القديمة، `MaterialState*`، `anonKey`، و`Switch.activeColor`) وتنظيف التحذيرات غير المستخدمة والتعابير الميتة الواردة في السجل.
* **UI/UX**: إعادة بناء الثيم إلى واجهة مسطحة ومضغوطة بخلفيات فاتحة، شريط علوي كحلي، أخضر للإجراء، بطاقات بلا elevation افتراضي، خطوط أصغر، زوايا أخف، وأيقونات خطية في المكونات الرئيسية.
* **المكونات المشتركة**: تحديث بطاقات التركيز والإجراءات والإحصاءات والطالب والحضور، ما ينعكس على جميع الشاشات التي تعتمد النظام المشترك، مع تعديلات مباشرة للشاشات الأعلى استخدامًا وإزالة التدرجات والظلال الثقيلة المتبقية من Flutter.
* **الويب**: توحيد متغيرات الألوان والزوايا والظلال والانتقالات الأساسية مع هوية الهاتف الجديدة.
* **الأداء المحسوس**: تأجيل تحميل بيانات الرئيسية الثقيلة إلى ما بعد الإطار الأول لتقليل التقطيع الذي ظهر في سجل جهاز المالك.
* **مرجع التصميم**: تمت دراسة الصور العامة المنشورة لتطبيق «قرضة» في Google Play لاستخلاص مبادئ الخفة والكثافة المفيدة والأيقونات المسطحة، من دون الوصول إلى مصدره أو نسخ شاشة بعينها.
* **P1.22 السحابي**: أكد المالك تنفيذ SQL ونجاح التحقق؛ لقطة النتيجة أثبتت `closing_respects_activity_exemption=true` و`closing_respects_full_pause=true`.
* **الجودة**: توجد 51 بوابة إصدار؛ نجحت 50 بوابة قابلة للتشغيل في حزمة المصدر النظيفة، وتعذر تشغيل P1.21 فقط لغياب `website/node_modules/typescript`. بوابة P1.23 نفسها ناجحة، ولا يتوفر Flutter SDK في بيئة التعديل لتشغيل `flutter analyze/test`.

### قاعدة البيانات

* لا تغيير على SQLite 22 ولا migration جديد في P1.23.
* migration P1.22 منفذ ومتحقق بحسب نتيجة المالك في 2026-08-08.
* إعداد Google OAuth الخارجي يبقى مستقلًا ما لم يؤكد المالك تفعيله.

### ✅ المرحلة P1.22 (تشغيل يومي مرن: النشاط والتلقين والتوقف والتسميع الممتد)

* **نسخة التطوير**: `4.3.0-alpha.15+66 (P1.22)`، مع ترقية SQLite إلى `22`.
* **النشاط والتحضير**: يمكن تحضير الطالب داخل محاضرة أو نشاط أو دوري أو عشاء أو رياضة أو مسابقة ثقافية، مع إعفائه من إلزام التسميع لذلك اليوم حتى لا يظهر ضمن «لم يسمّع».
* **التوقف المؤقت**: فصل إيقاف التسميع عن التوقف الكامل للدراسة أو العمل؛ التوقف الكامل يعفي من الحضور والتسميع ولا يولّد غيابًا تلقائيًا.
* **التلقين**: تبويب وسجل مستقلان للتلقين لا يرفعان المحفوظ، ويدعمان نطاقًا متصلًا داخل سورة أو عبر عدة سور.
* **الحفظ والتسميع عبر السور**: يمكن اختيار بداية في سورة ونهاية في سورة أخرى، بما يناسب السور القصيرة المتتابعة.
* **المراجعة**: ضمان تهيئة بيانات القرآن قبل بناء قائمة السور، وتقسيم المراجعة الإلزامية في نظام التثبيت إلى صفحات موزعة على أيام بدل إعادة السورة كاملة كل يوم.
* **الحضور والتسميع**: قبل حفظ تسميع لطالب مسجل غائبًا يظهر تنبيه صريح يطلب من المعلم تأكيد تحويله إلى حاضر. كما صُحح استدراك الغياب عند وجود أيام دراسة معلقة متتالية.
* **الانضباط**: إسناد النقاط والعقوبات لعدة طلاب دفعة واحدة، وموازنة قيم السلوك من الإعدادات، وسجل إداري مستقل للإنذارات والتنبيهات والتعهدات والتواصل مع ولي الأمر.
* **تشغيل الحلقة**: ترتيب طلاب التسميع بالسحب والإفلات، ونظام تقييم يومي أولي للحلقة النموذجية.
* **تقارير WhatsApp**: إضافة اسم اليوم والتاريخ وحالة الطالب لكل يوم داخل التقرير، مع توضيح النشاط والتلقين والتوقف والإعفاء.
* **الحسابات**: إضافة خيار «المتابعة باستخدام Google» للهاتف والويب عبر Supabase OAuth؛ يحتاج موفر Google وRedirect URLs إلى تفعيل خارجي قبل الاستخدام الفعلي.
* **السحابة**: migration P1.22 للنشاط والتلقين والإداريات والتوقف الكامل نفذه مالك النظام لاحقًا بنجاح، وفحص التحقق المرفق أكد احترام إعفاء النشاط والتوقف الكامل في الإغلاق.
* **الجودة**: نجحت **50/50** بوابة إصدار، وفحوص صياغة TypeScript وملفات Android/iOS، وفحص ساكن لـ184 ملف Dart. لم يُشغل `flutter analyze` أو `flutter test` في بيئة التطوير الحالية لعدم توفر Flutter/Dart SDK.

### قاعدة البيانات

* SQLite يترقى تلقائيًا إلى الإصدار `22` داخل التطبيق.
* Supabase: أكد المالك في 2026-08-08 تنفيذ `website/supabase/migrations/20260808000100_p1_22_activity_talaqqin_admin.sql` ونجاح `website/supabase/verify_p1_22.sql`.
* خطوات SQL وGoogle موثقة في `docs/P1.22_SQL_AND_GOOGLE_SETUP.md`، وسجل التنفيذ الكامل في `docs/P1.22_IMPLEMENTATION_LOG.md`.

### ✅ المرحلة P1.21 (تقارير الأسرة الدورية وتقوية التبادل)

* **نسخة التطوير**: `4.3.0-alpha.14+64 (P1.21)`.
* **التقارير الدورية**: اشتراك أسبوعي أو شهري لكل عائلة بموافقة صريحة، مع نشر لقطة تاريخية مستقلة عن العرض الآني.
* **بوابة ولي الأمر**: سجل لآخر التقارير المنشورة وحالة الإرسال الخارجي لكل تقرير.
* **الإرسال الخارجي**: صف محاولات ووظيفة Edge موقعة بـHMAC لمزود HTTPS يختاره المشغل؛ لا توجد مفاتيح مزود في المتصفح أو المصدر.
* **العمل دون إنترنت**: كود ربط من 12 رمزًا وصلاحية 30 دقيقة، ولا يُرسل الكود مع الملف المشفر.
* **تقليل البيانات**: حزمة التبادل تستبعد إعدادات الهاتف وسجل التدقيق وقوالب الرسائل، وتبقي النسخة الاحتياطية الكاملة مسارًا منفصلًا.
* **أمان الويب**: تحديث Next.js إلى `16.2.12` وPostCSS إلى `8.5.23` لمعالجة نشرات التدقيق عالية الخطورة.
* **قواعد البيانات**: لا تغيير في SQLite 21؛ يلزم migration Supabase الخاص بـP1.21 لتفعيل التقارير الدورية.

### ✅ المرحلة P1.20 (تشغيل شامل وتقارير وتبادل محلي)

* **نسخة التطوير**: `4.3.0-alpha.13+63 (P1.20)`.
* **الواجهة**: شبكة مضغوطة للمهام اليومية، وخط Readex Pro مضمن، وهوية داكنة فحمية تحافظ على الأخضر الأساسي.
* **التقارير**: لوحة أداء فعلية للحلقة، وفترات يومية وأسبوعية وشهرية، وشاشات مستقلة للتقرير الشامل وسند الاستلام.
* **الطالب**: إجمالي المحفوظ مشتق من نطاق البداية وسجلات التسميع غير المكررة، ويُصالح بعد تعديل الملف.
* **المراجعة**: أربعة أنظمة قابلة للاختيار لكل طالب، مع تطبيق التثبيت الخماسي والترتيب المتباعد داخل شاشة المراجعة.
* **المسابقات**: إدارة أحداث ونتائج وتحكيم بخمسة أنواع أخطاء، ودمج مولد أسئلة الاختبار.
* **دون إنترنت**: ملف تبادل مشفر وكود ربط، يُرسل عبر وسيلة المشاركة المحلية التي يوفرها Android ويُدمج دون استبدال إعدادات الهاتف.
* **ولي الأمر والإشراف**: تحديث دوري للبوابة وسجل السلوك، وجدولة زيارات الموجه وتوثيق إكمالها.
* **Android**: بقاء الحد الأدنى Android 6، وتفعيل R8 وتقليص الموارد، وإضافة بناء APK منفصل لكل معمارية.
* **قواعد البيانات**: SQLite 21، وترحيل Supabase واحد للمراجعة والبوابة والزيارات الإشرافية.

### ✅ الإصلاح P1.19.1 (استعادة بناء Flutter)

* **نسخة التطوير**: `4.3.0-alpha.12+62 (P1.19.1)`.
* **تخطيط الشاشة المشترك**: استُبدل استخدام `Center` غير الصحيح بـ`Align` مع الحفاظ على محاذاة المحتوى إلى أعلى المنتصف.
* **نتيجة الامتحان**: تمرير سياق الشاشة صراحةً إلى صفوف التفاصيل حتى تتبع ألوان الوضع الداكن من دون خطأ ترجمة.
* **الجودة**: إضافة بوابة إصدار تمنع رجوع خطأي البناء، من دون أي تغيير في SQLite أو Supabase.

### ✅ المرحلة P1.19 (الهوية وتجربة الاستخدام الشاملة)

* **نسخة التطوير**: `4.3.0-alpha.11+61 (P1.19)`.
* **بحث مرجعي**: دراسة رسمية لتجارب ترتيل وQuranly وآية وQuran.com والمحفظ، وتحويل المبادئ المناسبة إلى هوية مستقلة لحلقتي.
* **نظام تصميم واحد**: الهاتف والويب يستخدمان الألوان والمسافات والزوايا والحالات الدلالية نفسها، مع وضع داكن عالي التباين.
* **الرئيسية**: مساحة عمل تبدأ بما يحتاج إجراءً اليوم بدل التحيّات والبطاقات الضخمة.
* **الحفظ**: تسلسل واضح للطالب والنطاق والتقييم والاعتماد، وملخص تقدم يومي أكثر إحكامًا.
* **التنقل**: تصنيف الأدوات حسب سير العمل بدل قائمة طويلة مسطحة.
* **الأداء**: استعلامات متوازية في أهم شاشات الهاتف واشتراكات React محددة بدل إعادة التصيير مع كل تغيير في المخزن.
* **الحماية البصرية**: إزالة درجات الألوان غير الموجودة والرماديات الثابتة التي كانت تخفي النص في الوضع الداكن.

### ✅ المرحلة P1.18 (الخطة والتسميع وواجهة العمل)

* **نسخة التطوير**: `4.3.0-alpha.10+60 (P1.18)`.
* **إنجاز الخطة**: نسبة كلية ومحاور مستقلة للحفظ والمراجعة والسرد، محسوبة تلقائيًا من التسجيلات الواقعية داخل مدة الخطة.
* **الخطة المطبوعة**: اتجاه عربي من اليمين إلى اليسار ومربع واضح أمام كل يوم ليؤشر الطالب بعد الإنجاز.
* **إكمال السورة**: إشعار دائم وغير مكرر باسم السورة والطالب، مع توضيح انتقالها إلى المراجعة المستحقة.
* **QR والتسميع**: منع فتح مسارين متعارضين للطالب نفسه، وإيقاف الماسح بعد قبول الرمز.
* **الواجهة**: رأس رئيسية أقصر، ملخص حفظ مضغوط، وإجراءات طالب أقل ازدحامًا، مع تحسين تباين النصوص الثانوية.
* **الجودة**: 45 بوابة إصدار ناجحة، مع نجاح ESLint وTypeScript وبناء 31 مسارًا للويب.

### ✅ الإصلاح P1.17.2 (مركز الإعدادات المنظم)

* **نسخة التطوير**: `4.3.0-alpha.9+59 (P1.17.2)`.
* **تنقل واضح**: إعدادات المعلم اليومية منفصلة عن الإدارة والحماية، مع بحث سريع عن القسم.
* **البيانات**: ثلاث مساحات مستقلة للنسخ والحماية، والمزامنة والاتصال، والخصوصية والسجل.
* **إدارة النظام**: بوابات واضحة لجاهزية الإطلاق والتشخيص الفني وسجل التدقيق بدل عرض التفاصيل التقنية في مستوى واحد.
* **الجودة**: 44 فحص عقد، دون SQL جديد.

### قاعدة البيانات

لا يوجد SQL أو migration جديد في هذه الدفعة.

### ✅ الإصلاح P1.17.1 (تسجيل الحفظ المباشر عبر QR)

* **نسخة التطوير**: `4.3.0-alpha.8+58 (P1.17.1)`.
* **Android**: بعد مسح رمز الطالب يختار المعلم جلسة تسميع أو تسجيل حفظ مباشر دون جلسة، إضافةً إلى المراجعة والحضور والإجازة.
* **الويب**: نتيجة المسح تتيح الحفظ المباشر أو جلسة التسميع أو الاكتفاء بالحضور، وتنقل هوية الطالب وطريقة التسجيل بأمان إلى شاشة التسميع.
* **التحقق**: يبدأ النموذج من موضع الطالب التالي، ويتحقق من السورة والآيات ويمنع الحفظ الجديد للخاتم ويطبق التوقيف.
* **الجودة**: نجح TypeScript وESLint وبناء 31 مسارًا، و43 فحص عقد.

### قاعدة البيانات

لا يوجد SQL أو migration جديد في هذه الدفعة.

### ✅ المرحلة P1.17 (مسار الخاتمين ومساحة العمل اليومية)

* **نسخة التطوير**: `4.3.0-alpha.7+57 (P1.17)`.
* **الخاتمون**: تعريف موحد يعتمد حالة التخرج أو إتمام 6236 آية؛ يُحذف عنهم الحفظ الجديد من الشاشة والخطة ونسبة الإنجاز والطباعة، وتبقى المراجعة والسرد/التلاوة.
* **خطة PDF**: خطة الخاتم لا تعرض عنوانًا أو عمودًا أو مقدارًا للحفظ، فرديًا أو ضمن ملف جميع الطلاب.
* **المراجعة**: مساحة كبيرة للسور والآيات، بحث بالاسم أو الرقم، إعداد المقرر في لوحة مستقلة، وزر تسجيل ثابت داخل `SafeArea`.
* **الرئيسية**: ضغط إحصاءات الطلاب والحضور والغياب في شريط واحد، وتقليل الترويسة والتاريخ حتى تظهر المهام اليومية مبكرًا.
* **إغلاق اليوم**: اليوم المتروك مفتوحًا يُغلق عند منتصف الليل إن بقي التطبيق مفتوحًا، أو عند أول فتح تالٍ، مع تعويض الأيام الفائتة واستثناء العطلات والإجازات ومراعاة تاريخ انضمام الطالب ومنع التكرار.
* **تكافؤ الويب**: إخفاء مقرر وعمود الحفظ في خطط الطالب ذي حالة `graduated`.
* **الجودة**: TypeScript وESLint وبناء 31 مسارًا ناجحة، و42 فحص عقد ناجحًا بعد إضافة بوابة P1.17.

### قاعدة البيانات

لا يوجد SQL أو migration جديد في هذه الدفعة؛ الإغلاق التلقائي يستخدم جداول SQLite الحالية ومفاتيح إعدادات محلية آمنة.

### ✅ الإصلاح P1.16.2 (استعادة بيئة الويب على Windows)

* **نسخة التطوير**: `4.3.0-alpha.6+56 (P1.16.2)`.
* **تشغيل مباشر**: `SETUP_WEB_WINDOWS.cmd` يثبت حزم الويب المقفلة ويفحصها ويبني الموقع من خطوة واحدة.
* **تشخيص حقيقي**: تتحقق الأداة من React وNext.js وLucide وتعريفات React وخادم TypeScript بدل الاكتفاء بنجاح أمر التثبيت.
* **فتح مرن**: إعداد TypeScript يعمل عند فتح `Halaqah` أو `website` وحده داخل VS Code.
* **أنواع أوضح**: أصبحت عناصر القائمة و`ReactNode` وأحداث الواجهة معرفة صراحةً، فلا تتكاثر أخطاء `implicit any` عند تعطل تحليل الحزم.
* **السحابة**: نتيجة التحقق المرفقة ناجحة `4/4 true`، ولا يوجد SQL إضافي.

### ✅ الإصلاح P1.16.1 (خطط قرآنية دقيقة وتشغيل أكثر صمودًا)

* **نسخة التطوير**: `4.3.0-alpha.5+55 (P1.16.1)`.
* **خطة يومية صريحة**: كل يوم دراسة يحدد سورة وآية البداية والنهاية للحفظ والمراجعة والسرد/التلاوة.
* **لا مقادير عامة**: أزيل مسار طباعة «صفحتين» أو «ثلاث صفحات»؛ نقص نطاق المحفوظ يظهر للمعلم قبل إنشاء الخطة.
* **كل أيام الفترة**: الجمعة والتعليق والإجازة والتوقيف تبقى ظاهرة داخل الجدول مع السبب.
* **استمرار السرد**: الخطة الجديدة تبدأ من آخر موضع سرد مسجل للطالب عبر الخطط السابقة.
* **تقارير هجرية سليمة**: مفاتيح الأشهر ثابتة وفريدة، فلا ينهار حوار PDF الجماعي عند إعادة البناء.
* **تشغيل متين**: تعطل السحابة أو النسخ الخلفي أو جزء واجهة يسجل للتشخيص ولا يغلق التطبيق كله.
* **قاعدة البيانات**: لا يوجد SQL جديد في هذا الإصلاح.

### ✅ المرحلة P1.16 (السرد الرقمي وإعداد الويب)

* **نسخة التطوير**: `4.3.0-alpha.4+54 (P1.16)`.
* **جلسة مستقلة**: تسجيل السرد/التلاوة داخل الخطة دون احتسابه حفظًا جديدًا أو تغيير إجمالي المحفوظ.
* **نطاق متصل**: يبدأ المعلم من سورة وآية، ويكوّن التطبيق مقدار الخطة عبر السور بحسب الآيات أو الأسطر أو الصفحات أو الأحزاب.
* **توثيق كامل**: تاريخ وجودة وملاحظات وسجل وحذف للجلسة كلها، حتى إذا قُسمت إلى أكثر من سورة.
* **إنجاز أدق**: نسبة الخطة تجمع الحفظ والمراجعة والسرد، ولا تكتمل حسابيًا قبل إتمامها جميعًا.
* **صفوف قرآنية يومية**: تعرض الخطة المطبوعة لكل يوم نطاق الحفظ والمراجعة والسرد من سورة وآية إلى سورة وآية، مع تجاوز أيام عدم الدراسة.
* **مزامنة آمنة**: رفع وتنزيل السرد مع RLS وفحص تطابق الطالب والخطة والحلقة والتاريخ.
* **إصلاح بيئة الويب**: أمر Windows واحد يثبت الاعتمادات المقفلة ويعيد فحص TypeScript والعقود والبناء بعد فك المصدر الكامل.
* **الجودة**: 41 فحص عقد، وTypeScript وESLint بلا أخطاء، وبناء 31 مسارًا ناجح.

### ✅ المرحلة P1.15 (الخطط الذكية والتقرير الإداري)

* **نسخة التطوير**: `4.3.0-alpha.3+53 (P1.15)`.
* **إنجاز قابل للقياس**: تعرض الخطة مقدار الحفظ والمراجعة الفعليين والمطلوبين ونسبة الإنجاز، بعد استبعاد أيام التعليق والعطلة والإجازة والتوقيف.
* **استمرارية عادلة**: تبدأ الخطة التالية من آخر تسميع فعلي ولو لم تكتمل الفترة السابقة، مع بقاء اختبار التجاوز شرطًا قبل إنشاء الخطة التالية.
* **ثلاثة مقررات**: حفظ ومراجعة وسرد/تلاوة في الخطة والشاشة والطباعة الفردية والجماعية ونسخة الكاشير.
* **مراجعة بحسب المحفوظ**: اقتراح تلقائي يزداد بحسب مقدار ما يحفظه الطالب ويمكن للمعلم اعتماده أو تعديله.
* **تقرير مفصول**: الغياب والاستئذان والتأخر والمخالفات أقسام مستقلة، مع إجمالي دقائق التأخر ولون لكل حالة وتقييم يومي للحفظ والمراجعة.
* **ترتيب تشجيعي**: يظهر المركز الأول والثاني والثالث بناءً على الأداء خلال الفترة، لا على إجمالي قديم خارجها.
* **QR عملي**: بعد مسح رمز الطالب يمكن التحضير أو تسجيل الغياب والاستئذان والحفظ والمراجعة والإجازة مباشرة.
* **نقاط أكثر عدلًا**: نقاط إتمام المقرر والزيادة تتدرج بحسب نسبة الإنجاز وحجم المقرر، وتجمع تسجيلات اليوم قبل الحساب.
* **تسوية الصندوق**: يمكن ربط السداد بالمخالفة وتسوية رصيد سلبي دون محو السجل التاريخي، ويظهر المسوّى والمتبقي في التقرير.
* **إشعارات منظمة**: بحث ومرشحات لغير المقروء والحضور والأداء والخطط والمقروء.
* **قاعدة البيانات**: يلزم تنفيذ محتوى migration P1.15 ثم استعلام التحقق، وليس اسم الملف.
* **الجودة**: 40 فحص عقد، وESLint بصفر تحذيرات، وبناء Next.js ناجح.

### ✅ المرحلة P1.14 (الإغلاق السحابي ودوام الحلقة)

* **نسخة التطوير**: `4.3.0-alpha.2+52 (P1.14)`.
* **إغلاق ويب ذري**: اعتماد الحضور والإجازات والنقاط وإيصال الإغلاق داخل معاملة Supabase واحدة بلا نتائج جزئية.
* **طلب آمن للتكرار**: عند انقطاع الشبكة أو تكرار الضغط يعاد الإيصال نفسه ولا تتكرر السجلات أو النقاط.
* **تسميع صحيح**: توحيد قراءة سجل التقييم الحديث والسجل المتوافق، مع استثناء جلسة درجتها «غائب».
* **تعليق متزامن**: سبب تعليق الدراسة محفوظ سحابيًا ويظهر في الحضور ويُستبعد من الغياب والتقارير.
* **دوام المركز**: إعداد وقت النهاية والمنطقة الزمنية وأيام الإجازة الأسبوعية؛ الجمعة هي الافتراضية.
* **عدالة العقوبات**: الغياب بلا عذر يبقى أشد من مجموع التأخر وعدم إتمام المقرر وعدم لبس الثوب.
* **توقيف التسميع**: الطالب الموقوف يحتاج حضورًا لكنه لا يعاقب على عدم التسميع خلال مدة التوقيف.
* **قاعدة البيانات**: يلزم تنفيذ محتوى migration P1.14 ثم استعلام التحقق؛ لا تلصق اسم الملف وحده.
* **الجودة**: 39 فحص عقد، وESLint بصفر تحذيرات، مع بوابة بناء Next.js.

### ✅ المرحلة P1.13 (إغلاق اليوم والتشغيل الآمن)

* **نسخة التطوير**: `4.3.0-alpha.1+51 (P1.13)`.
* **مركز يومي**: شاشة مراجعة تعرض المكتمل، وبلا حضور، والغائب، ومن لم يسمّع، والمستثنى قبل الاعتماد.
* **قرار صريح**: لا يُعاقب الطالب الذي لم يُسجل حضوره أثناء الدوام؛ لا يتحول إلى غائب إلا بعد انتهاء الدوام واعتماد المعلم.
* **معاملة واحدة**: سجلات الحضور والنقاط وعلامة إغلاق اليوم تُحفظ ذريًا، مع منع التكرار لنفس التاريخ.
* **استثناءات عادلة**: تعليق الدراسة والجمعة والإجازة المعتمدة لا تنتج عقوبة، وإيقاف التسميع لا يلغي واجب الحضور.
* **تكافؤ الويب**: لوحة تدقيق عمليات اليوم مرتبة بالعربية وروابط مباشرة لاستكمال الحضور والتسميع.
* **تواصل مكتمل**: زر متابعة الانضباط يفتح واتساب فعلًا برسالة عربية قابلة للتعديل، ويتحقق من وجود رقم ولي الأمر.
* **أمان الدخول**: إلغاء الحساب الوهمي عند غياب إعداد Supabase، وإظهار عائق واضح وتعطيل الإرسال بدل تجاوز المصادقة.
* **مرونة الواجهة**: بطاقات وإجراءات الرئيسية تتغير بين عمود وعمودين وثلاثة أو أربعة حسب العرض وتكبير الخط.
* **رئيسية فعالة**: مراجعة اليوم ضمن الإجراءات الأساسية، والتاريخ يعتمد يوم الجهاز المحلي، وأزرار الإشعارات والتحفيز والإجراء العاجل تنفذ وجهتها فعلًا.
* **عمل دون زخارف شبكية**: أزيلت الصور الزخرفية الخارجية من الدخول والرئيسية والتقارير لصالح خلفيات CSS محلية.
* **اعتمادات آمنة**: تحديث `sharp` و`postcss` التابعين وإغلاق تنبيهات التدقيق، لتصبح نتيجة `npm audit` صفر ثغرات معروفة.
* **الجودة**: فحص عقد جديد يرفع البوابات إلى 38، ولا يوجد SQL تعديلي جديد.

### 🛡️ المرحلة P1.12 (جاهزية التشغيل والقبول الفعلي)

* **نسخة التطوير**: `4.2.0-alpha.8+50 (P1.12)`.
* **لوحة موحدة**: تعرض مخطط SQLite وسلامة البيانات والنسخة الحديثة وأخطاء النسخ واتصال Supabase والجلسة والحوادث في قرار واحد.
* **حالات صريحة**: ناجح أو ملاحظة أو عائق أو اختبار قبول متبقٍ، بدل ترك المستخدم يفسر السجلات التقنية.
* **قبول فعلي**: خمسة بنود للجهازين والاستعادة والعزل والطباعة وQR، محفوظة لهذه النسخة فقط.
* **خصوصية**: تقرير الجاهزية قابل للنسخ والمشاركة ولا يتضمن بيانات الطلاب الشخصية أو رموز الجلسات.
* **بوابات آمنة**: استعلام قراءة فقط يشرح أن صفر سياسات لجداول الاعتماد والجلسات هو الوضع المتوقع عند تفعيل RLS وسحب الصلاحيات المباشرة.
* **الجودة**: ارتفع عدد فحوص العقود إلى 37، ولا يوجد SQL تعديلي جديد.

### 🧪 المرحلة P1.11 (سلامة البيانات وقبول النسخة)

* **نسخة التطوير**: `4.2.0-alpha.7+49 (P1.11)`.
* **فحص محلي آمن**: مركز التشخيص يفحص العلاقات والتكرار وأكواد الطلاب والمقررات والإجازات والنطاقات القرآنية دون تنفيذ أي تعديل.
* **خصوصية التقرير**: تظهر أنواع المشكلات وأعدادها فقط، دون أسماء أو هواتف أو معرفات أو ملاحظات الطلاب.
* **قبول APK**: يمكن اختيار `debug` للاختبار السريع أو `release` للنسخة المرشحة، مع بصمة SHA-256 وفحص التوقيع عند توفر `apksigner`.
* **بوابة Supabase**: يقبل السكربت ملف CSV من استعلام الجاهزية الحالي ويتوقف عند أي صف غير ناجح.
* **تقرير موحد**: تُحفظ نتيجة التحليل والاختبارات والويب والحزمة والسحابة في ملف قبول داخل `build/release-artifacts`.
* **الجودة**: ارتفع عدد فحوص العقود إلى 36، ولا يوجد SQL جديد.

### 🧭 المرحلة P1.10 (تثبيت الويب والنسخة المرشحة)

* **نسخة التطوير**: `4.2.0-alpha.6+48 (P1.10)`.
* **جودة صفرية التحذيرات**: أصبح ESLint في CI يعمل بـ`--max-warnings 0` بعد إغلاق 23 ملاحظة.
* **تأثيرات React مستقرة**: الحضور والتقارير والحفظ والمراكز والمعلمون والتنقل لا يعيدون الطلبات بسبب اعتماد مفقود.
* **تهيئة قابلة للإلغاء**: القرعة والعائلات والقوالب وخريطة المصحف لا تحدث الحالة بعد مغادرة الشاشة.
* **تصيير آمن**: لوحة التحكم تستخدم حالة عميل/خادم مستقرة بدل تحديث متزامن داخل `useEffect`.
* **بوابات محمية**: صفر سياسات على جداول credentials/sessions نتيجة أمنية مقصودة مع RLS و`REVOKE ALL`، وليست خطأً يحتاج سياسة فتح.
* **الجودة**: ارتفع عدد فحوص العقود إلى 35، ولا يوجد SQL جديد.

### 📱 المرحلة P1.9 (مرونة الشاشات وتكبير الخط)

* **نسخة التطوير**: `4.2.0-alpha.5+47 (P1.9)`.
* **خط النظام الكبير**: صفوف المعلومات والأزرار تتبدل تلقائيًا من أفقي إلى عمودي عند ضيق الشاشة أو تكبير الخط.
* **التسميع والمراجعة**: شريط التقدم وأزرار السابق/التالي/التوقف وملخص السور المختارة لا تتجاوز العرض.
* **الإشعارات والمخالفات**: بيانات الطالب والتاريخ تلتف، وإجراءات التصحيح والحذف أصبحت في قائمة مدمجة.
* **التقارير والطلاب**: عناوين النوافذ وصفوف التفاصيل وأزرار A4/A5 وسجل الحضور أصبحت مرنة.
* **اختبار انحدار**: ثلاثة اختبارات Widget بعرض ضيق ومقياس خط 200%، إضافة إلى بوابة المصدر الرابعة والثلاثين.
* **قاعدة البيانات**: لا يوجد SQL جديد لهذه المرحلة.

### 💾 المرحلة P1.8 (النسخ الاحتياطي الخلفي)

* **نسخة التطوير**: `4.2.0-alpha.4+46 (P1.8)`.
* **نسخ والجوال مغلق**: يسجل Android مهمة دورية تنشئ النسخة المحلية المشفرة بعد الساعة المختارة حتى إذا أُغلق التطبيق بالطريقة العادية.
* **جدولة متزامنة**: تفعيل الميزة أو تغيير الساعة يعيد ضبط المهمة، وتعطيلها يلغيها من نظام Android.
* **مسار احتياطي**: عند تأخير Android للمهمة، يستمر التطبيق في فحص الاستحقاق عند أول فتح بعد الموعد.
* **تشخيص واضح**: الإعدادات ومركز التشخيص يعرضان آخر تشغيل خلفي وحالته وأي خطأ في الجدولة.
* **إصلاح البناء**: قراءة إصدار SQLite تستخدم `PRAGMA user_version` بدل دالة غير موجودة في `sqflite`.
* **قيود النظام**: الموعد هو «بعد الساعة المحددة» وليس منبهًا دقيقًا؛ قد يؤخره توفير البطارية، ولا يعمل بعد «فرض الإيقاف» حتى يعاد فتح التطبيق.
* **قاعدة البيانات**: لا يوجد SQL جديد لهذه المرحلة.

### 🌐 المرحلة P1.7 (تكافؤ التسميع وملف الطالب في الويب)

* **نسخة التطوير**: `4.2.0-alpha.3+45 (P1.7)`.
* **تسميع مفتوح في الويب**: تنقل يدوي بين الآيات عبر السور، تقييم اختياري للآية، وإغلاق الجلسة بزر «التوقف هنا» فقط.
* **النطاقات الأربع**: اقتراح المقرر بالآيات أو الأسطر أو الصفحات أو الأحزاب، مع نهاية الصفحة والحزب.
* **حفظ سحابي ذري**: تحفظ مقاطع السور كلها أو لا يحفظ أي منها، وتُسجل الأخطاء والملاحظة في أول مقطع فقط منعًا لتضخم التقارير.
* **ملف الطالب التفصيلي**: QR والكود والمحفوظ المثبت وآخر حفظ ومراجعة والحضور والغياب والنقاط والخطة والمقرر القابل للتعديل.
* **إجازات مرتبة**: فلاتر الأسبوع والشهر والفترة والحالة والطالب والسبب، مع قوائم أبجدية عربية.
* **الجودة**: 32 فحص عقد، وبناء Next.js ناجح لكل المسارات الثلاثين.
* **قاعدة البيانات**: يلزم نسخ **محتوى** migration P1.7 إلى SQL Editor؛ لا تلصق اسم الملف.

### 📖 المرحلة P1.6 (الحفظ والتسميع المتصل)

* **نسخة التطوير**: `4.2.0-alpha.2+44 (P1.6)`.
* **تسميع مفتوح حقيقي**: يستمر عبر السور في اتجاه حفظ الطالب، ويعتمد المعلم النهاية بزر مستقل.
* **مقرر الطالب**: اقتراح الجلسة من المقرر المحفوظ في ملف الطالب، بأي وحدة من الوحدات الأربع.
* **حفظ ذري**: تقسم الجلسة إلى مقاطع لكل سورة داخل معاملة واحدة دون تضخيم أخطاء الجلسة أو إجمالي الطالب.
* **تسجيل يدوي متصل**: شاشة إضافة الحفظ تستفيد من المقرر نفسه عند عبور نهاية السورة.
* **سلامة الغياب**: «غائب» لا يضيف حفظًا ولا نقاطًا.
* **قاعدة البيانات**: لا يوجد SQL جديد لهذه المرحلة.

### 📖 المرحلة P1.5 (المراجعة المتصلة وخطط الأحزاب)

* **نسخة التطوير**: `4.2.0-alpha.1+43 (P1.5)` منفصلة عن RC2 السابقة.
* **نطاق قرآني متصل**: مقدار الآيات أو الأسطر أو الصفحات أو الأحزاب لا يتوقف قسرًا عند نهاية السورة.
* **مراجعة آمنة**: يتوقف الاختيار عند حدود محفوظ الطالب ولا يقفز فوق سورة أو آيات غير مسجلة.
* **جلسة مرنة**: اختيار وحدة المراجعة ومقدارها وتطبيقها على موضع الاستئناف من الشاشة نفسها.
* **حدود حقيقية**: نهاية الصفحة والحزب تستطيعان إكمال النطاق في سورة أخرى مع حفظ سجل مستقل لكل سورة.
* **خطة الحزب**: وحدة الأحزاب متاحة في الطالب والخطط والنقاط والتكريم والويب والبوابة.
* **Supabase**: migration واحد غير مدمر مطلوب لتوسيع القيود النصية إلى `hizbs`.

### ✅ المرحلة P6.5 (سلامة حزم RC2)

* **تحقق بأمر واحد**: فاحص Windows يقارن ملف ZIP أو APK بملف `.sha256` ويعرض نتيجة واضحة.
* **منع الخلط**: يرفض الفاحص بصمة إصدار آخر حتى لو كان ملفها صالحًا.
* **APK مؤرشف**: فحص RC2 يحفظ APK وملف بصمته معًا باسم الإصدار داخل مجلد مخصص.
* **إجراء واضح**: عند التطابق يُفك الملف، وعند الاختلاف يُرفض ويعاد تنزيله؛ ولا تستبدل البصمة أي ملف في المشروع.
* **جودة**: ارتفعت بوابات التحقق إلى 28، ولا يوجد SQL جديد.

### 🩺 الإصدار v4.1.0-alpha.2 (RC2 — التشخيص الآمن)

* **مركز صحة موحد**: إصدار التطبيق وSQLite والنظام وأعداد السجلات وحالة النسخ والمزامنة واتصال Supabase في شاشة واحدة.
* **تقارير بلا بيانات طلاب**: نسخ ومشاركة تقرير دعم لا يضم الأسماء أو الهواتف أو الملاحظات أو كلمات المرور أو رموز الجلسات.
* **بصمة حادثة**: أخطاء Flutter والمنصة تتحول إلى بصمة مختصرة قابلة للمقارنة دون حفظ الرسالة أو مسار الجهاز أو stack trace.
* **منع التكرار**: الحادثة نفسها لا تسجل أكثر من مرة خلال خمس دقائق.
* **تعافٍ من فشل البدء**: شاشة عربية واضحة برمز حادثة وتحذير من حذف البيانات بدل بقاء التطبيق فارغًا.
* **إصدار مركزي**: `4.1.0-alpha.2+42 (RC2)` مع فحص عقد جديد يرفع بوابات الجودة إلى 27.

> الحالة: جاهز لاختبار الجهاز. لا يوجد SQL جديد؛ افتح الإعدادات > التشخيص وراجع التقرير المنقح قبل مشاركة أي سجل دعم.

### 🧪 الإصدار v4.1.0-alpha (الإطلاق المرحلي P6.3)

* **فحص السحابة من التطبيق**: اختبار DNS وTLS وحالة Supabase من الإعدادات دون أي رفع أو تنزيل.
* **تشخيص عربي**: تمييز فقد DNS والمهلة وخطأ الاتصال الآمن وتعطل الخادم، مع خطوات علاج مناسبة.
* **إعداد بيئي آمن**: دعم تغيير رابط Supabase ومفتاحه العام وقت البناء دون تعديل المصدر.
* **مساران للـAPK**: نسخة staging داخلية، ونسخة production لا تُبنى إلا بمفتاح توقيع خاص وهوية تطبيق دائمة.
* **تحقق الحزمة**: فحص `apksigner`، وبصمة SHA-256، ورموز فك التتبع للإصدارات الإنتاجية.
* **بوابة أقوى**: أصبح CI يبني APK debug بعد `flutter analyze` و`flutter test`، وأضيف فحص P6.3 السادس والعشرون.
* **هوية الحزمة**: الإصدار `4.1.0-alpha.1+41` واسم التطبيق الظاهر «حلقتي»، مع إبقاء هوية الحزمة الدائمة قرارًا مطلوبًا قبل الإصدار العام.
* **قبول موثق**: استعلام قراءة فقط يفحص جاهزية Supabase ونموذج نتائج يغطي العزل والجهازين والمزامنة والطباعة والواجهة وAPK.
* **فحص deny-all صحيح**: جداول تجزئات PIN والجلسات لا تحتاج سياسة عميل؛ يعد الفحص RLS مع صفر سياسات وسحب الصلاحيات نجاحًا أمنيًا بدل إظهاره كفشل.
* **RC1 بأمر واحد**: سكربت PowerShell يشغل التحليل والاختبارات وبناء APK وفحوص الويب ويعرض بصمة الحزمة، مع خيار سريع لتجاوز إعادة فحص الويب عند الضرورة.

> الحالة: أدوات الإطلاق جاهزة برمجيًا. يبقى اختيار `applicationId` الدائم، إضافة أسرار التوقيع، وتنفيذ اختبارات جهازين وحسابين وطابعات فعلية.

### 🛠️ إصلاح تشغيل Android (2026-07-18)

* **إشعارات مرنة**: تتكيف عناوين الإشعارات وأسماء الطلاب مع عرض الهاتف وحجم الخط دون أشرطة overflow.
* **شبكة الإصدار**: أضيف إذن Android `INTERNET` إلى البيان الرئيسي لضمان عمل Supabase في APK النهائي.
* **رسائل DNS**: الرسالة `Failed host lookup` تعني أن الجهاز لم يحوّل نطاق Supabase إلى عنوان شبكة؛ لا تحذف البيانات المحلية ولا تدل بنفسها على خطأ SQL.

### 👨‍👩‍👧‍👦 الإصدار v4.0.1-alpha (حساب ولي الأمر متعدد الأبناء)

* **إصلاح توافق Supabase**: أضيف مخطط `extensions` إلى مسار دوال `pgcrypto` حتى تعمل `gen_random_bytes` و`crypt` و`digest` في المشاريع التي تثبت الإضافة خارج `public`.
* **حزمة تحديث مكتفية ذاتيًا**: أضيفت ملفات سياسة نقاط التسميع ونموذج الطالب ومعاملة الصندوق إلى Hotfix لمنع خلط الخدمات الحديثة مع Models أقدم عند الاستبدال الجزئي.
* **هوية عائلية عالمية**: كود عشوائي ثابت وفريد من 20 خانة بدل اشتقاق كود قصير من UUID.
* **PIN عائلي مشفر**: رقم واحد من ستة أرقام بتجزئة bcrypt وحد محاولات وقفل مؤقت، دون تخزين الرقم الأصلي.
* **تبديل الأبناء**: ولي الأمر ينتقل بين أبنائه النشطين من الحساب نفسه، ولا يقبل الخادم طالبًا خارج العائلة صاحبة الجلسة.
* **جلسة معزولة**: مدة 12 ساعة وبصمة SHA-256 للرمز فقط، مع إلغاء جميع الجلسات عند تغيير PIN أو إيقاف الحساب.
* **توافق دخول الطالب**: بقي دخول الطالب القديم وبيانات جلساته السابقة صالحين، وتحولت الجلسات الجديدة إلى صيغة تميز الطالب عن العائلة.
* **إدارة Android والويب**: تفعيل وتوليد ونسخ وتغيير وإيقاف بيانات ولي الأمر من شاشة العائلة في المنصتين.
* **مزامنة الهوية**: SQLite v18 يحفظ كود العائلة السحابي ولا يستبدله بكود محلي متوقع أثناء المزامنة.

> الحالة: migration وEdge Function والواجهات وفحص العقد وبناء 30 مسارًا جاهزة. يلزم التنفيذ على Supabase مرحلي، وإعادة نشر الوظيفة، واختبار عائلة بطفلين وعائلتين منفصلتين قبل الإنتاج.

### 🏢 الإصدار v4.0.0-alpha (الجهة الإشرافية متعددة المراكز)

* **تسلسل مؤسسي فعلي**: جهة إشرافية ترتبط بعدة مراكز، ويظل كل مركز محتفظًا بمالكه وحلقاته ومعلميه وطلابه.
* **ثلاثة أدوار**: مالك ثابت، ومدير إشرافي، ومحلل للقراءة فقط؛ لا يرث المحلل صلاحية تعديل سجلات المراكز.
* **دعوات مؤقتة**: ربط المركز ودعوة أعضاء الفريق برموز عشوائية لا تخزن القاعدة إلا SHA-256 لها، مع انتهاء صلاحية وعدد استخدامات وربط دعوة العضو ببريده.
* **سجل إشرافي**: توثيق إنشاء الجهة والدعوات وربط المراكز وفصلها وتعديل أعضاء الفريق دون حفظ نص الدعوة أو PIN أو بيانات الطالب.
* **لوحة مجمعة**: أعداد المراكز والحلقات والطلاب، والحضور والغياب، وآيات الحفظ والمراجعة، والنقاط لكل مركز وخلال فترة تصل إلى 366 يومًا.
* **تقرير RTL**: طباعة اللوحة أو حفظها PDF بالعربية، مع ترتيب المراكز حسب الأداء وإظهار تفاصيل كل مركز.
* **إدارة الفريق**: دعوة مدير أو محلل، وتغيير الدور، وإيقاف العضوية وإعادتها مع حماية عضوية المالك من التعديل.
* **توافق مرحلي**: حُفظ `centers.supervisor_id` كمرجع الربط الحالي، ورُحّل مالكو الجهات القديمة تلقائيًا إلى جدول العضويات الجديد.

> الحالة: SQL وواجهة `/supervision` وفحص العقد وبناء 30 مسارًا جاهزة. يلزم تنفيذ migration P7.3 على نسخة مرحلية واختبار مالك مركز ومالك جهة ومحلل بحسابات منفصلة قبل الإنتاج.

### 🔐 الإصدار v3.9.0-alpha (بوابة الطالب وولي الأمر الآمنة)

* **دخول بكود وPIN**: كود الطالب العام ليس سرًا، والرقم السري من ستة أرقام يخزن كتجزئة bcrypt فقط.
* **حماية المحاولات**: حد محاولات بحسب الطالب والعميل، مع قفل مؤقت ورسالة عامة لا تكشف وجود الكود.
* **جلسة قصيرة**: رمز عشوائي لمدة 12 ساعة؛ لا تخزن القاعدة إلا بصمته ويحفظه المتصفح داخل التبويب الحالي.
* **عرض للقراءة فقط**: الخطة والحضور والحفظ والمراجعة والنقاط دون كشف الهاتف أو ملاحظات الإدارة وسجلات التدقيق.
* **فترات مرنة**: 7 و30 و90 و180 و366 يومًا، بتواريخ هجرية وميلادية وطباعة RTL إلى PDF.
* **إدارة المعلم**: تفعيل الرقم السري وتغييره وإيقافه من ملف الطالب في Android أو شاشة الطلاب في الويب.
* **وظيفة خادمية**: كل دخول يمر عبر Edge Function، ولا يوجد مفتاح خدمة في حزمة المتصفح.

> الحالة: الكود والبناء جاهزان. يلزم تنفيذ migration P7.2 ونشر Edge Function وضبط أسرارها واختبار العزل قبل إعطاء الرابط للطلاب.

### 🪪 الإصدار v3.8.0-alpha (هوية الطالب والتقارير العادلة والخطط الجماعية)

* **هوية عالمية**: كود عام مستقل بطول 20 رمزًا مع قيد فريد محلي وسحابي، بينما بقي رمز QR العشوائي الكامل مستقلًا وغير صالح وحده لتسجيل الدخول.
* **QR في التقارير**: يظهر في رأس تقرير الطالب، مع ورقة A4 موحدة لبطاقات جميع الطلاب جاهزة للقص والتوزيع.
* **تقرير إدارة واحد**: ملخص أفقي لجميع الطلاب في صفحة واحدة، إضافة إلى التقرير التفصيلي وملف PDF الجماعي.
* **أشهر هجرية فعلية**: اختيار محرم أو أحد آخر 18 شهرًا هجريًا مع تحويل حدوده إلى التواريخ الميلادية الموافقة، واحترام إعداد التقويم في PDF وواتساب.
* **رسالة ولي الأمر**: نطاق السورة والآيات في الحفظ والمراجعة وعبارة متابعة تطلب ملاحظات الأسرة.
* **نسبة عادلة**: أيام الجمعة وتعليق الدراسة لا تدخل المقام، والاستئذان الخاص بالطالب يبقى محسوبًا، وإتمام المقرر بجودة جيدة يصل إلى تقييم مشجع فوق 90%.
* **نقاط تراكمية**: يجمع النظام كل تسجيلات اليوم عبر السور والجلسات، ويمنح 0–5 للإنجاز و1/2/5 للزيادة حتى سقف 10 نقاط عند مضاعفة المقرر.
* **عقوبات متوازنة**: الغياب بلا عذر أشد من اجتماع التأخر وعدم التسميع وعدم لبس الثوب.
* **الصندوق والمخالفة**: يمكن ربط الغرامة المالية مباشرة بسجل النقاط السلبية الذي تسبب فيها.
* **خطط من ملف الطالب**: مقرر مستقل للحفظ والمراجعة، وإنشاء وطباعة خطط جميع الطلاب دفعة واحدة مع احترام بوابة اختبار التجاوز.
* **تكافؤ الهاتف والويب**: يقرأ الويب كود الطالب ومقرر المراجعة ويحفظهما ضمن ملف الطالب، وتحدّث الخطة النشطة مقرري الحفظ والمراجعة معًا.
* **دليل داخل التطبيق**: شاشة قابلة للبحث تشرح الحضور والتسميع والمراجعة والخطط والتقارير والمزامنة والنقاط.
* **قرعة أدق**: مفتاح مستقل لاستبعاد الغائبين ومفتاح آخر لاستبعاد المستأذنين.
* **Overflow أقل**: عناصر مالية وخيارات طويلة أصبحت قابلة للتمرير أو الالتفاف بدل ظهور الشريط الأصفر والأسود.

> الحالة: SQLite v17 وثلاث migrations سحابية جاهزة، ونجحت 22 بوابة عقد. يلزم تنفيذ SQL بالترتيب وتشغيل `flutter analyze` و`flutter test` ومعاينة QR وA4 على جهاز فعلي.

### 🎨 الإصدار v3.7.0-alpha (هوية موحدة وخفة الواجهات)

* **اتجاه مزامنة واضح**: ثلاثة أوامر مستقلة للرفع فقط، والتنزيل فقط، والرفع ثم التنزيل، مع سهم اتجاه ووقت منفصل لآخر عملية.
* **لا مزامنة صامتة**: تسجيل الدخول لا ينقل البيانات تلقائيًا؛ يختار المعلم اتجاه النقل بعد تحديد المركز والحلقة.
* **تنزيل محمي**: نسخة محلية مشفرة قبل التنزيل، وتوقف آمن إذا لم تُعد عبارة الحماية أو تعذر إنشاء النسخة.
* **نطاق الحلقة**: تنزيل الحضور وخريطة المصحف والخطط يقتصر على طلاب الحلقة المحددة.
* **مراجعة مرتبطة بملف الطالب**: تظهر السور والآيات المسجلة كمحفوظ سابق حتى إن لم يسبق تسجيل تسميع يومي داخل التطبيق.
* **توافق الطلاب القدماء**: يقرأ التطبيق أيضًا المحفوظ المسبق من خريطة المصحف، ويجمعه مع الحفظ اليومي دون تكرار.
* **نطاق آمن**: لا يسمح السلايدر أو حقول الآيات أو اختصارات نهاية الصفحة والحزب بتسجيل مراجعة تتجاوز محفوظ الطالب.
* **دورة مستمرة**: تبدأ المراجعة من أول آية محفوظة في النطاق الجزئي، ثم تستأنف بعد آخر موضع مسجل وتلتف عند نهاية المحفوظ.
* **إصلاح البيانات الناقصة**: إذا وجد إجمالي محفوظ بلا نطاق تفصيلي، يظهر زر لتحديد البداية والنهاية من ملف الطالب ثم تعاد القائمة فورًا.
* **مزامنة أدق**: غياب عمود اختياري قديم في Supabase لم يعد يُسقط بقية حقول تقدم الطالب من طلب المزامنة.
* **هوية هادئة**: أخضر قرآني وعاجي دافئ وذهبي خافت بدل المظهر الرمادي الثقيل، مع نسخة داكنة متوازنة.
* **خط واحد**: Tajawal مضمن داخل Android والويب بأربعة أوزان، فلا تختلط خطوط الواجهة ولا يلزم اتصال لتنزيلها.
* **مكونات أخف**: بطاقات وحدود وأزرار وحقول وقوائم ونوافذ سفلية أكثر هدوءًا واتساقًا.
* **Safe Area شاملة**: حماية مركزية لكل الشاشات والحوارات من أزرار Android ومنطقة الإيماءة، مع دعم PWA و`viewport-fit=cover`.
* **شريط تنقل أوضح**: قائمة جانبية وشريط جوال أخف، وحالة نشطة واضحة بلا تكبير أو ظلال مزعجة.
* **استثناء مقصود**: يظل النص القرآني بخطه المتخصص، بينما تستخدم جميع عناصر الواجهة Tajawal.

> الحالة: التنفيذ مركزي ويغطي المسارات القديمة والجديدة. يلزم فحص بصري على هاتف بأزرار Android الثلاثة وهاتف بالتنقل بالإيماءات، وفي المقاسين الصغير والكبير. يلزم تنفيذ SQL توافق نطاق المحفوظ مرة واحدة على Supabase قبل اختبار المزامنة على جهاز جديد.

### 🔐 الإصدار v3.6.0-alpha (حماية البيانات والنسخ السحابي المشفر)

* **نسخ مشفرة فعليًا**: AES-256-GCM مع عبارة حماية ومفتاح مشتق وملح عشوائي مستقل لكل ملف.
* **استعادة موثوقة**: إصلاح قبول الإصدار `2.1`، والتحقق من الحجم والبنية وسلامة المحتوى قبل استبدال البيانات.
* **مخزن مفاتيح النظام**: عبارة الحماية لا تُحفظ في SQLite ولا في الملف أو سجل التدقيق.
* **سحابة خاصة**: رفع واستعادة الملفات المشفرة من Supabase Storage مع عزل كل حساب وسياسة احتفاظ.
* **سجل تدقيق شامل**: مراقبة حساسة محلية وسحابية للطلاب والحضور والتسميع والنقاط والخطط، دون نسخ البيانات الشخصية إلى السجل.
* **سياسة خصوصية**: صفحات Android/Web تشرح الجمع والاستخدام والمشاركة والاحتفاظ والتصدير والحذف والاستجابة للحوادث.
* **واجهات إدارة**: حالة التشفير وعبارة الحماية والنسخ السحابية وسجل التدقيق من الإعدادات، ومسارا `/privacy` و`/audit-log` في الويب.
* **منع النسخ غير الآمن**: تعطيل Android Auto Backup العام لصالح النسخ المشفر الذي يديره التطبيق.

> الحالة: الكود و19 فحص عقد وبناء 28 مسار ويب ناجحة. يلزم تنفيذ migration P6.2 وتجربة الاستعادة بعبارة صحيحة وخاطئة على جهازين تجريبيين. Flutter SDK غير متاح محليًا وسيُشغّل الاختبار المضاف عبر GitHub Actions.

### 🛡️ الإصدار v3.5.0-alpha (الحماية وبوابات جودة الإصدار)

* **رؤوس حماية**: CSP ومنع iframe وسياسة إحالة وصلاحيات كاميرا مقيدة ومنع كشف نوع الخادم.
* **Supabase آمن**: تحقق من الرابط والمفتاح العام مع توثيق صريح لمنع `service_role` في المتصفح.
* **تعافي الواجهة**: صفحات عربية للخطأ و404 وإعادة المحاولة دون عرض تفاصيل حساسة للمستخدم.
* **اعتمادات مصححة**: تحديث Next.js وSupabase وإزالة جميع ثغرات التدقيق عالية الخطورة المكتشفة.
* **جودة قابلة للقياس**: صفر أخطاء ESLint، خفض الملاحظات من 144 إلى 23، ومنع زيادة الحد في CI.
* **GitHub Actions**: فحص الويب وFlutter والعقود والبناء تلقائيًا، وبناء APK يدوي بعد نجاح analyze والاختبارات.

> الحالة: بوابة الويب الكاملة ناجحة وبناء المسارات الـ26 ناجح. لا يوجد SQL جديد. يلزم تشغيل سير Flutter على GitHub أو جهاز يحتوي SDK، ثم إكمال النسخ السحابي والخصوصية واختبار Supabase المرحلي في P6.2.

### 🎨 الإصدار v3.4.0-alpha (نظام التصميم وتجربة الاستخدام)

* **ثيم شامل**: اتساق فعلي للوضعين الفاتح والداكن في البطاقات والحقول والأزرار والحوارات والتنقل.
* **مكونات قابلة لإعادة الاستخدام**: رؤوس صفحات وبحث وحالات فارغة ومؤشرات وبطاقات طلاب موحدة في Android والويب.
* **شاشات يومية أوضح**: تحديث الطلاب والحضور والتقارير والتسميع مع تباعد واستجابة وتسلسل بصري موحد.
* **تنقل جوال عملي**: الرئيسية والطلاب والحضور والحفظ والتقارير في الشريط السفلي، وبقية الأقسام في القائمة الجانبية.
* **إتاحة أفضل**: تسميات للأزرار وحالة الصفحة الحالية وتركيز لوحة المفاتيح واحترام تقليل الحركة.

> الحالة: نظام التصميم الأساسي وتطبيقه على الشاشات اليومية جاهز، وبناء الويب ناجح. يلزم مراجعة بصرية على هاتف Android وشاشات ويب مختلفة قبل الإطلاق. لا يوجد SQL مطلوب.

### 🧭 الإصدار v3.3.0-alpha (خريطة المصحف والرصد الرقمي للاختبارات)

* **خريطة حقيقية**: اختيار أرباع المصحف بصريًا حسب 60 حزبًا × 4 أرباع وتوليد الأسئلة من المحدد فقط.
* **نطاق متعدد السور**: يمكن أن تستمر إجابة السؤال إلى السورة التالية داخل الحزب، كما يدعم المحرر نهاية الصفحة أو الحزب.
* **رصد أثناء الاختبار**: عدادات أخطاء الحفظ والتشكيل والتلاوة والتنبيه مع درجة آلية من 10.
* **قالب محفوظ**: يحتفظ السؤال بنهاية السورة والرصد والدرجة محليًا وفي Supabase.
* **PDF مكتمل**: يطبع كود الطالب والرصد الفعلي والدرجة الإجمالية، ويترك الخانات فارغة قبل بدء التقييم.
* **بيانات المصحف**: تصحيح ربع سورة يونس وإضافة فحص آلي يضمن 240 ربعًا متسقة مع الأحزاب.
* **توافق السحابة**: ملف SQL مستقل يعالج خطأ غياب `memorization.halaqa_id` قبل ترحيل P5.6.

> الحالة: المنطق وSQLite v13 وSQL وفحوص العقود وبناء الويب جاهزة. يلزم تشغيل Flutter واختبار A4/A5 ومزامنة قالب متعدد السور على قاعدة مرحلية قبل الإنتاج.

### 🎲 الإصدار v3.2.0-alpha (القرعة المستمرة وسجل الويب المتزامن)

* **قرعة لكل حلقة**: تحفظ الطالب الحالي والمسحوبين والمستبعدين ونتيجة السحب حتى بعد التنقل.
* **دفعة واحدة**: ترتيب مرقم كامل للتسميع مع منع تكرار الطالب.
* **حضور اليوم**: استبعاد الغائب والمستأذن اختياريًا.
* **سجل واضح**: فلاتر الطالب والحفظ/المراجعة والفترة، مع تعديل وحذف مؤكد.
* **تقارير صحيحة**: يسجل الويب التقييم والحفظ في عملية ذرية واحدة حتى يدخل الإنجاز في تقارير الفترة.
* **مزامنة آمنة**: أحدث تعديل يفوز، والحذف اللين لا يعود عند مزامنة Android.
* **خريطة صحيحة**: يعاد بناؤها بعد تصحيح السجل بدل إبقاء متوسط قديم.

> الحالة: الويب وفحوصات العقد والبناء جاهزة. يجب تنفيذ migration P5.6 على Supabase مرحليًا وتشغيل فحوص Flutter واختبار المزامنة بجهازين قبل الإنتاج.

### 📊 الإصدار v3.1.0-alpha (تقرير الحلقة وصور المشاركة)

* **فترة حقيقية**: تقرير يومي أو أسبوعي أو شهري أو مخصص من محرك سجلات الفترة نفسه.
* **ملخص شامل**: الحفظ والمراجعة والصفحات والأجزاء والحضور والنقاط ومستوى الحلقة.
* **صورة مشاركة**: بطاقة PNG عربية احترافية للمشاركة مع الإدارة أو فريق الحلقة.
* **PDF تجميعي**: جدول الطلاب والمتميزون والتنبيهات بتصميم من اليمين إلى اليسار.
* **لا أرقام وهمية**: إزالة نسب الاختبارات والتحسن التجريبية من الويب.
* **متابعة عادلة**: لا يصنف الطالب بلا بيانات على أنه متعثر تلقائيًا.

> الحالة: Android والويب والمنطق وPDF جاهزة برمجيًا؛ يلزم اختبار التقاط الصورة والمشاركة والطباعة على جهاز فعلي. لا يوجد SQL مطلوب.

---

### 👨‍👩‍👧‍👦 الإصدار v3.0.0-alpha (العائلات وأولياء الأمور)

* **عائلة مستقلة**: اسم ورمز ثابت ومرجع للجد أو الفرع لتمييز العائلات المتشابهة.
* **عدة طلاب**: ربط الإخوة والأقارب بالعائلة نفسها ونقل الطالب بإجراء صريح.
* **عدة أولياء**: الأب والأم والأخ والجد والعم/الخال وولي آخر، مع جهة اتصال أساسية واحدة.
* **توافق آمن**: رقم الولي الأساسي يحدّث الحقل القديم المستخدم في الرسائل والتقارير.
* **واجهة حقيقية**: استبدال بيانات الويب التجريبية ببيانات Supabase الفعلية.
* **حماية النطاق**: تمنع قاعدة البيانات ربط طالب بعائلة من مركز أو حلقة أخرى.

> الحالة: Android والويب وSQLite والمزامنة وmigration جاهزة برمجيًا. نفّذ SQL على قاعدة تجريبية ثم اختبر عائلتين متشابهتين وطالبًا ينتقل بينهما قبل الإنتاج.

---

### 🧾 الإصدار v2.9.0-alpha (التقارير العربية والتصدير الجماعي)

* **اتجاه عربي صحيح**: عكس بصري فعلي لأعمدة PDF وتنسيق كامل من اليمين إلى اليسار.
* **رأس تقرير أوضح**: كود طالب مختصر وآمن، وفترة التقرير، ونسبة مستوى بارزة بالألوان.
* **تصدير دفعة واحدة**: ملف واحد لكل الطلاب مع صفحة مستقلة لكل تقرير واختيار A4 أو A5.
* **تحكم بالنطاق**: فترة مخصصة وخيار الاقتصار على الطلاب النشطين أو تضمين الأرشيف.
* **قائمة جانبية**: وصول سريع إلى الشاشات اليومية والإدارية من التبويبات الرئيسية.

> الحالة: التنفيذ البرمجي وفحوصات العقد جاهزة. يلزم معاينة PDF عربي على جهاز واختبار طباعة A4/A5. لا يوجد SQL مطلوب لهذا الإصدار.

---

### ✨ الإصدار v2.8.0-alpha (متميزو اليوم)

* **اكتشاف تلقائي**: من تجاوز مقرره اليومي يظهر تلقائيًا حسب وحدة الآيات أو الصفحات أو الأسطر.
* **دقة الحساب**: استبعاد المراجعة ومنع تكرار الآيات عند تداخل السجلات.
* **إضافة المعلم**: إدراج طالب مجتهد بسبب يدوي موثق حتى لو لم تكن الزيادة رقمية.
* **مكافآت متنوعة**: نقاط أو شهادة أو هدية أو وجبة جماعية أو تكريم آخر.
* **ملخص الطالب**: إنجاز اليوم وآخر حفظ ومراجعة والخطة النشطة وتعديل المقرر.

> الحالة: Android والويب والتخزين والمزامنة جاهزة برمجيًا؛ يلزم تنفيذ migration واختبار الوحدات الثلاث على جهاز وبيانات تجريبية.

---

### 🗂️ الإصدار v2.7.0-alpha (أرشيف الطلاب وتدقيق النقاط)

* **أرشيف آمن**: المفصولون والمتخرجون والسابقون خارج القوائم اليومية دون حذف بياناتهم.
* **سجل حالة**: سبب وتاريخ لكل فصل أو أرشفة أو إعادة تفعيل، بما فيها الإجراءات التلقائية.
* **هوية واضحة**: الاسم وآخر أرقام ولي الأمر قبل إسناد المكافأة أو المخالفة.
* **تصحيح موثق**: نقل السجل إلى الطالب الصحيح أو حذفه بسبب إلزامي مع الاحتفاظ بأثر التدقيق.
* **مزامنة صحيحة**: بقاء المخالفة سالبة في Supabase وإصلاح السجلات التاريخية المتعارضة.

> الحالة: Android والويب وSQLite وmigration السحابية جاهزة برمجيًا؛ يجب تنفيذ SQL على قاعدة تجريبية واختبار طالبين متشابهين قبل الإنتاج.

---

### 🛡️ الإصدار v2.6.0-alpha (الإعدادات وحماية النسخ)

* **أقسام واضحة**: تنقل مباشر بين إعدادات الحلقة والأوقات والقواعد والمظهر والبيانات ومعلومات التطبيق.
* **تذكير مرن**: تذكير يومي أو كل ثلاثة أو سبعة أيام إذا لم توجد نسخة حديثة.
* **نسخ تلقائي محلي**: أول فتح بعد الوقت المختار ينشئ نسخة واحدة لليوم.
* **سياسة احتفاظ**: الاحتفاظ بآخر 7 أو 14 أو 30 نسخة تلقائية دون المساس بالنسخ اليدوية.
* **حالة مفهومة**: تاريخ آخر نسخة وعدد النسخ وحالة اتصال Supabase بدل مزامنة وهمية.

> الحالة: المنطق والربط موثقان ومغطّيان بفحص عقد؛ يلزم اختبار إنشاء واستعادة على جهاز Android. النسخ والجهاز مغلق والنسخ السحابي ليسا ضمن هذا الإصدار.

---

### 🔁 الإصدار v2.5.0-alpha (استمرارية المراجعة)

* **استئناف تلقائي**: تبدأ المراجعة من الآية التالية لآخر موضع مسجل.
* **دورة واضحة**: الانتقال إلى السورة المحفوظة التالية حسب الترتيب المختار.
* **خطة المراجعة**: مقدار مقترح بالآيات أو الصفحات أو الأسطر من الخطة النشطة.
* **اختصارات**: نهاية الصفحة أو الحزب أو السورة كاملة.
* **حفظ ذري**: السجل والتقييم وملخص اليوم يحفظون معًا أو يتراجعون معًا.

> الحالة: منطق الاستمرارية والحفظ جاهز ومغطى بفحوصات العقد؛ يلزم اختبار Flutter وخريطة المصحف على جهاز فعلي.

---

### ▶️ الإصدار v2.4.0-alpha (التسميع المفتوح)

* **بداية فقط**: يحدد المعلم آية البداية دون فرض نهاية مسبقة.
* **التوقف هنا**: تعتمد الآية الحالية نهاية للجلسة بعد تقييمها.
* **تصحيح الرجوع**: لا تدخل تقييمات الآيات الواقعة بعد موضع التوقف في النتيجة.
* **حدود المصحف**: اختيار سريع إلى نهاية الصفحة أو الحزب في Android والويب.
* **Supabase**: إصلاح migration الخطط ليتوافق مع قاعدة لا تحتوي دوال الصلاحيات السابقة.

> الحالة: فحوصات العقود والويب جاهزة؛ يحتاج التسميع المفتوح واختصارات الحدود إلى اختبار Flutter على جهاز فعلي.

---

### 🎯 الإصدار v2.3.0-alpha (الخطط واختبار التجاوز)

* **خطط دائمة**: أسبوعية وشهرية محفوظة في SQLite وSupabase بدل ذاكرة الواجهة.
* **تعديل مباشر**: تغيير الوحدة والفترة والمقادير والتواريخ والملاحظات، مع أزرار زيادة ونقصان.
* **بوابة تجاوز**: لا تبدأ خطة جديدة حتى ينجح الطالب في اختبار لاحق لإكمال خطته السابقة.
* **طباعة**: جدول يومي للخطة على A4 أو كاشير 80 مم في Android والويب.
* **مزامنة آمنة**: أحدث تعديل يفوز، وحذف الويب لا تعيده مزامنة الهاتف.

> الحالة: الكود جاهز وفحوصات الويب ناجحة. يجب تنفيذ migration الخطط المرفق على Supabase مرحليًا ثم اختبار الطباعة وAndroid قبل الإنتاج.

---

### 📚 الإصدار v2.2.0-alpha (سجل التسميع والتصحيح)

* **سجل واضح**: الحفظ والمراجعة مع البحث والطالب والنوع والفترة.
* **تصحيح السجل**: تعديل السورة والآيات والتاريخ والتقييم والنوع والملاحظات.
* **حذف آمن**: تأكيد ثم إعادة حساب اليوم وإجمالي المحفوظ وخريطة المصحف.
* **حماية المحفوظ السابق**: تطبيق فرق السجل فقط دون تصفير الرصيد التاريخي غير المفصل.
* **مزامنة الحذف**: منع عودة السجلات المحذوفة من Supabase.

> الحالة: التنفيذ البرمجي جاهز؛ يلزم تشغيل اختبارات Flutter وتجربة التصحيح على نسخة بيانات تجريبية قبل الإنتاج.

---

### 🛡️ الإصدار v2.1.0-alpha (الإجازات والانضباط)

* **إجازة جماعية**: اختيار عدة طلاب لنفس السبب والفترة، وحفظ ذري بلا سجلات جزئية.
* **إيقاف التسميع**: يبقى الطالب قابلًا للتحضير، بينما يُغلق الحفظ والمراجعة مؤقتًا بسبب موثق.
* **تنبيه متتالٍ**: تذكير بعد يومين دراسيين متتاليين دون تسميع، وبعد انتهاء الدوام فقط.
* **فصل تلقائي**: تطبيق حد الغياب الموجود في الإعدادات مع استثناء الجمعة وتعليق الدراسة.
* **حماية البيانات**: النسخ الاحتياطي والمزامنة السحابية وسياسات RLS تشمل سجلات الإيقاف.

> الحالة: التنفيذ البرمجي جاهز؛ يلزم اختبار Flutter وقاعدة Supabase مرحلية قبل التطبيق الإنتاجي.

---

### 📊 الإصدار v2.0.0-alpha (تقارير الفترة)

* **فترة مرنة**: تقرير أسبوعي أو شهري أو من تاريخ إلى تاريخ لطالب محدد.
* **تفاصيل كل يوم**: الحضور والحفظ والمراجعة والنقاط والإجازة وتعليق الدراسة والأيام بلا سجل.
* **إنجاز المصحف**: الآيات والأسطر وما يعادلها من صفحات وأجزاء، مع متوسط جودة التسميع.
* **الأداء**: مؤشر عام وأشرطة يومية قابلة للطباعة.
* **التصدير**: PDF بمقاسي A4 وA5 وقالب WhatsApp منسق للفترة نفسها.

> الحالة: منطق الفترة والواجهة والطباعة جاهزة برمجيًا؛ يلزم تشغيل Flutter واختبار A4/A5 ومشاركة WhatsApp على جهاز فعلي.

---

### 📝 الإصدار v1.9.0-alpha (اعتماد نماذج الاختبارات)

* **تحرير الأسئلة**: ترتيب السؤال أو تعديل نطاقه ونوعه أو استبداله أو حذفه قبل الاعتماد.
* **سجل النماذج**: حفظ دائم مع إعادة الفتح والتعديل والحذف والطباعة بمقاسي A4 وA5.
* **نسخ احتياطي**: تدخل القوالب وأسئلتها ضمن النسخة الكاملة، مع قبول النسخ الأقدم التي لا تحتويها.
* **مزامنة آمنة**: جداول Supabase مرتبطة بالطالب والحلقة وتخضع لسياسات RLS التراكمية.

> الحالة: التنفيذ البرمجي جاهز، لكن migration السحابي لم يطبق على الإنتاج، وتحليل Flutter والطباعة الفعلية يحتاجان جهاز تطوير.

---

### 📝 الإصدار v1.8.0-alpha (مولد الاختبارات)

* **فئات التوليد**: محفوظ الطالب، الجزء، الحزب، أو سورة ونطاق آيات.
* **معايير الأسئلة**: العدد، الصعوبة، طول الإجابة التقريبي بالأسطر، ومنع التكرار.
* **المعاينة**: بداية السؤال وموقعه وإظهار الإجابة الكاملة قبل الطباعة.
* **الطباعة**: نموذج A4 أو A5 بأعمدة رصد أخطاء الحفظ والتشكيل والتلاوة والتنبيه والدرجة.

> الحالة: نسخة Android الأولى جاهزة برمجيًا، وتحتاج اختبار Flutter وطابعة فعلية؛ اكتمل الاعتماد والتحرير في الإصدار التالي.

---

### 🔐 الإصدار v1.7.0-alpha (الأمان والحضور الموحّد)

*التركيز الأساسي: منع تجاوز صلاحيات الحلقة وتوحيد بطاقات الحضور بين الهاتف والويب.*

* **دعوات المعلمين**:
  * دعوة أحادية الاستخدام مرتبطة بالبريد، بصلاحية 14 يومًا، ولا تقبل معرّف مستخدم قادمًا من الجهاز.
* **عزل الحلقات**:
  * المعلم يرى ويعدل طلاب حلقته فقط، بينما تبقى الإدارة الشاملة لمدير المركز.
* **QR موحد**:
  * Android والويب يطبعان العقد نفسه باستخدام رمز عشوائي للطالب، مع قراءة بطاقات Android القديمة.
* **حضور بلا تكرار**:
  * قاعدة البيانات ومسار الويب يضمنان سجلًا واحدًا للطالب في التاريخ نفسه.
* **بناء ويب مستقل**:
  * لا يحتاج البناء إلى تنزيل Google Fonts، ونجح إنتاج جميع مسارات Next.js.

> الحالة: الكود وmigration جاهزان، لكن تطبيق migration في الإنتاج مؤجل حتى اجتياز اختبار قاعدة مرحلية وصلاحيات حسابات متعددة.

---

### 📖 الإصدار v1.6.0-alpha (ثبات الحفظ والمراجعة)

*التركيز الأساسي: منع اختلاف موضع الطالب وحساب المقرر بطريقة صحيحة مهما كانت وحدته.*

* **محرك المقرر الموحد**:
  * يحسب نهاية المقرر والزيادة للآيات والصفحات والأسطر من بيانات المصحف نفسها.
  * يمنع تكرار مكافأة الزيادة في اليوم نفسه.
* **سلامة إجمالي المحفوظ**:
  * لا يزيد الإجمالي عند إعادة تسجيل آيات محفوظة، مع الاحتفاظ الكامل بالسجل الجديد لأغراض التدقيق.
* **نقطة الحفظ التالية**:
  * اختيار أبعد موضع وصل إليه الطالب والانتقال الموثوق للسورة التالية حسب اتجاهه.
* **نطاق الآيات**:
  * تزامن ثنائي بين حقلي البداية والنهاية وشريط التحديد، مع منع أي رقم خارج السورة.
* **المراجعة بعد إتمام السورة**:
  * اكتشاف اكتمال السورة من تغطية جميع آياتها، ثم جعل مراجعتها كاملة إلزامية حتى تسجيلها.
* **ترتيب الطلاب**:
  * ترتيب مركزي بالاسم في Android، وترتيب عربي في حالة الويب.
* **النقاط والقرعة (Android)**:
  * تأكيد اسم الطالب والسبب والقيمة قبل تسجيل النقاط، وإمكانية حذف الإدخال المحلي الخاطئ بتأكيد مستقل.
  * نتيجة قرعة مستمرة عند مغادرة الشاشة، وخيار استبعاد الغائبين، وترتيب كامل بقرعة واحدة.

> الحالة: منطق الأعمال واختبارات الوحدة جاهزة، ويظل اختبار قبول Android مطلوبًا بسبب عدم توفر Flutter SDK في بيئة العمل الحالية.

---

### 🛡️ الإصدار v1.5.0-alpha (سلامة البيانات والاستعادة)

*التركيز الأساسي: إيقاف فقد المحفوظ بعد المزامنة وإعادة خريطة المصحف وإصلاح المظهر والنسخ الاحتياطي قبل إضافة ميزات جديدة.*

* **حماية بيانات الطالب أثناء المزامنة**:
  * مزامنة إجمالي المحفوظ ونطاق المحفوظ السابق وعدم استبدالهما بقيم فارغة.
  * نسخة احتياطية كاملة قبل أول مزامنة يومية.
* **إعادة خريطة الأحزاب والأثمان**:
  * مدخل واضح من تفاصيل الطالب وإصلاح الخلايا المفقودة من النطاق المحفوظ دون مسح التقييمات.
* **سلامة بيانات القرآن**:
  * فصل البسملة رقم 0 عن عداد الآيات المرقمة وإضافة فحص 114 سورة و6236 آية و604 صفحات.
* **المظهر**:
  * الوضع حسب الجهاز أو الفاتح أو الداكن، مع حفظ الاختيار واستعادته.
* **النسخ الاحتياطي v2**:
  * حفظ جميع جداول SQLite ومنع النسخ القديمة من حذف البيانات التي لا تحتوي عليها.
* **Supabase**:
  * migration تراكمي آمن لحقول تقدم الطالب والحقول المشتركة بين Android والويب.

> الحالة: منفذ برمجيًا، ويحتاج اختبار قبول على جهاز Android وقاعدة Supabase تجريبية قبل تطبيقه على البيانات الحقيقية.

---

### 🟥 الإصدار v0.1.0-alpha (الهيكل والنواة الأساسية)
*التركيز الأساسي: بناء هيكلية البيانات المحلية والوظائف الأساسية لإدارة الطلاب والحضور البسيط.*

*   **إدارة الطلاب المحلية (SQLite)**:
    * إمكانية إضافة، تعديل، وحذف الطلاب محلياً على الجهاز.
    * حفظ بيانات الطالب: الاسم، رقم الهاتف، رقم هاتف ولي الأمر، وخطة الحفظ اليومية.
*   **خطة التحفظ والمقرر اليومي**:
    * دعم ثلاثة أنواع للمقرر اليومي: بالآيات، بالأسطر، أو بالصفحات.
*   **نظام التحضير البسيط (Attendance)**:
    * تسجيل الحضور والغياب اليومي للطلاب يدوياً وحفظ السجلات محلياً.
*   **تسجيل التسميع الأساسي**:
    * واجهة لاختيار السورة ونطاق الآيات لحفظ وتسميع الطالب وتحديد جودة الحفظ (ممتاز، جيد، مقبول، إلخ).
*   **تصميم مخصص للمحمول**:
    * واجهة تطبيق مستجيبة ومريحة تعتمد على الألوان الهادئة (اللون الزيتي/الأخضر الداكن لتمثيل بيئة الحلقات).

---

### 🟨 الإصدار v0.5.0-beta (المزامنة السحابية والصندوق المالي)
*التركيز الأساسي: تفعيل المزامنة السحابية الفورية، نظام الصندوق المالي، حسابات المصحف التفاعلية وتوليد تقارير الإنجاز الفاخرة.*

*   **المزامنة السحابية ثنائية الاتجاه (Supabase Client)**:
    * دمج نظام التحقق وتسجيل الدخول للمعلمين (بالبريد أو كود المعلم).
    * نظام مزامنة ذكي وتلقائي يدمج التعديلات المحلية بالسحابة ثنائية الاتجاه مع تجنب التكرار بفضل معرّفات UUID المستندة إلى الهاش (Hash-based UUIDs).
    * إمكانية العمل الكامل في وضع عدم الاتصال (Offline Mode) ومزامنة التغييرات المعلقة فور عودة الإنترنت.
*   **نظام صندوق الحلقة المالي (Center Fund)**:
    * إدارة كاملة للمقبوضات والمدفوعات الخاصة بصندوق الحلقة.
    * تخصيص رمز العملة العالمي (ر.س، $، د.إ، إلخ) من شاشة الإعدادات وانعكاسه في شاشات الإحصاءات والويب.
*   **بطاقة التقرير اليومي الفاخرة (Daily Report Sharing)**:
    * توليد صورة بطاقة تقييم مميزة باستخدام الـ Canvas المدمج، تحتوي على درجات الطالب وتفاصيل أخطائه وخلفية وتصميم إسلامي فاخر لمشاركتها فوراً عبر WhatsApp مع ولي الأمر.
*   **واجهة التسميع المباشر المتكاملة (Interactive Recitation Screen)**:
    * شاشة تفاعلية تمكن المعلم من تسميع الآيات للطالب آية بآية، مع ميزة إخفاء وعرض النص القرآني لدعم التدقيق وحفظ الملاحظات وعدد الأخطاء وتقييم كل آية بشكل مستقل.

---

### 🟩 الإصدار v0.9.0-beta (تجربة الاستخدام الفائقة والضبط المطور)
*التركيز الأساسي: معالجة كافة ثغرات الاستخدام (QoL) لضمان السرعة الفائقة للمعلم والتفاعل الأفضل ونظافة قاعدة البيانات.*

*   **التحضير التلقائي الفوري (Auto-Attendance)**:
    * بمجرد حفظ تسميع (حفظ أو مراجعة أو تسميع مباشر)، يتم حضور الطالب تلقائياً للقمة الفاعلية واختصار وقت المعلم.
*   **مؤقت التسميع وتقدير الوقت المقترح**:
    * عرض Stopwatch تفاعلي لتوقيت التسميع، مع احتساب وقت مرجعي مقترح آلياً مستنداً لعدد الصفحات المقررة (مثال: صفحتان = 3-4 دقائق) استناداً لخوارزمية `QuranService`.
*   **فرز الطلاب المرن**:
    * فرز وتصفية الطلاب إما أبجدياً أو حسب إجمالي مقدار المحفوظ الكلي لسرعة الوصول.
*   **تسجيل الطلاب بنطاق السور الحقيقي**:
    * إلغاء Slider الأجزاء العشوائي واستبداله باختيار دقيق (من سورة كذا إلى سورة كذا) وتأثير ذلك مباشرة في تظليل خريطة تقدم المصحف (`mushaf_progress`) بمعدل دقة 100% في الأحزاب والأثمان.
    * لوحة إحصاءات ديناميكية فورية عند تعيين النطاق تعرض إجمالي السور والآيات والصفحات للنسبة المحددة.
*   **الربط العائلي الذكي ومقترحات الإخوة**:
    * اقتراح ذكي وتلقائي بمجرد كتابة كنية أو لقب الطالب للربط مع هاتف ولي الأمر المشترك واستكمال الاسم الأخير فوراً.
*   **الفلترة التفاعلية للتحضير وديناميكية التأخر وثبات التمرير**:
    * فرز فوري لقائمة التحضير بنقرة واحدة على شارات الداشبورد (الكل، حاضر، غائب، مستأذن، متبقي).
    * إدراج حالة الطالب "مستأذن" (`excused`) لحفظ أسباب الغياب المقبول.
    * تحديد وحساب التأخر تلقائياً ومقارنته بزمن بدء الحلقة المسجل في الإعدادات.
    * ثبات موضع شريط التمرير (Scroll Position) في قائمة التحضير دون العودة للأعلى عند تحديث حضور أي طالب.
*   **دعم وحماية الأبعاد وشاشات النوتش (SafeArea)**:
    * تغليف كافة واجهات التطبيق الرئيسية بـ `SafeArea` لضمان ملاءمتها الكاملة لجميع الهواتف الحديثة.
    * تغليف نافذة خيارات الطالب (Bottom Sheet) بـ `SafeArea` لمنع تداخل أزرار النظام والـ Home Indicator مع خيار "تسجيل الحفظ".

---

### 🟦 الإصدار v1.0.0-rc1 (إصدار ما قبل الإطلاق والاستقرار)
*التركيز الأساسي: فحص واستقرار الكود البرمجي، التحقق النهائي وتوثيق الميزات.*

*   **تحديد دقيق للمحفوظ المسبق بالسور والآيات**:
    * ترقية أداة تحديد الحفظ الأولي للطلاب الجدد، بحيث يدعم اختيار آية البداية (من أول السورة افتراضياً) وآية النهاية بدقة متناهية، وبناء تقدم المصحف الفعلي في قاعدة البيانات تلقائياً.
*   **الربط التلقائي والذكي للإجازات والتحضير**:
    * عند حلول يوم إجازة معتمدة لطالب، يتم تحضيره تلقائياً كـ "مستأذن" (Excused) وتثبيت ملاحظة الإجازة.
    * عند محاولة تحضير طالب كـ "مستأذن" يدوياً دون وجود إجازة مسجلة له، يتم إظهار نافذة منبثقة سريعة لتسجيل الإجازة وتحديد السبب لحفظ إحصائيات الغياب المبرر بدقة.
    * إظهار شارة برتقالية مميزة `🌴 إجازة: [السبب]` على بطاقات الطلاب في شاشة الحضور والغياب.
*   **حلول الـ Overflow (تداخل العناصر)**:
    * معالجة كافة مشاكل الـ Overflow وتداخل نصوص القوائم المنسدلة في الشاشات الصغيرة لضمان توافق واستقرار واجهات التطبيق.
*   **نزاهة الكود البرمجي**:
    * التحقق الكامل من عدم وجود أخطاء برمجية أو لغوية وخروج التطبيق بـ zero-errors عبر `flutter analyze`.
*   **توثيق إلزامي بعد التطوير (Continuous Docs Rule)**:
    * تحديث قواعد العمل في ملف [AGENTS.md](file:///c:/Users/salman/Documents/flutter_App/Halaqah/AGENTS.md) بإلزام المطورين بتوثيق الميزات في هذا الملف بعد كل تعديل.

---

### 🟪 الإصدار v1.2.0 (التخصيص والذكاء اللغوي وترقية المحفوظ المسبق)
*التركيز الأساسي: إعطاء التطبيق مرونة تامة للتكيف مع جنس الحلقة، وتعديل المحفوظ المسبق للطلاب المسجلين، وتسهيل الإدخال عبر جهات الاتصال والمعالج الترحيبي.*

*   **تعديل المحفوظ المسبق للطلاب الحاليين**:
    * إضافة أعمدة النطاق الأربعة لقاعدة البيانات (`version 5`) في جدول `students`.
    * إمكانية تعديل وتعديل نطاق السور والآيات للطلاب الحاليين من واجهة تعديل بيانات الطالب.
    * إعادة احتساب `totalMemorized` تلقائياً ومسح وإعادة إنشاء تقدم خريطة المصحف المسبق بدقة متناهية.
*   **معالج الإعداد الأولي والترحيب (Onboarding Wizard)**:
    * شاشة إعداد ترحيبية وجميلة تظهر عند تشغيل التطبيق لأول مرة لتهيئة اسم الحلقة والمسجد والاسم ورقم جوال المعلم، ووقت الحلقة، وجنسها، وصيغة الوقت.
    * حماية انطلاق التطبيق للتأكد من المرور بالتهيئة وتخزين مفتاح الحفظ `setup_completed = 'true'`.
*   **التكيف اللغوي التلقائي مع جنس الحلقة (Gender-Adaptive UI)**:
    * مواءمة لغوية ذكية لجميع نصوص وعناوين وحالات الحضور في الشاشات الرئيسية (الطلاب، تفاصيل الطالب، التحضير، والصفحة الرئيسية) بناءً على كونها حلقة بنين أو بنات (طالب/طالبة، حاضر/حاضرة، غائب/غائبة، مستأذن/مستأذنة، متبقي/متبقية، وصل/وصلت، متأخر/متأخرة).
*   **تعدد تنسيقات الوقت المتاحة**:
    * تعميم تنسيق 12 ساعة (ص/م) وتنسيق 24 ساعة والتنسيق التلقائي المعتمد على الهاتف في جميع الشاشات.
*   **استيراد الأرقام من جهات الاتصال بضغطة زر**:
    * دمج حزمة `flutter_contacts` وتعيين الصلاحيات الخاصة بـ Android و iOS لطلب الإذن واستيراد رقم الجوال للطلاب وأولياء أمورهم تلقائياً دون كتابة يدوية.

---

### 🟫 الإصدار v1.3.0 (التوقيت الديناميكي المرن وإدارة الإجازات المتقدمة)
*التركيز الأساسي: أتمتة وقت بدء الحلقة وتأخر الطلاب تبعاً للموقع الجغرافي وحركة الشمس وفصول السنة بالربط مع مواقيت الصلاة الفعلية ومراعاة رمضان المبارك، مع إتاحة تعديل إجازات الطلاب.*

*   **جدولة أوقات الحلقة الديناميكية المعتمدة على الصلوات**:
    * ربط بداية الحلقة ونهايتها بمواقيت الصلاة اليومية (مثلاً: بدء الحلقة بعد العصر بـ 15 دقيقة، أو بعد الفجر بـ 30 دقيقة).
    * احتساب مواقيت الصلاة محلياً بالكامل (بدون إنترنت) بالاعتماد على معادلات فلكية من خلال حزمة `adhan`.
    * قاعدة بيانات جغرافية مدمجة للمدن العربية والإسلامية الكبرى، مع تركيز خاص ودقيق على جميع المدن اليمنية (صنعاء، عدن، تعز، المكلا، الحديدة، إب، سيئون، صعدة، عتق، دوعن، حجة) مع خطوط الطول والعرض والتقويم المناسب لكل منها.
    * إمكانية تعيين إحداثيات مخصصة (Latitude & Longitude) يدوياً لأي مدينة غير مدرجة.
    * دعم كامل لتقويم أم القرى (Umm Al-Qura) كخيار فلكي رئيسي ومعتمد لجميع الدول والمدن.
*   **توقيت رمضان المبارك الذكي والقبل/بعد صلوات**:
    * الكشف التلقائي عن شهر رمضان بناءً على التقويم الهجري (الشهر التاسع) أو التفعيل اليدوي لوضع رمضان.
    * إعدادات مستقلة لتوقيت رمضان تتيح ربطها بالصلوات المخصصة (مثال: بعد صلاة الفجر، بعد صلاة العصر، قبل صلاة الظهر) أو ساعات ثابتة مخصصة أو استخدام التوقيت المعتاد.
*   **عرض أوقات البدء الذكية وحساب التأخير ديناميكياً**:
    * تعديل حساب التأخر (`isLate`) ليقارن حضور الطالب بوقت البدء الفعلي والنسبي المحسوب لليوم المحدد.
    * إظهار وقت بدء حلقة اليوم المحسوب ومصدره اللغوي بشكل واضح أسفل محدد التاريخ في شاشة التحضير (مثال: "وقت بدء اليوم: 3:30 م (نسبةً إلى صلاة العصر +15 د)").
*   **إدارة وتعديل الإجازات المبررة (التطبيق والويب)**:
    * توفير إمكانية تعديل تفاصيل وتواريخ وأسباب وملاحظات الإجازات المسجلة للطلاب دون الحاجة لحذفها وإعادتها من خلال واجهة التعديل التفاعلية على التطبيق والويب.
    * إضافة زر التعديل (`Icons.edit_outlined` في التطبيق وأيقونة القلم في الويب) بجانب زر الحذف في بطاقات وجداول الإجازات.
*   **قرعة الطلاب العشوائية التفاعلية (التطبيق والويب)**:
    * إضافة ميزة "القرعة العشوائية" كاختصار سريع وجميل في الصفحة الرئيسية لكل من تطبيق الجوال ولوحة تحكم الويب.
    * تصميم لوحة سحب تفاعلية تدعم شارات توضيحية لعدد الطلاب المتاحين للقرعة وعدد المستبعدين مؤقتاً.
    * دمج مؤثرات حسية واهتزازات اهتزازية متدرجة (Haptic Feedback Ticks) في الجوال، ومؤثرات اهتزازية متوافقة مع المتصفحات (Web Vibrate API) في الويب لزيادة الحماس والتفاعل.
    * إتاحة خيار استبعاد الطالب المسحوب مؤقتاً لتسهيل سحب باقي الطلاب بعدالة، وإعادة إدراجهم بضغطة زر.
    * ربط مباشر لملف الطالب المختار أو بدء جلسة تسميع مباشر له فوراً من نفس الشاشة.
*   **رسالة تأكيد الخروج بنمط داكن فاخر**:
    * اعتراض زر الرجوع الخاص بالنظام على الشاشة الرئيسية (`PopScope`) لمنع خروج المستخدم بطريق الخطأ.
    * تصميم نافذة تأكيد خروج جذابة وداكنة (`#1E2124`) مستوحاة من التصميم المرفق، تضم عنوان "تأكيد!" وعبارة "هل تريد الخروج من تطبيق حلقتي؟".
    * ترتيب وتنسيق الأزرار بما يتناسب مع اتجاه القراءة والواجهة العربية (RTL)، بحيث يظهر زر "حسناً" الأبيض البارز على اليمين وزر "إلغاء" الرمادي الداكن على اليسار، مع استخدام خط `Cairo` الفاخر.

---

### 🟩 الإصدار v1.4.0 (ختم المصحف والمراجعة الدقيقة والنقاط التلقائية)
*التركيز الأساسي: تسهيل تسجيل الطلاب حفظة القرآن الكريم كاملاً، وتحسين دقة المراجعة عبر دعم اختيار نطاقات الآيات بالتفصيل، وتحفيز الطلاب آلياً بمنحهم نقاط مكافأة سلوكية عند تفوقهم على المقدار المقرّر.*

*   **خيار ختم المصحف الشريف كاملاً (Quran Completion)**:
    * إضافة زر تفاعلي بارز "ختم المصحف كاملًا 🏆" في شاشة تسجيل الطالب/الطالبة الجديدة.
    * إظهار نافذة تأكيد لتجنب الإدخال بالخطأ.
    * عند التأكيد، يتم تعبئة نطاق الحفظ المسبق تلقائياً من سورة الفاتحة آية 1 إلى سورة الناس آية 6، وتحديث إحصاءات تقدم المصحف للطلاب كاملاً فوراً.
*   **المراجعة التفصيلية بنطاقات الآيات الدقيقة**:
    * ترقية شاشة المراجعة للطلاب لتحويلها من مجرد "اختيار سورة كاملة" إلى إمكانية اختيار "من آية" و "إلى آية" لكل سورة محددة.
    * إظهار منزلقات dropdowns لاختيار نطاق الآيات بشكل ديناميكي مرن يتقلص ويتسع بناءً على طول السورة والآية المحددة للبدء.
    * تحديث شريط التقرير السفلي (الـ Bottom Sheet) ليحتسب ويعرض إجمالي عدد الآيات المختارة فعلياً للمراجعة عبر كل السور المحددة.
    * تخزين نطاقات الآيات المحددة بدقة في جدول قاعدة البيانات (`MemorizationProgress`) بدلاً من تسجيل السورة كاملة دائماً.
*   **نظام مكافأة النقاط السلوكية التلقائي (Auto Bonus Points)**:
    * تشغيل منطق تحقق ذكي عند حفظ تسميع جديد أو جلسة تسميع مباشر للطلاب.
    * مقارنة عدد الآيات المسمّعة فعلياً بالمقرر اليومي للطالب المحدّد (في حال كان نمط خطته بالآيات `ayahs`).
    * إذا تجاوز الطالب خطته المقررة لليوم، يمنحه التطبيق تلقائياً نقاط مكافأة سلوكية بقيمة نقاط `extra_memorization` (المسجلة في إعدادات الحلقة، والافتراضي لها 2 نقطة).
    * إدراج نقطة السلوك تلقائياً في السجل المالي والأكاديمي للطالب، مع إعلام المعلم برسالة تأكيد تفاعلية (SnackBar) مبهجة: "تم حفظ التسجيل بنجاح، وإضافة X نقاط مكافأة للزيادة 🎉".

---

### 🔵 الإصدار v1.5.0 (المزامنة الشاملة والقرعة المستمرة وسجل التسميع والأيام المعلقة)
*التركيز الأساسي: إتمام ومزامنة كافة الميزات بين تطبيق الجوال والموقع الإلكتروني، وتعزيز كفاءة إدارة الحلقة.*

*   **مزامنة تسجيل الطلاب بالكامل (تحديد المحفوظ المسبق والختم والبدء التلقائي)**:
    * **الويب والجوّال:** إتاحة خيار تحديد الحفظ المسبق بالسور والآيات (من/إلى) مع إدراج زر "ختم المصحف كاملًا 🏆" لتعبئة النطاق كاملاً فوراً.
    * **الويب والجوّال:** عند اختيار اتجاه الحفظ "تصاعدي"، يضبط النظام سورة البدء تلقائياً لـ "سورة الناس" لتسريع الإدخال.
    * **الويب:** أتمتة تعبئة السورة والآية الحالية وبدء الحفظ الجديد تلقائياً من آخر نقطة تسميع منجزة مع حساب المقرر اليومي.
*   **انتقال التسميع التفاعلي التلقائي (Live Recitation Auto-Advance)**:
    * **الجوّال:** تمكين المعلم من تسميع القرآن الكريم آية بآية مع الانتقال التلقائي الفوري (300ms) للآية التالية بمجرد وضع تقييم للآية الحالية لزيادة سرعة التسميع.
*   **القرعة المستمرة وحفظ الحالة (Persistent Raffle State)**:
    * **الويب والجوّال:** حفظ حالة الطلاب المسحوبين والمستبعدين من القرعة في الذاكرة المحلية (`localStorage` / `SharedPreferences`) لمنع تصفير القرعة عند التنقل بين الشاشات، مع توفير زر "إعادة ضبط القرعة" لتصفير الحالة وبدء قرعة جديدة.
*   **نظام السلوك المخصص ولوحة التحكم بنقاط المعلم (Dynamic Rules Dashboard)**:
    * **الويب والجوّال:** تمكين المعلم من إنشاء وتعديل وحذف بنود وقواعد سلوكية مخصصة ديناميكياً (نقاط إيجابية وسلبية) لتنعكس فوراً في شاشات تسجيل السلوك.
*   **تعليق الحلقة وإجازات المسجد العامة (Suspended Days)**:
    * **الويب والجوّال:** دعم خيار "تعليق الحلقة" لليوم وتوليد لافتة تنبيهية واضحة، مع استبعاد هذا اليوم بالكامل من احتساب نسب الحضور والغياب والإحصاءات العامة لعدم التأثير سلباً على درجات الطلاب.
*   **التصحيح التلقائي للحضور عند الإجازات (Auto-Attendance Correction)**:
    * **الويب والجوّال:** معالجة مشكلة بقاء حالة الطالب "غائب" بالرغم من وجود إجازة معتمدة. حيث تم توسيع منطق التصحيح التلقائي ليقوم بتصحيح وتعديل الحضور لـ "مستأذن" (`excused`) ليس فقط عند تسجيل الإجازة الجديدة، بل وعند تحميل شاشة الحضور والتحضير (لتعديل أي سجل غياب سابق)، بالإضافة إلى أتمتة تصحيح السجلات عند اعتماد أو إلغاء أو حذف الإجازة في كلا الواجهتين (الويب والجوال) لضمان اتساق البيانات المطلق.
*   **سجل التسميع اليومي والأكاديمي للطالب (Memorization Timeline)**:
    * **الويب والجوّال:** عرض سجل زمني تفصيلي (Timeline) تحت ملف كل طالب يعرض تواريخ الحفظ والمراجعة، الدرجات المستحقة، الأخطاء المسجلة، وملاحظات وتوصيات المعلم بشكل متناسق ومقروء.
*   **إعطاء الأولوية للطلاب الذين لم يسمعوا (Priority Sorting for Left-out Students)**:
    * **الويب والجوّال:** تمييز الطلاب الذين حضروا الحلقة السابقة ولم يسجلوا حفظاً أو مراجعة بشارة تنبيهية برتقالية مميزة: `"⚠️ لم يسمّع في المرة السابقة"`.
    * **الويب والجوّال:** إضافة خيار فرز/ترتيب ذكي في قائمة الطلاب لوضع الطلاب الذين لم يسمعوا في أعلى القائمة للبدء بهم مباشرة.
*   **سجل الإشعارات والتنبيهات الديناميكي المتفاعل (Interactive System Notifications)**:
    * **الويب والجوّال:** ربط وتفعيل سجل الإشعارات ليعرض تنبيهات تلقائية ديناميكية تغطي الحالات الهامة:
      1. **غياب اليوم:** تنبيه فوري عند رصد غياب الطالب في اليوم الحالي.
      2. **تحذير غياب متكرر:** تنبيه تحذيري عند غياب الطالب لـ 3 أيام متتالية أو أكثر.
      3. **لم يسمّع اليوم:** تنبيه ذكي بعد انتهاء دوام الحلقة للطلاب الذين حضروا ولكنهم لم يسمعوا حفظاً أو مراجعة.
    * **الجوّال:** توليد وحفظ هذه التنبيهات تلقائياً في قاعدة البيانات المحلية SQLite عند تحميل شاشة الإشعارات لمنع التكرار.
    * **الويب:** حساب وتوليد التنبيهات ديناميكياً من بيانات المتجر مع تخزين حالات القراءة والحذف محلياً (`localStorage`) لضمان سرعة التحميل وثبات تجربة المستخدم دون إرهاق السحابة.
*   **حل قيود قاعدة البيانات سحابياً (Supabase Schema Constraint Alignment)**:
    * **الويب والجوّال:** توسيع قيود التحقق (Check Constraints) لعمودي خطة الحفظ (`plan_type`) وحالة الطالب (`status`) لتشمل خيار الحفظ بالأسطر (`lines`) وحالات الطلاب الأخرى في الجوال (`suspended` موقوف، `expelled` مفصول، `graduated` متخرج) لضمان نجاح المزامنة بدون أخطاء `PostgrestException`.
    * **الويب:** تحديث الواجهة والمتجر ودوال احتساب الإنجاز اليومي لدعم نوع الخطة بالأسطر (`lines`) وعرض الشارات والتصنيفات الملائمة.

---

## 🛠️ آلية التوثيق المستمر
يلتزم فريق التطوير والمحرك الذكي بتحديث هذا المستند بعد إنجاز أي ميزة جديدة أو إجراء تعديل رئيسي على بنية التطبيق لضمان تواصل ووعي كامل بالمميزات للمستخدمين والمدرسين.


## P1.22 build 66 hotfix (2026-08-08)

- Fixed Flutter compilation in the student period/WhatsApp report by importing `daily_record.dart` for `DailyActivityType`.
- Study-day suspension now preserves all manually entered `daily_records`; suspended dates are excluded by policy instead of deleting history.
- The P1.22 Supabase migration now bootstraps `public.student_holds` when an older deployment is missing the P3 migration, then applies `scope`, trigger, RLS, and the P1.22 closing logic.
- Student-hold cloud sync now tolerates a missing remote table until the owner runs the migration.
- The previous SQL failure `42P01: relation public.student_holds does not exist` is therefore addressed in this build.

## P1.27 Build 75 Hotfix 1 — توافق Flutter SDK (2026-08-12)

- إصلاح تعارض `flutter pub get` على Flutter SDK الذي يثبت `path 1.9.1` عبر `flutter_test`.
- تحديث اعتماد `path` في `pubspec.yaml` من `1.8.3` إلى `1.9.1`.
- لا يتضمن هذا التصحيح أي migration جديدة، ولا يعيد تطبيق SQL Build 75.
- إذا ظهر في Supabase SQL Editor الخطأ `Failed to fetch (api.supabase.com)` أثناء VERIFY، لا تعتبره فشلًا في migration؛ أعد تشغيل ملف VERIFY فقط بعد استعادة اتصال لوحة Supabase بالمشروع.

