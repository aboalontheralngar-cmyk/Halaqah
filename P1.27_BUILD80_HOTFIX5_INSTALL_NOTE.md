# P1.27 Build 80 Hotfix 5 — Install / Recovery Note

هذا الإصدار مخصص أولًا لاستعادة البيانات بعد فقد SQLite ومنع تكرار حذف التطبيق بسبب Android version downgrade.

1. **قبل تطبيق الكود إن كانت النسخة الحالية ما تزال تفتح:** لا تشغّل مزامنة رفع/ثنائية. استخدم «استعادة من السحابة» واختر أحدث نسخة قبل وقت الحذف إذا ظهرت لك النسخ.
2. طبّق حزمة Hotfix 5 المعدلة فوق مشروع Hotfix 4 الحالي مع إبقاء ملفات الخطوط و`pubspec.lock`.
3. رقم البناء أصبح `80`. لا تخفضه إلى 78 أو 79.
4. شغّل:

```powershell
PowerShell -ExecutionPolicy Bypass -File .\tools\verify_source_prerequisites.ps1
flutter clean
flutter pub get
flutter analyze
flutter test
flutter run
```

5. إذا أوقف `verify_source_prerequisites.ps1` التنفيذ بسبب versionCode أحدث على الهاتف، **لا تتجاوز الفحص ولا تسمح لـFlutter بحذف التطبيق**. ارفع build number أولًا.
6. بعد فتح Build 80: الإعدادات → البيانات والمزامنة → النسخ والحماية → «استعادة من السحابة» → اختر أحدث نسخة قبل فقد البيانات.
7. بعد نجاح الاستعادة تحقق من العدد الظاهر في رسالة النجاح ومن شاشة الطلاب.
8. بعد ذلك فقط نفّذ «رفع تغييرات الجهاز». إذا نجح، اختبر المزامنة الثنائية.
9. لا يوجد SQL جديد لهذا الإصدار.
