# P1.27 Build 84 Hotfix 9 - analyzer clean patch

This tiny patch is intended to be copied over the existing Build 84 Hotfix 9 source before `flutter run`.

It only addresses the six `flutter analyze` findings reported on 2026-08-17:

- Replaces the four deprecated `DropdownButtonFormField.value` uses in the connected revision range picker with `initialValue`, while adding stable `ValueKey`s so dependent selections still refresh correctly.
- Removes two redundant non-null assertions in the incremental cloud-sync cursor advancement path.

No SQL changes are required. No database schema or sync contract is changed by this patch.

Run after copying:

```powershell
flutter analyze
flutter test
```

Only continue to `flutter run` after `flutter analyze` reports `No issues found!` and all tests pass.
