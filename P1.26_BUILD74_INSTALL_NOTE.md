# P1.26 build 74 — installation note

**Release:** `4.3.0-alpha.20+74`  
**SQLite:** `24` (unchanged)  
**Date:** 2026-08-12

هذه الحزمة ترقية مصدر فوق Build 73 وتجمع إصلاحات التدقيق 1–14 مع إصلاح بوابة الجهات الإشرافية.

## المسار الموصى به

استخدم حزمة **modified-files-only** فوق مشروعك الحالي. هذا هو المسار الأكثر أمانًا لأن حزمة المصدر التي استلمناها لا تحتوي ملفات Tajawal الثنائية عمدًا، بينما مشروعك الحالي يفترض أنه يحتفظ بها.

1. خذ نسخة احتياطية من مجلد المشروع وقاعدة Supabase.
2. أغلق VS Code وخوادم Flutter/Next.js.
3. فك حزمة الملفات المعدلة فوق جذر المشروع مع السماح بالاستبدال والحفاظ على بنية المجلدات.
4. لا تحذف ملفات غير موجودة في الحزمة، وبالأخص `assets/fonts/Tajawal-400.ttf` و`assets/fonts/Tajawal-700.ttf`.
5. نفّذ SQL Build 74 الموضح أدناه.
6. شغّل فحوص Flutter والويب قبل النشر.

## SQL المطلوب لقاعدة Supabase الحالية

لا تشغّل `website/database_schema.sql` ولا تعاود P7.3 القديمة. بناءً على مخطط القاعدة الحالي، جداول الإشراف موجودة بالفعل؛ Build 74 يصلح العقود المحيطة بها ويحافظ على البيانات.

من Supabase SQL Editor انسخ **محتوى** هذا الملف كاملًا وشغّله مرة واحدة:

```text
website/supabase/P1.26_BUILD74_APPLY.sql
```

الملف يجمع بالترتيب:

1. `20260812000100_build74_supervision_hardening.sql`
2. `20260812000200_build74_atomic_family_operations.sql`

العمليات idempotent ومصممة لقبول إعادة التشغيل عند الحاجة. لا يحتوي الإصلاح على حذف للمراكز أو الجهات الإشرافية أو الطلاب.

بعد النجاح شغّل الملف القرائي فقط:

```text
website/supabase/P1.26_BUILD74_VERIFY.sql
```

المطلوب أن تكون كل قيمة في عمود `passed` مساوية لـ`true`. إذا فشل صف، لا تحذف الجهة ولا تعِد P7.3؛ احتفظ بنتيجة الفحص لتحديد العقد المفقود.

## فحص Flutter

على Windows:

```powershell
PowerShell -ExecutionPolicy Bypass -File .\tools\verify_source_prerequisites.ps1
flutter analyze
flutter test
flutter run
```

على macOS/Linux:

```bash
./tools/verify_source_prerequisites.sh
flutter analyze
flutter test
flutter run
```

الفحص المسبق يمنع البناء إذا فُقد خط Tajawal، ويولّد `pubspec.lock` عند غيابه ثم يفرض وجوده. بعد أول `flutter pub get` احفظ `pubspec.lock` في المستودع/نسختك المحلية؛ `.gitignore` لم يعد يتجاهله.

### بناء Release

Build 74 يمنع Release من استخدام Supabase الافتراضي بصمت. عرّف قبل البناء:

```text
SUPABASE_URL
SUPABASE_PUBLISHABLE_KEY
```

ثم استخدم `tools/build_lean_android.ps1` أو `tools/build_lean_android.sh`. لا تستخدم `service_role` في التطبيق.

## فحص الويب

```powershell
PowerShell -ExecutionPolicy Bypass -File .\tools\setup_web.ps1
```

أو:

```bash
cd website
npm ci
npm run quality:ci
```

## ملاحظة حزمة الاستبدال الكاملة

حزمة **full-source-upgrade** تحتوي كل ملفات المصدر بعد Build 74، لكنها لا تعيد توزيع ملفات الخطوط الثنائية التي كانت غائبة أصلًا. إذا استبدلت المشروع كاملًا، انسخ مجلد `assets/fonts` من مشروعك الحالي قبل تشغيل Flutter. لذلك يبقى **modified-files-only** الخيار المفضل لهذا التسليم.
